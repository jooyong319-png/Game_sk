# 신작 가이드(블로그) 글 작성 규격 — 작가 에이전트 필독

이 문서는 **신작 가이드 글을 자동/주기적으로 작성하는 에이전트**를 위한 규격이다.
글 1편을 쓸 때마다 이 문서를 먼저 읽고, 아래 규칙을 **그대로** 지킨다.

> ⚠️ 이 파일(BLOG_AUTHORING.md)은 `content/blog/`가 **아니라** 레포 루트에 있다.
> `content/blog/` 안의 모든 `.md`는 자동으로 발행되므로, **글이 아닌 파일을 거기 두지 말 것.**

---

## 1. 글이 발행되는 방식 (자동)

- 파일을 `content/blog/<slug>.md`로 만들면 **그 자체로 발행**된다.
  - `<slug>`(파일명, `.md` 제외) = URL. 예: `content/blog/2026-07-monthly.md` → `https://gcalen.com/blog/2026-07-monthly`
- 목록 페이지 `/blog`("신작 가이드")에 **frontmatter의 `date` 내림차순(최신 먼저)**으로 자동 노출.
- `sitemap.xml`에 URL이 **자동 등록** → 구글/네이버 수집.
- Vercel이 push를 감지해 자동 빌드. 별도 등록·코드수정 불필요.

## 2. 파일명(slug) 규칙

- 케밥케이스, 영문/숫자만. 한글·공백·대문자 금지.
- **날짜 프리픽스 권장**: `YYYY-MM-...` (정렬·중복방지에 유리)
- 예시: `2026-07-monthly-releases`, `2026-q3-mmorpg-server-guide`, `2026-summer-mobile-top10`
- **이미 존재하는 slug와 겹치지 말 것** (덮어쓰기 사고 방지). 작성 전 `content/blog/` 목록 확인.

## 3. Frontmatter (필수 — 형식 엄격)

파일 **맨 첫 줄부터** 정확히 아래 형태. 파서가 단순해서 규칙을 어기면 깨진다.

```
---
title: 한 줄 제목 (따옴표 없이)
description: 한 줄 설명. 검색 스니펫·목록 요약으로 쓰임. 줄바꿈 금지.
date: 2026-07-01
tags: [태그1, 태그2, 태그3]
---
```

- 모든 값은 **한 줄**. `description`에 줄바꿈 금지(여러 줄로 쓰면 잘림).
- `date`는 `YYYY-MM-DD`. 미래 날짜 쓰지 말 것(작성 시점 기준 실제 날짜).
- `tags`만 `[ ]` 배열. 3~6개 권장, 검색 키워드 위주(예: `[7월신작, 모바일게임, 출시일정]`).
- `title`/`description`은 **검색 노출 카피**다. 핵심 키워드(연·월, 플랫폼, "신작/출시/TOP/추천/신규 서버")를 자연스럽게 포함.

## 4. 본문 마크다운 — 지원 문법만 사용 (중요)

본문 파서는 외부 라이브러리 없이 동작하며 **아래만** 지원한다. 그 외 문법은 깨지므로 쓰지 말 것.

**쓸 수 있는 것**
- 제목: `#`, `##`, `###` (h1~h3까지만)
- 굵게 `**굵게**`, 이탤릭 `*기울임*`
- 링크 `[보이는 텍스트](/game/some-id)`
- 목록: `- 항목` (불릿만)
- 코드블록: ```triple backtick```
- 단락: 빈 줄로 구분. 단락 안 한 줄 줄바꿈은 `<br/>`로 처리됨.

**쓰면 안 되는 것 (깨짐)**
- 이미지 `![](...)` ❌ (링크 문법과 충돌)
- 표 `| a | b |` ❌
- 번호 목록 `1. ` ❌ (불릿 `-`로 대체)
- 인용 `> ` ❌
- 인라인 코드 백틱 1개 `` `x` `` ❌ (3중 백틱 블록만 됨)
- `####` 이상, 수평선 `---`(본문 중) ❌

## 5. SEO 핵심 — 내부 링크 (가장 중요)

가이드 글의 본업은 **검색 유입 → 내부 페이지 회유**다. 글마다 내부 링크를 충분히 넣는다.

- **게임 이름은 상세페이지로 링크**: `[게임명](/game/<id>)`
  - `<id>`는 반드시 **`data/games.json`의 실제 `id`**를 써야 한다(없는 id는 404). 작성 전 JSON에서 id·release_date·name_ko를 가져와 사용.
- **카테고리 랜딩 페이지로도 링크**(문맥에 맞게):
  - `/upcoming-games`(출시 예정) · `/new-servers`(신규 서버·이벤트) · `/mobile-games`(모바일) · `/pc-console-games`(PC·콘솔) · `/global-games`(글로벌)
- 한 글에 게임 상세 링크 **8개 이상** 목표. 날짜/플랫폼/장르 등 사실은 JSON 기준으로 정확히.

## 6. 글 길이·품질

- 도입 단락(2~4문장) → 본문(게임/주제별 `##` 섹션) → 마무리(내부 링크로 유도).
- 사실 왜곡 금지. 출시일·플랫폼·개발사는 `data/games.json`과 일치해야 함.
- 단순 데이터 나열 X. 한 게임당 1~3문장의 **고유한 코멘트**(왜 주목할 만한지)를 붙인다(AdSense "독자 콘텐츠" 신호).

## 7. 주제 아이디어 (주기별)

- **매월 1일**: "YYYY년 M월 출시 예정 게임 총정리" — 그 달 release_date 게임 모음 (가장 기본)
- **분기/시즌**: "YYYY 하반기/여름 기대작 TOP 10" (플랫폼·카테고리별)
- **신규 서버**: "이번 달 리니지·메이플 등 신규 서버·대규모 이벤트 일정"
- **카테고리 특화**: "이달의 모바일 신작" / "주목할 글로벌 대작"
- 같은 달 같은 주제 **중복 발행 금지**(이미 있으면 스킵).

## 8. 마무리 절차

1. `content/blog/<slug>.md` 작성 (위 규격 준수)
2. 로컬 빌드 시도 X (샌드박스 제한). 형식만 self-check.
3. 커밋 → `git fetch` → `git rebase origin/main` → push (리서처 커밋과 충돌 대비)
4. 커밋 메시지 예: `[작가] 2026-07 월간 출시 정리 발행`
