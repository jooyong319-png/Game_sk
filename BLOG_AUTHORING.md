# 신작 가이드(블로그) 글 작성 규격 — 작가 에이전트 필독

이 문서는 **신작 가이드 글을 자동/주기적으로 작성하는 에이전트**를 위한 규격이다.
글 1편을 쓸 때마다 이 문서를 먼저 읽고, 아래 규칙을 **그대로** 지킨다.

> ⚠️ 이 파일(BLOG_AUTHORING.md)은 `content/blog/`가 **아니라** 레포 루트에 있다.
> `content/blog/` 안의 모든 `.md`는 자동으로 발행되므로, **글이 아닌 파일을 거기 두지 말 것.**

---

## 1. 글이 발행되는 방식 (자동)

- 파일을 `content/blog/<slug>.md`로 만들면 **그 자체로 발행**된다.
  - `<slug>`(파일명, `.md` 제외) = URL. 예: `content/blog/2026-07-monthly.md` → `https://gcalen.com/blog/2026-07-monthly`
- 목록 페이지 `/blog`("신작 가이드")에 **frontmatter의 `date` 내림차순(최신 먼저)**으로 자동 노출.
- `sitemap.xml`에 URL이 **자동 등록** → 구글/네이버 수집.
- Vercel이 push를 감지해 자동 빌드. 별도 등록·코드수정 불필요.

## 2. 파일명(slug) 규칙

- 케밥케이스, 영문/숫자만. 한글·공백·대문자 금지.
- **날짜 프리픽스 권장**: `YYYY-MM-...` (정렬·중복방지에 유리)
- 예시: `2026-07-monthly-releases`, `2026-q3-mmorpg-server-guide`, `2026-summer-mobile-top10`
- **이미 존재하는 slug와 겹치지 말 것** (덮어쓰기 사고 방지). 작성 전 `content/blog/` 목록 확인.

## 3. Frontmatter (필수 — 형식 엄격)

파일 **맨 첫 줄부터** 정확히 아래 형태. 파서가 단순해서 규칙을 어기면 깨진다.

```
---
title: 한 줄 제목 (따옴표 없이)
description: 한 줄 설명. 검색 스니펫·목록 요약으로 쓰임. 줄바꿈 금지.
date: 2026-07-01
tags: [태그1, 태그2, 태그3]
---
```

- 모든 값은 **한 줄**. `description`에 줄바꿈 금지(여러 줄로 쓰면 잘림).
- `date`는 `YYYY-MM-DD`. 미래 날짜 쓰지 말 것(작성 시점 기준 실제 날짜).
- `tags`만 `[ ]` 배열. 3~6개 권장, 검색 키워드 위주(예: `[7월신작, 모바일게임, 출시일정]`).
- `title`/`description`은 **검색 노출 카피**다. 핵심 키워드(연·월, 플랫폼, "신작/출시/TOP/추천/신규 서버")를 자연스럽게 포함.

## 4. 본문 마크다운 — 지원 문법만 사용 (중요)

본문 파서는 외부 라이브러리 없이 동작하며 **아래만** 지원한다. 그 외 문법은 깨지므로 쓰지 말 것.

**쓸 수 있는 것**
- 제목: `#`, `##`, `###` (h1~h3까지만)
- 굵게 `**굵게**`, 이탤릭 `*기울임*`
- 링크 `[보이는 텍스트](/game/some-id)`
- 목록: `- 항목` (불릿만)
- 코드블록: ```triple backtick```
- 단락: 빈 줄로 구분. 단락 안 한 줄 줄바꿈은 `<br/>`로 처리됨.

**쓰면 안 되는 것 (깨짐)**
- 이미지 `![](...)` ❌ (링크 문법과 충돌)
- 표 `| a | b |` ❌
- 번호 목록 `1. ` ❌ (불릿 `-`로 대체)
- 인용 `> ` ❌
- 인라인 코드 백틱 1개 `` `x` `` ❌ (3중 백틱 블록만 됨)
- `####` 이상, 수평선 `---`(본문 중) ❌

## 5. SEO 핵심 — 내부 링크 (가장 중요)

가이드 글의 본업은 **검색 유입 → 내부 페이지 회유**다. 글마다 내부 링크를 충분히 넣는다.

- **게임 이름은 상세페이지로 링크**: `[게임명](/game/<id>)`
  - `<id>`는 반드시 **`data/games.json`의 실제 `id`**를 써야 한다(없는 id는 404). 작성 전 JSON에서 id·release_date·name_ko를 가져와 사용.
- **카테고리 랜딩 페이지로도 링크**(문맥에 맞게):
  - `/upcoming-games`(출시 예정) · `/new-servers`(신규 서버·이벤트) · `/mobile-games`(모바일) · `/pc-console-games`(PC·콘솔) · `/global-games`(글로벌)
- 한 글에 게임 상세 링크 **8개 이상** 목표. 날짜/플랫폼/장르 등 사실은 JSON 기준으로 정확히.

## 5-1. 대표 이미지·관련 글 (자동 연동 — 작가가 별도 이미지 삽입 불필요)

글을 발행하면 다음이 **자동**으로 처리된다. 작가는 이미지 태그를 쓰지 않는다(§4에서 이미지 문법은 금지).

- **대표 이미지 = 본문 첫 번째 `/game/<id>` 링크의 게임 이미지.** 이 이미지가 글 상단 히어로, `/blog` 목록 썸네일, OG(공유 미리보기), 구조화데이터(JSON-LD) `image`에 자동으로 쓰인다.
  - 따라서 **첫 게임 링크는 그 글을 대표하는 화제작으로 고르고, 반드시 `image_url`이 있는 게임**을 써라. `image_url`이 `null`인 게임을 첫 링크로 두면 히어로가 비고 기본 og-image로 폴백된다(품질↓).
  - 작성 후 `data/games.json`에서 첫 링크 게임의 `image_url`이 존재하는지 확인할 것.
- **관련 글**은 상세페이지 하단 "함께 보면 좋은 가이드"에 **태그 겹침 수** 기준으로 3개 자동 노출된다. 시리즈성 글(같은 월·플랫폼·주제)은 태그를 일부 공유하도록 달아 서로 연결되게 하라(내부 회유↑).

## 6. 글 길이·품질

- 도입 단락(2~4문장) → 본문(게임/주제별 `##` 섹션) → 마무리(내부 링크로 유도).
- 사실 왜곡 금지. 출시일·플랫폼·개발사는 `data/games.json`과 일치해야 함.
- 단순 데이터 나열 X. 한 게임당 1~3문장의 **고유한 코멘트**(왜 주목할 만한지)를 붙인다(AdSense "독자 콘텐츠" 신호).

## 7. 주제 로테이션 — 주 4회 발행 (ISO 주차·4-슬롯 슬레이트, 상태 저장 불필요)

매주 4편. 스케줄러가 주 4회(월·수·금·일) 실행되며, **매 실행마다 그 주(ISO 주차)의 4개 슬롯 중 아직 비어 있는 슬롯 하나**를 채운다. 슬롯 성격이 서로 달라 한 주에 총정리·비교·가이드·읽을거리가 골고루 나온다.

주차 식별: ISO 주차. `date +%G-W%V`로 구한다(예: 2026-07-09 → `2026-W28`). slug에는 소문자 `w28`을 쓴다.

**매 실행 절차**
- 1) 오늘의 ISO 주차 `wNN`을 구한다.
- 2) 이번 주 4개 슬롯 slug 중 `content/blog/`에 이미 있는 것을 확인한다.
- 3) 비어 있는 슬롯 중 **A→B→C→D 순으로 가장 앞선 것 1편**을 작성한다. 이미 있는 slug는 **절대 덮어쓰지 않는다.**
- 4) 4개가 다 찼거나 해당 슬롯 데이터가 빈약(§7-4)하면 → 에버그린 백업(§7-4)에서 미발행 주제 1편으로 대체한다.

### 7-1. 4개 슬롯 (성격·slug·내용)
- **A. 총정리(백본)** — slug `YYYY-wNN-roundup` — 데이터·목록형. 그 주 시의성 있는 출시 묶음. 소주제는 §7-2의 '월 주차'로 정한다.
- **B. 비교·대결** — slug `YYYY-wNN-versus` — 구매결정형. 같은 시기 겹치는 2~4작을 장르·플랫폼·가격 기준으로 비교("A vs B, 뭘 살까"). 대안·비슷한 게임도 링크로 엮는다.
- **C. 독자·상황 가이드** — slug `YYYY-wNN-guide` — 하우투형. '누구에게' 기준(복귀 유저 MMO, 스위치2·PC 입문, 예산·상황별, 사전예약 활용법 등)으로 신작을 큐레이션.
- **D. 데이터·IP 읽을거리** — slug `YYYY-wNN-story` — 에세이·인사이트형. 시리즈·개발사 계보, 출시일 데이터 트렌드, 한국어 지원작·뽑기 없는 B2P 등 테마 큐레이션.

### 7-2. 슬롯 A 소주제 — 월 주차별(반복 방지)
`월 주차 = ceil(오늘 일자 / 7)`로 그 달 몇째 주인지 계산해 A의 소주제를 고른다.
- **1주**: 이달 출시 총정리(그 달 `release_date` 게임 전부, 날짜·카테고리 골고루)
- **2주**: D-14 출시 임박작(오늘~14일 내 출시·업데이트)
- **3주**: 월별 카테고리 특집(§7-2b)
- **4주**: 이달 신규 서버·대규모 업데이트(`new_server`) 정리
- **5주**(있을 때): 다음 달 미리보기 또는 에버그린 백업

B·C·D 슬롯은 매주 소주제를 바꾼다. **최근 4주 내 같은 소주제·같은 IP를 반복하지 말 것.**

#### 7-2b. 월별 카테고리 특집 (그 달 3주차 슬롯 A)
- 1·4·7·10월: 모바일 신작 특집(`mobile_kr`)
- 2·5·8·11월: PC·콘솔 신작 특집(`pc_console_kr`)
- 3·6·9·12월: 글로벌 대작 특집(`global_aaa`)

카테고리 매핑: 모바일=`mobile_kr`, PC·콘솔=`pc_console_kr`, 글로벌=`global_aaa`, 신규 서버=`new_server`.

### 7-3. 시즌 특집 (해당 주 슬롯 하나를 덮어씀 — 우선)
아래 조건이면 그 주 슬롯 A(또는 성격이 맞는 슬롯) 자리에 시즌 특집을 넣고, 나머지 슬롯은 평소대로 채운다. 연 1회성이라 slug에 주차를 넣지 않는다.
- **1월 1주**: 올해 최고 기대작 미리보기 (`YYYY-preview-top`)
- **3월/9월 1주**: 분기 라인업 (`YYYY-q2-lineup` / `YYYY-q4-lineup`)
- **6월 3주**: 하반기 기대작 TOP 10 (`YYYY-h2-anticipated-top10`)
- **12월 4주**: 올해의 신작 결산 + 내년 미리보기 (`YYYY-year-review`)

### 7-4. 공통 규칙 (얇은 글·중복 방지 — 중요)
- 작성 전 `content/blog/` 목록 확인 → 채우려는 slug가 **이미 있으면 그 슬롯은 건너뛰고** 다음 빈 슬롯으로. (중복 slug 덮어쓰기 절대 금지.)
- **데이터가 빈약하면 강행 금지.** 대상 게임이 **5개 미만**이면: 1) 인접 카테고리·기간을 합쳐 보강, 2) 슬롯 A(총정리)로 대체, 3) 그래도 빈약하면 에버그린 백업으로 대체.
- **에버그린 백업**(미발행 택1): `YYYY-mmo-lineup`(신작 MMORPG 총정리) · `YYYY-mobile-preorder-now`(지금 사전예약 모바일) · `YYYY-imminent-d14`(출시 임박 D-14) · `YYYY-<platform>-lineup`(플랫폼별 신작) · `YYYY-korean-subtitle-games`(한국어 지원 신작) · `YYYY-b2p-no-gacha`(뽑기 없는 패키지). 이마저 대상 5개 미만이면 그때만 그 슬롯 스킵.
- **길이·링크**: 매 글 600자 이상. 게임 상세 `/game/<id>` 링크는 **총정리(A) 8개 이상, 비교·가이드·읽을거리(B·C·D) 6개 이상**. 항상 카테고리 랜딩 링크(§5) 1개 이상 포함.
- **대표 이미지**(§5-1): 본문 첫 `/game/<id>` 링크는 `image_url`이 있는 화제작으로.
- **시리즈 연결**: 같은 슬롯·같은 주제 계열은 tags를 일부 공유해 하단 "함께 보면 좋은 가이드"로 서로 연결.

## 8. 마무리 절차

1. `content/blog/<slug>.md` 작성 (위 규격 준수)
2. 로컬 빌드 시도 X (샌드박스 제한). 형식만 self-check.
3. 커밋 → `git fetch` → `git rebase origin/main` → push (리서처 커밋과 충돌 대비)
4. 커밋 메시지 예: `[작가] 2026-07 월간 출시 정리 발행`
