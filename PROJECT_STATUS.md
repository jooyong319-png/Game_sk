# 프로젝트 현재 상태

마지막 갱신: 2026-05-29 (기획자 사이클 — 푸터 완료 처리 + 캘린더 큐 재정렬)

## 현재 단계
Phase 1 — 정적 JSON 기반 게임 출시 캘린더 (3개 카테고리 + new_server)

## 🔁 방향 전환 (2026-05-27 07:30)
사이트 이름이 "게임 출시 **캘린더**"인데 그동안 카드 그리드만 있었음.
유저 피드백 반영: **메인 뷰를 월간 캘린더로 전환**, 카드 그리드는 "리스트 뷰" 토글로 보존.
캘린더는 한 사이클에 전부 못 만드니까 5단계로 쪼갰음. 한 사이클당 1단계.

## 아키텍처
- **프론트** → `/data/games.json` (리서처 Claude가 매일 9시 갱신)
- RAWG API 의존성 없음. 모든 데이터는 리서처 Claude가 WebSearch로 큐레이션.

## 완료한 기능
- [x] 프로젝트 스켈레톤 (HTML/CSS/JS)
- [x] 정적 JSON 데이터 파일 (`data/games.json`)
- [x] 카테고리 필터 (국내 모바일 / 국내 PC/콘솔 / 글로벌 대작)
- [x] 플랫폼 필터 (PC, PS5, Xbox, Switch, iOS, Android)
- [x] 기간 필터 (30일~1년, 전체)
- [x] D-Day 표시 + 출시 임박 강조 (7일 이내 노란 보더)
- [x] 모바일 반응형
- [x] 다크 테마 디자인
- [x] 카테고리별 색상 구분
- [x] 게임 카드 클릭 시 상세 모달 (X / 배경 / ESC 닫기, body 스크롤 잠금)
- [x] 월간 캘린더 뷰 1단계: 그리드 뼈대 (현재 월, 7x6, 오늘 강조, 이전·다음 달 칸 흐리게)
- [x] 월간 캘린더 뷰 2단계: 셀에 그날 출시 게임 카테고리 점(dot) 표시 (최대 3개 + "+N", title 툴팁)
- [x] **푸터 운영자 정보 교체** (사용자 요청 / 개발 완료 05-27 09:30, QA 소스 검증 통과 09:40)

## 다음 TODO (우선순위 순)

### 1순위 — 캘린더 3단계: 이전/다음 달 네비게이션
헤더 "2026년 5월" 좌우에 `<` `>` 버튼 추가 + "오늘로" 버튼. 상태 변수 `viewYear`/`viewMonth` 도입.
버튼 클릭 시 해당 값 변경 후 `renderCalendar()` 재호출. 헤더 라벨도 상태 기반으로 갱신.
점(dot) 표시도 이동한 달 기준으로 다시 그려져야 함(6월로 가면 6월 게임 점 보이게).

### 2순위 — 캘린더 4단계: 셀 클릭 → 그날 게임 목록 → 기존 모달 재사용
날짜 셀 클릭 시 그날 출시 게임 리스트가 캘린더 바로 아래 패널에 펼쳐짐(날짜 제목 + 게임명 줄 목록).
게임 항목 클릭 시 이미 만들어진 `openModal()` 재사용. 게임 없는 날 클릭 시 패널 닫기 또는 "출시 예정 게임 없음" 표시.

### 3순위 — 캘린더 5단계: 캘린더/리스트 뷰 토글
상단에 토글 버튼 2개 (📅 캘린더 / 📋 리스트). 선택값 localStorage(`viewMode`)에 저장, 다음 방문 시 복원.
캘린더 선택 시 카드 그리드 숨김, 리스트 선택 시 캘린더 숨김. 필터 바는 양쪽에서 공통 노출.

### 4순위 — 검색 기능 (게임명 부분 일치)
header(필터 바)에 검색 input 1개 추가. 입력 시 `name_ko`/`name_en` 부분 일치(대소문자 무시)로 카드 그리드 실시간 필터링.
기존 카테고리/플랫폼/기간 필터와 AND로 결합. 빈 입력이면 필터 해제.

### 5순위 — 카테고리별 개수 뱃지
각 카테고리 필터 버튼 라벨 옆에 현재(기간 필터 적용 후) 해당 카테고리 게임 수를 작은 뱃지로 표시.
예: `글로벌 대작 (10)`. 기간 필터 변경 시 숫자도 갱신.

### (이후) — 위시리스트(localStorage) / 빈 상태 안내 / IDEAS 항목

## 알려진 버그 (BUGS)
- (없음) — 푸터 배포 캐시 잔존은 QA가 커밋 후 ~15분 지연으로 추정, 시간 경과로 자연 해소 예상. 다음 QA 사이클에서 gcalen.com 재확인 권장.

## 개선 아이디어 (IDEAS)
- 출시일별 그룹핑 (리스트 뷰 옵션)
- 한국 게임 vs 글로벌 게임 통계 차트
- 게임 트레일러 YouTube 임베드
- 카카오톡 공유 기능
- 일간/주간 뷰 (월간 안정화 후)

## 최근 변경 로그
- 2026-05-29 [기획자] 푸터 교체 완료 처리(완료한 기능으로 이동, USER_REQUESTS 아카이브). 캘린더 3·4·5단계를 1·2·3순위로 당기고 검색/개수 뱃지를 4·5순위로 추가 → 큐 5개 유지.
- 2026-05-27 09:40 [QA] 푸터 소스 검증 통과(배포 캐시 잔존만 관찰).
- 2026-05-27 09:30 [개발자] 푸터 교체 완료(운영자 정보 2줄, AI 협업 문구·GitHub 링크 제거).
- 2026-05-27 08:30 [기획자] 사용자 직접 요청 반영: 푸터 교체를 1순위로 끼움.
- 2026-05-27 08:20 [개발자] 캘린더 2단계 완료: 셀 카테고리 색상 점 표시.
- 2026-05-27 07:50 [개발자] 캘린더 1단계 완료: 7x6 그리드 뼈대.
- 2026-05-27 07:30 [기획자] 방향 전환: 메인 뷰를 월간 캘린더로 → 5단계 분할.
- 2026-05-27 07:20 [개발자] 게임 카드 클릭 시 상세 모달 구현.
- 2026-05-27 [Phase 0.5] RAWG API 제거, 정적 JSON + 리서처 Claude 구조로 전환.
- 2026-05-27 [Phase 0] 초기 스켈레톤 + GitHub/Vercel 셋업.
