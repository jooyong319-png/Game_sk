# 👤 사용자 요청 사항

사용자가 직접 기획자 Claude한테 시키고 싶은 작업을 여기 적습니다.
기획자는 매 사이클마다 이 파일을 가장 먼저 읽고, 새 요청이 있으면 **최우선 TODO로 처리**합니다.

## 사용 방법

새 요청을 위에 추가하고 GitHub에 push만 하면 됩니다. (또는 GitHub 웹에서 직접 편집)

## 형식

```
## [YYYY-MM-DD] (제목)
설명: 구체적으로 무엇을 원하는지
우선순위: 높음 / 보통 / 낮음
상태: 대기 중 / 처리 중 / 완료 / 보류
```

기획자가 처리 시작하면 "처리 중", TODO로 들어가면 그 TODO ID를 적어둠.
개발자가 완료하면 "완료"로 바꿈.

---

## 활성 요청 (위가 최신)

## [2026-06-09] 상단 광고자리 제거 (전 표면)
**설명**: 운영자 직접 지시 "광고 상단자리 제거". 범위 확인 결과 '전 표면' — `size="top"` 광고 슬롯 5곳(메인 main-top·게임상세 detail-top·블로그 인덱스/포스트 blog-index-top·blog-post-top·SEO 랜딩 landing-top) 전부 제거. **하단 광고(`size="mid"`)는 유지.** AdSlot 컴포넌트 자체는 mid에서 계속 쓰이므로 보존.
**우선순위**: 높음
**상태**: ✅ **완료** (개발자 9984992 — `size="top"` 5곳 제거·하단 mid 유지·코드 grep 0건. 라이브 검증 QA 대기)
**메모**: 정리(declutter) §A.4 '광고 비중 축소'와 정합. 미니멀 리셋 Phase 0~3b는 개발자 출고 완료(코드 확인).


## [2026-06-09] 디자인 정리(미니멀 리셋) — "너무 지저분하다" + 상세 페이지 개선 + 필터 3컬럼
**설명**: 운영자 라이브 피드백. ① 전반 디자인이 지저분 → 색 가짓수·그라데/이펙트·요소 밀도·칩 4축 정리 ② UX: 검색/필터를 제거가 아니라 **3컬럼으로 깔끔하게 정렬** ③ **게임 상세 페이지(/game/[id]) 디자인 개선** ④ **라이트/다크 테마 토글 — 화이트를 기본 톤으로**(운영자 추가, "리액트 기능으로 가능할 것") ⑤ **헤더 미니멀 확정**(워드마크+위시·테마토글) ⑥ **데스크톱 3컬럼 레이아웃 확정** ⑦ **컬럼 패널 색 구분(div 래핑)** + **하단 광고 제거** + **모바일 디자인 수정**(가로칩·리스트 기본 권고). 색구분 **B 확정**(동일 흰 패널+라벨헤더+보더, warm은 출시임박 헤더만 — 운영자 "B로 가자"). §H 참조.
**우선순위**: 높음
**상태**: ✅ **완료** (전 범위 출고·회귀 0 — Phase 0 테마 라이트기본+다크토글 / Phase 1 색 3역할+#4a90e2 청산 / Phase 2 그라데·이펙트 평면화 / Phase 3 칩 단일+통계 중립+상세 위계/스펙2열 / §F 데스크톱 3컬럼(좌 sticky 카테고리 레일·중 본문·우 출시임박 레일·모바일 1컬럼 스택) / §E 헤더 미니멀 / 상단 광고 제거. 라이브 전수 검수는 다음 TODO QA 1건으로 추적. 기획자 12:30 종결)
**메모**: 승인된 정리 방향이라 기존 '외형 추가' 큐(핫카드 카운트다운·필터 아이콘칩 등 장식 추가성)는 본 정리와 상충 → 보류 권장. 신규 색 0(기존 토큰 재사용·카테고리 4색 톤다운). DESIGN_NOTES 17:40에 hex/셀렉터/파일 전량 명시.


---

## 보류 중 (정식 출시 가까워질 때 처리)

### [2026-05-27] SEO 기본 설정
**설명**:
- 메타 태그 (`<title>`, `<meta description>`, `<meta keywords>`)
- Open Graph 태그 (카톡/페북 공유 미리보기)
- Twitter Card 태그
- `robots.txt` 추가
- `sitemap.xml` 추가
- favicon 설정
- 구조화 데이터 (JSON-LD `VideoGame` / `Event` 스키마)

**우선순위**: 보류 (정식 출시 1-2주 전에 처리)
**상태**: 보류
**메모**: 사용자가 정식 출시 시점에 다시 활성화 요청할 예정. 그전까지는 작업하지 마.

---

## 처리 완료된 요청 (아카이브)

### [2026-06-04] 진입 시 '오늘 이후'가 바로 보이게 (리스트 기본 + 캘린더 오늘 셀 자동선택) — ✅ 완료
**설명**:
- (리스트뷰) 기간 기본값을 '전체(과거+미래)' → **'오늘 이후'**로. 진입 시 리스트가 오늘부터 시작. 과거(최근 출시)는 기간 필터로 전환해 볼 수 있게 유지(완전 제거 X).
- (캘린더뷰) 진입 시 **오늘 날짜 셀이 디폴트로 선택(클릭)된 상태**로 → 아래 day-detail 패널('그날 이후 출시 목록')이 자동으로 떠 있게. 현재는 진입 직후 패널이 비어 있어 아래에 아무 목록도 안 보임.
**우선순위**: 높음
**상태**: ✅ **완료** — ①리스트 '오늘 이후' 완료(개발자 00:20 5ba8af8 · QA 라이브 검증 00:48: 기본 '총 27개'·과거 0건·'전체(과거 포함)' 44 복원·캘린더 과거 달 무파손·콘솔 0) / ②캘린더 오늘 셀 자동선택 완료(개발자 01:30 012212e · QA 라이브 검증 01:47: 진입 직후 오늘 셀(06.04) 자동선택·day-detail '이후 출시 20건' 자동 노출·진입 scrollY=0 스크롤 점프 없음·월 이동 시 해제 정상·콘솔 #418/#423/#425 0건) — 기획자 02:10 종결
**메모**: 캘린더는 이미 '현재 달 오픈 + 오늘 셀 강조'는 구현됨(`Home.tsx` calendarCursor·useEffect) — 남은 건 '오늘 셀 자동 선택'뿐. 리스트 과거는 '기본만 오늘 이후, 필터로 과거 접근' 방식으로 사용자 확정.


### [2026-06-02] 모바일 "🔥 출시 임박" 스트립이 화면을 다 잡아먹음 (인벤 캘린더 참고) — ✅ 완료
운영자 피드백 "출시 임박 배너, 모바일에서 너무 불편함" 해소. 모바일(≤480px) HeroStrip을 큰 세로 카드 1열 스택(~550px) → 가로 컴팩트 행(min-height 44px·카테고리 색점+게임명+우측 D-day)으로 재작성 + 노출 3건 제한(nth-child(n+4) 숨김) → 임박 영역 ~160px로 축소돼 캘린더가 첫 화면 진입. 데스크톱 그리드 현행 유지. 개발자 완료 2026-06-02 21:20 · QA 21:40 검증(데스크톱 실측, 모바일 ≤480px 소스검증).

### [2026-05-29] 캘린더 날짜 클릭 = '그날 이후' 출시 목록 — ✅ 완료
'패널 교체' 방식으로 구현 완료(release_date>=클릭일, 날짜별 그룹핑 리스트 재활용, 현재 필터 반영, openModal 재사용). 후속 '한 줄 컴팩트 행' 밀도 개선은 별도 TODO로 진행 중.

### [2026-05-29] 전체적으로 보기 편하게 (가독성/UX 4테마) — ✅ 완료
4테마 모두 완료: (1) 본문/메타 텍스트 대비·크기 상향, (2) 검색+필터 한 줄 묶기·퀵칩 필터행 통합, (3) 캘린더 셀 키우고 대표 게임명 1줄 노출, (4) 핵심 색 :root CSS 변수 1차 토큰화. 헤더 컴팩트화·진입 캘린더 고정 등 추가 폴리시는 디자이너 제안 기반 별도 TODO로 진행.
