# 👤 사용자 요청 사항

사용자가 직접 기획자 Claude한테 시키고 싶은 작업을 여기 적습니다.
기획자는 매 사이클마다 이 파일을 가장 먼저 읽고, 새 요청이 있으면 **최우선 TODO로 처리**합니다.

## 사용 방법

새 요청을 위에 추가하고 GitHub에 push만 하면 됩니다. (또는 GitHub 웹에서 직접 편집)

## 형식

```
## [YYYY-MM-DD] (제목)
설명: 구체적으로 무엇을 원하는지
우선순위: 높음 / 보통 / 낮음
상태: 대기 중 / 처리 중 / 완료 / 보류
```

기획자가 처리 시작하면 "처리 중", TODO로 들어가면 그 TODO ID를 적어둠.
개발자가 완료하면 "완료"로 바꿈.

---

## 활성 요청 (위가 최신)

### [2026-06-02] 모바일에서 "🔥 출시 임박" 스트립이 화면을 다 잡아먹음 (인벤 캘린더 참고)
**설명**: 운영자 피드백 "출시 임박 배너, 모바일에서 너무 불편함". 원인: 모바일(≤480px)에서 HeroStrip이 1열로 큰 카드(각 min-height 110px)가 세로로 쌓여, 임박 게임 최대 5건이면 첫 화면(~550px+)을 전부 차지하고 캘린더가 한참 아래로 밀림. 참고: https://www.inven.co.kr/webzine/calendar/ — 인벤은 임박을 '단일 카운트다운 하이라이트 1개 + 컴팩트 날짜 리스트'로 처리(큰 카드 세로 스택 없음).
**방향(인벤 기준)**: 모바일에서 큰 카드 스택 → 컴팩트 단일 행 리스트(카테고리 색점·게임명·D-day)로 축소 + 노출 건수 제한. 데스크톱은 현행 유지.
**우선순위**: 높음
**상태**: 처리 중 → TODO 1순위 (2026-06-02 17:50 기획자)

---

## 보류 중 (정식 출시 가까워질 때 처리)

### [2026-05-27] SEO 기본 설정
**설명**:
- 메타 태그 (`<title>`, `<meta description>`, `<meta keywords>`)
- Open Graph 태그 (카톡/페북 공유 미리보기)
- Twitter Card 태그
- `robots.txt` 추가
- `sitemap.xml` 추가
- favicon 설정
- 구조화 데이터 (JSON-LD `VideoGame` / `Event` 스키마)

**우선순위**: 보류 (정식 출시 1-2주 전에 처리)
**상태**: 보류
**메모**: 사용자가 정식 출시 시점에 다시 활성화 요청할 예정. 그전까지는 작업하지 마.

---

## 처리 완료된 요청 (아카이브)

### [2026-05-29] 캘린더 날짜 클릭 = '그날 이후' 출시 목록 — ✅ 완료
'패널 교체' 방식으로 구현 완료(release_date>=클릭일, 날짜별 그룹핑 리스트 재활용, 현재 필터 반영, openModal 재사용). 후속 '한 줄 컴팩트 행' 밀도 개선은 별도 TODO로 진행 중.

### [2026-05-29] 전체적으로 보기 편하게 (가독성/UX 4테마) — ✅ 완료
4테마 모두 완료: (1) 본문/메타 텍스트 대비·크기 상향, (2) 검색+필터 한 줄 묶기·퀵칩 필터행 통합, (3) 캘린더 셀 키우고 대표 게임명 1줄 노출, (4) 핵심 색 :root CSS 변수 1차 토큰화. 헤더 컴팩트화·진입 캘린더 고정 등 추가 폴리시는 디자이너 제안 기반 별도 TODO로 진행.
