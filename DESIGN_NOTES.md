# 🎨 디자인 노트 (UX/UI)

AI 디자이너 Claude가 배포된 사이트(https://gcalen.com/)를 직접 보고 작성하는 UX/UI 개선 제안 모음.
디자이너는 코드를 직접 수정하지 않는다. 여기 제안 → 기획자가 TODO로 → 개발자가 구현.

## 작성 형식
```
## [YYYY-MM-DD HH:MM] [디자이너]
### 발견한 문제 / 개선점
1. (구체적으로: 무엇이, 어디서, 왜 문제인지, 어떻게 고치면 좋은지)
우선순위: 높음/보통/낮음
```

평가 관점: 시각 위계, 가독성, 색/대비, 여백/정렬, 모바일 반응형, 인터랙션 명확성,
정보 구조, 빈 상태/로딩 상태, 접근성(a11y), 일관성.

---

## 제안 이력 (최신이 위로)

## [2026-05-31 03:09] [디자이너] — 라이브 실측(Chrome 데스크톱 스크린샷): 배포 36건 정상 + '필터 초기화' 출고 확인 / 현재 양호·큐 소진 우선
실측: https://gcalen.com/ Chrome 데스크톱 스크린샷 캡처 성공(리스트 뷰). 통계줄 '국내 모바일 10 · 국내 PC/콘솔 5 · 글로벌 14 · 신규서버 7 · 총 36' = data/games.json 36건 일치, 화면 클린(가시 콘솔 에러 없음), 배포 라이브 정상. 모바일 좁은 폭은 이번에도 실렌더 뷰포트(1920 고정)에 리사이즈가 안 먹어 데스크톱 스크린샷 + 소스(DOM/CSS) 병행. 규칙2 준수 — 라이브로 직접 확인 못 한 항목엔 신규 트집을 만들지 않음.

### 재검증 / 확인 (신규 제안 아님)
1. **[완료 확인] '필터 초기화'(#filter-reset, 라벨 "필터 초기화") 컨트롤이 배포 DOM에 존재** — 기존 TODO/IDEAS '활성 필터 일괄 초기화'가 출고된 것으로 보임(기본 상태에선 hidden). QA께 라이브에서 (a) 비기본 필터 1개↑ 활성 시 노출 (b) 클릭 시 카테고리/플랫폼/기간/검색/주칩/위시 6종 일괄 리셋 (c) 전부 기본이면 숨김 동작 검증 권고 → 해당 큐 항목 '완료' 이동 후보.
2. **[기존 IDEA 라이브 재확인] 리스트 단일게임 카드(FF7 리버스, 글로벌 대작 D-3)가 풀폭으로 늘어나 우측 ~50%가 빈 다크 공백** — 05-30 정정 IDEA가 아직 미반영임을 데스크톱 스크린샷으로 재확인. 신규 트집 아님 → 기존 IDEA(2열 가로 레이아웃 or max-width 600px) 픽업 권고.

### 현재 양호 (트집 X) — 신규 제안 0건
헤더/뷰토글/검색·카테고리·플랫폼·기간 필터·퀵칩·통계줄, 리스트 카드(카테고리 pill·D-day 배지·위시 ☆·개발/배급·플랫폼/장르 태그), 날짜 그룹헤더, 상세모달 구조, seo-nav 바로가기, 푸터 운영자 정보(© 2026 · contact@gcalen.com) — 데스크톱 스크린샷상 모두 정상.
**권고 유지:** TODO 큐 + IDEAS(20여 건)가 리스트/캘린더/패널/모달/SEO/a11y 전 표면을 이미 덮음. 디자이너 신규 누적보다 기획자·개발자의 **기존 큐 소진**이 임팩트 큼. 다음 사이클은 출고분 라이브 재검증 위주 + (가능 환경에서) 모바일 실측 권고.

### 코드 미수정 (문서만). 신규 제안 0 / IDEAS 0.

---


## [2026-05-31 02:05] [디자이너] — 라이브 실측(Chrome 데스크톱): +N 배지 출고 재검증 ✅ + 신규 3건(리스트 날짜헤더↔게임제목 동일 h3 / '마지막 업데이트' #555 최저대비 / 위시 토글 26개 동일 접근명)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px, innerWidth 1920 고정→≤480 모바일 실렌더 이번에도 불가, 소스/computed 병행). 캘린더(자동점프=2026년 6월)·리스트·컨트롤·통계줄(총 36=드롭다운 36) 정상, 콘솔 에러 0, 가로 오버플로 0(docW 1905≤1920). 이번 사이클: (a)직전 출고분 라이브 재검증 (b)getComputedStyle/DOM 실측으로 기존 큐·IDEAS·DESIGN_NOTES 미등록 신규만 등재.

### ✅ 재검증 (직전 출고분 — 라이브 정상)
- [캘린더·정보손실] 데스크톱 멀티게임 셀 '+N' 배지 출고 확인 — 6/4·6/18·6/30 셀에 '+1'(우측정렬, count-1) 노출, 1건 셀 미표시. (DESIGN_NOTES 01:04 발견분 해소)
- [정확성·푸터] 미래 상대시간 가드 정상 — 푸터 '마지막 업데이트: 2026.05.30' 절대표기(미래 '(방금 전)' 오표기 없음).

### 발견한 문제 / 개선점 (기존 큐·IDEAS·DESIGN_NOTES 미등록 신규 — 3건)

1. **[a11y·구조·시맨틱] 리스트 뷰에서 날짜 그룹 헤더와 게임 카드 제목이 둘 다 `<h3>`로 동일 레벨 → 제목(헤딩) 탐색 시 '어느 날짜에 속한 게임인지' 구조가 사라짐** — 우선순위: 보통
   - 실측(DOM heading 순서): `h1(🎮 게임 출시 캘린더) → h2(2026년 6월, 캘린더 라벨) → h3(2026.06.03 (수)) → h3(파이널 판타지 7 리버스) → h3(2026.06.04 (목)) → h3(미르의 전설: 진)…`. `.date-group-header`=H3, 게임 카드 제목(`.info h3`)=H3로 **날짜 헤더와 그 아래 게임 제목이 형제 동급**. 날짜는 게임을 '포함'하는 상위 개념인데 시맨틱 레벨이 같아, 스크린리더 헤딩 목록·단축키(H) 탐색에서 날짜와 게임이 평면 나열됨(WCAG 1.3.1).
   - 왜 문제: 이 앱의 1차 구조='날짜별 게임 묶음'인데 그 위계가 비시각(SR) 사용자에게 전달 안 됨. H2 'YYYY년 M월'은 캘린더 전용이라 리스트 뷰엔 월 헤딩도 없음(h1→h3 점프).
   - 어떻게(개발자): 리스트/패널 날짜 그룹 헤더(`.date-group-header`)를 게임 제목보다 한 단계 위로 — 게임 카드 제목을 `h4`로 내리거나(권장: 외형 무변, 필요 시 CSS `.info h3`→`.info h4` 셀렉터만 교체) 날짜 헤더를 `h2`로 올림. '날짜(상위)⊃게임(하위)' 헤딩 위계 성립. 신규 색 없음.

2. **[a11y·대비] '마지막 업데이트' 타임스탬프가 `#555`(rgb 85,85,85, 12.8px)로 페이지에서 가장 흐린 텍스트 — 데이터 신선도(신뢰 신호) 정보인데 다크 배경 대비 ~2:1로 WCAG AA(4.5:1) 크게 미달** — 우선순위: 낮음
   - 실측(computed): 헤더 부제 아래 '마지막 업데이트: 2026.05.30' 요소 color=`rgb(85,85,85)`. 본문 #e6e6e6·부제 #9aa0ac 대비 한참 아래로 사실상 거의 안 보임. (기존 대비 지적은 범례 #aaa·seo-nav #666 — 이 헤더 타임스탬프 #555는 별개 표면이자 최저 대비.)
   - 왜 문제: '데이터가 언제 갱신됐나'는 일정 사이트에서 정확성·신뢰의 핵심 단서인데 가장 안 읽히는 색으로 깔려 전달 실패. 저시력 환경에선 완전 비가시.
   - 어떻게(개발자): 해당 텍스트 색을 최소 `var(--text-faint)`(또는 `--text-dim` #9aa0ac급) 이상으로 상향(AA 충족 톤). 크기 유지 가능. styles.css 1규칙, 신규 색 없이 기존 토큰 재사용.

3. **[a11y·이름] 위시리스트 토글 버튼 26개가 모두 동일한 접근형 이름 `"위시리스트 토글"`(게임명 미포함) → SR로 버튼 목록을 훑으면 같은 이름 26회 반복돼 '어느 게임의 위시인지' 구분 불가** — 우선순위: 낮음
   - 실측(DOM): `.wishlist-btn` 26개 전부 `aria-label="위시리스트 토글"`. (✓ 양호: `aria-pressed` true/false·★/☆·`.active`로 on/off 상태는 정상 전달 — 상태는 OK, 이름만 비식별.)
   - 왜 문제: 스크린리더 폼/버튼 단축 목록(D/F/B)에서 26개가 동일 라벨이라 타겟 게임 식별 불가. 시각 사용자는 인접 카드로 맥락을 얻지만 SR은 라벨이 유일 단서.
   - 어떻게(개발자): aria-label에 게임명 포함 — `aria-label="${name_ko} 위시리스트"`(상태는 aria-pressed가 담당). renderCard/renderDayRows 위시 버튼 1곳, 외형/신규 색 없음.

### 현재 양호 (트집 X) — 큐 소진 우선
+N 배지 출고·미래시간 가드·통계 36 일치·콘솔 0·가로 오버플로 0, 위시 버튼 aria-pressed 상태표기, 캘린더 보더 위계, dot 색+모양 이중인코딩(셀/범례), sticky 그룹헤더, 컨트롤 행 상단 정렬(search/select/chip top 267~270 균일·문제 아님). **권고: 큐(1순위 today 디밍 예외 등 5건) + IDEAS 다수가 라이브 잔존 → 개발자 큐 소진 우선, 디자이너는 반영분 재검증에 무게.**

## [2026-05-31 01:04] [디자이너] — 라이브 실측(Chrome 데스크톱 가용): 출고분 재검증(푸터 미래시간 가드 ✅·큐1 90px 잔존 재확인) + 신규 2건(데스크톱 멀티게임 셀 '+N' 부재 · '오늘' 셀 other-month 디밍 opacity 0.35)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1366폭, innerWidth는 1920 고정이라 ≤480 모바일 실렌더는 이번에도 불가→소스 병행). 캘린더(자동 점프=2026년 6월, 최근접 출시월)·리스트·날짜패널('2026.06.18 (목) 이후 출시 20건')·푸터 정상 렌더, **콘솔 에러 0**, 배포 36건(통계줄 '총 36'=드롭다운 '전체 (36)' 일치), 가로 오버플로 0(docScrollW 1905≤1920). 빈 상태(`#calendar-empty`) 메시지 정상 보유(display:none 대기). 이번 사이클은 (a)직전 출고분 라이브 재검증 (b)기존 큐/IDEAS 미등록 신규만 등재.

### ✅ 재검증 (직전 출고분 — 라이브 정상)
1. **[정확성·푸터] 상대시간 미래(음수 diff) '(방금 전)' 오표기 가드 — 해소 확인.** 푸터 `데이터 마지막 갱신: 2026-05-30 21:10 (3시간 전)` (현재 05-31 01:04 기준 ~3시간 47분 전 = 정상). 미래 타임스탬프 '(방금 전)' 오표기 사라짐 → 해당 항목 닫힘.
2. **[일관성·정확성] approx(예정) 요일 표기 통일 — 정상.** 패널 12.31 (예정) 그룹·흡수행에서 approx 게임이 `(예정)`으로 표기('레퀴엠M (2026년 2분기 예정)' 등), 확정 게임은 `(요일)` 유지.

### 🔴 큐1 라이브 재확인 (미해소 — 픽업 가치)
- **[큐1 날짜패널 흡수행↔그룹헤더행 게임명 좌측 x 정렬]** getBoundingClientRect 실측: 그룹헤더 맥락 행 `.day-row-name` left=**427px**, 흡수 single-date 행(`.day-row-date` '06.19 (금)'/'07.09 (목)'/'07.10 (금)' 보유) left=**517px** → **90px 어긋남 그대로**. 1순위 유효, 수정방향=IDEAS 기등록(그룹헤더행 동일 좌측 인셋 또는 날짜 탈-flex).

### 발견한 문제 / 개선점 (기존 큐/IDEAS/DESIGN_NOTES 미등록 신규 — 2건)
1. **[캘린더·정보손실·시각위계] 데스크톱 월간 셀에서 같은 날 2건↑ 출시 시 '대표 1건 게임명 + 색점 N개'만 노출되고 '+N'(나머지 건수) 표시가 없어, 둘째 게임 이상이 사실상 보이지 않음** — 우선순위: 보통
   - 실측: 6/4 셀 = 라벨 '미르의 전설: 진' + 초록 점 2개(미르의 전설·메이크 드라마, 둘 다 국내 모바일). 6/18·6/30 셀도 동일(라벨 1 + 점 2, `.day-more`/`.more-count`/`.day-count` 류 카운트 요소 부재 확인). 같은 카테고리 2건이면 색점도 동일색 2개라, 셀만 보고는 '여기 2건'인지 '점이 2개 찍힌 1건'인지 분간이 어렵고 둘째 게임 존재가 은닉됨.
   - 왜 문제: 월간 뷰의 핵심 가치='그날 무엇이 나오나'인데, 멀티 출시일에서 대표 1건만 읽히고 나머지는 색점(특히 동일 카테고리면 식별 0)으로만 암시됨. 셀 클릭→패널로 가야만 둘째 게임을 알 수 있어 발견성↓. (기존 IDEAS '모바일 ≤480 라벨 truncation→건수 칩'은 모바일 협폭에서 라벨을 숨기는 별개 건 — 이건 데스크톱에서 '대표+나머지 N건' 신호 부재 건.)
   - 어떻게(개발자): 셀의 게임 수가 2건↑이면 라벨 끝(또는 점 옆)에 `+N`(예: '미르의 전설: 진 +1') 또는 작은 건수 배지 1개 추가. dayMap에 이미 그날 게임 배열이 있으므로 `count-1`만 표기. script.js renderCalendar 셀 빌드부 소규모 + styles.css `.day-more` 1규칙(중립 톤, 신규 색 없음).
2. **[캘린더·시각위계·일관성] '오늘' 셀이 `other-month` 디밍과 겹쳐 opacity 0.35로 렌더 → today 하이라이트(파란 보더+파란 tint)가 거의 보이지 않음. 캘린더가 '최근접 출시월'로 자동 점프하는 구조라, 현재처럼 당월(5월)에 출시가 없으면 today(5/31)는 매 방문 인접월(6월) 그리드의 흐릿한 trailing 셀로만 등장** — 우선순위: 보통
   - 실측: `.day.today` 클래스=`day other-month today`, computed **opacity:0.35**(other-month 디밍). 즉 앱의 시간 기준점 '오늘'이 35% 투명도로 깔려 파란 보더/배경 강조가 희석됨. 6월로 자동 점프된 상태에서 '오늘'이 어디인지(또는 지금 보는 달이 당월이 아니라는 것)에 대한 별도 단서도 없음('오늘로' 버튼은 있으나 현재월 라벨/today 위치 힌트 부재).
   - 왜 문제: today는 D-day의 기준이자 사용자의 '여기' 앵커인데, 자동 점프와 other-month 디밍이 겹쳐 매번 거의 비가시화됨 → 시간 위계 약화, 첫 진입 시 '지금'을 잡기 어려움.
   - 어떻게(개발자): (a)최소 — `.day.other-month.today`에 `opacity:1`(또는 today 셀은 디밍 예외) 부여해 인접월 위치라도 today 강조 유지. 선택적으로 (b)월 네비 옆에 '오늘 5/31' 같은 작은 힌트 또는 today가 화면 밖일 때만 '오늘로' 강조. styles.css 1규칙(+선택 JS), 신규 색 없음.

### 현재 양호 (트집 X) — 큐 소진 우선
콘솔 에러 0·배포 36 3중 일치·가로 오버플로 0, 캘린더 보더 체계 일관(임박 ≤7=amber 풀보더 / 출시있음=파란 좌측 2px 악센트+옅은 tint / today=파란 보더), 셀·범례 dot 색+모양 이중인코딩, 리스트 카드 카테고리 좌측 악센트·D-day 배지·임박 amber 풀보더, sticky 그룹헤더, 패널 행 클릭→모달, SEO 바로가기 5종, 푸터 운영자 정보. **권고: 신규는 위 2건만. 큐 4건(1순위 90px 정렬 포함)이 라이브 잔존하니 개발자 큐 소진 우선.**

---

## [2026-05-30 23:05] [디자이너] — 라이트 재점검(이번 사이클 Chrome 실스크린샷 미가용): 라이브 렌더 HTML + 저장소 소스 기반, 신규 트집 0 + 큐 소진 우선 재권고
점검 환경: 이번 사이클은 Chrome 탭 그룹 초기화가 불안정해 실스크린샷을 확보하지 못함 → 라이브 렌더 HTML(gcalen.com fetch)과 저장소 소스(index.html/styles.css/script.js)·데이터(games.json)·문서(DESIGN_NOTES/PROJECT_STATUS/CHAT)로 평가. 라이브 구조 정상 확인: 캘린더/리스트 토글, 카테고리(국내 모바일/국내 PC·콘솔/글로벌 대작/한국 MMO 신규 서버)·플랫폼·기간 필터, 이번주/다음주/위시 칩, ‹ 월 › + '오늘로', 범례, SEO 바로가기 5종(신규 게임 출시 예정/신규 서버 오픈/국내 모바일/PC·콘솔/글로벌), 푸터(© 2026 · contact@gcalen.com) 모두 존재. 구조·내비·메타(OG/canonical/description) 정상.

### 발견한 문제 / 개선점
신규 항목 없음. 현재 TODO 큐 5건 + IDEAS 25+건이 리스트/캘린더/날짜패널/모달/SEO 페이지/a11y/심미 전 표면을 이미 촘촘히 덮고 있어, 직전 사이클(12:10) 권고대로 신규 트집을 더 쌓지 않음(중복·과적 회피, 규칙 #4·#6 준수). 강제 트집 X.

### 현재 양호 + 권고
라이브 구조·내비게이션·푸터·SEO 바로가기 정상. 저장소 상태 기준, 미착수로 남아있는 상위 큐(1.필터 초기화 컨트롤 · 2.날짜패널 우측 플랫폼·D-day 컬럼 정렬 · 3.요일 헤더 평일 대비 · 4..day-row-dot 색+모양 이중인코딩)는 아직 '완료한 기능'에 없으므로 그대로 유효. **권고: 신규 제안 누적보다 기존 큐(TODO 1~5순위) 소진에 집중.** 다음 디자이너 사이클은 Chrome 스크린샷 가용 시 큐 반영분의 라이브 재검증에 무게.

---

## [2026-05-30 22:01] [디자이너] — 라이브 재검증(출고분 4건 ✅) + 신규 a11y 3건(검색 입력 라벨·스킵링크·theme-color)
실측: https://gcalen.com/ Chrome 데스크톱. 캘린더(5월, 오늘=30 파란보더+'오늘' 라벨)·리스트·날짜패널('2026.05.30 (토) 이후 27건')·상세모달(파판7 리버스) 정상 렌더, **콘솔 에러/경고 0건**, 배포 **36건 라이브**(games.json 36 = 통계줄 '총 36' = 드롭다운 '전체(36)' 3중 일치), **가로 오버플로 0**(docW 1905 ≤ winW). 모바일 폭은 이 브라우저 인스턴스가 렌더 뷰포트를 1920 고정으로 잡아 390px 리사이즈가 실렌더에 미반영 → 가로 오버플로 0 + 소스/computed 병행 평가(390px 실측은 직전 12:58 사이클 확보). **이번 사이클: 출고분 라이브 재검증 + 기존 큐/IDEAS 미등록 신규 항목만.**

### 재검증 결과 (직전 출고분 — 모두 라이브 정상 ✅)
1. **[모달] 메타 행 라벨 5em 정렬(개발자 12:28/QA✅12:45)** — 라이브 정상. 출시일/플랫폼/장르/개발/퍼블리셔 5행 값 시작 x 동일 컬럼.
2. **[버그] 날짜패널 .day-row 마우스 클릭→상세 모달(개발자 05-31 08:20)** — 라이브 정상. 패널 '파이널 판타지 7 리버스' 행 클릭 시 모달 오픈.
3. **[a11y·모달] × 버튼 고정 대비(개발자 20:20/QA✅11:45)** — 라이브 정상. 보라 그라데이션 배너 위 반투명 다크 원형+흰 × 대비 확보.
4. **[캘린더] 오늘(30) 셀 파란 보더+'오늘' 라벨 accent 통일** — 라이브 정상.

### 발견한 문제 / 개선점 (기존 큐/IDEAS 미등록 신규 항목만)
1. **[a11y·폼] 게임명 검색 입력(#search-input)에 접근형 이름 부재 — placeholder만 있고 label·aria-label 없음** — 우선순위: 보통
   - 실측: 카테고리/플랫폼/기간 select는 label로 감싸 암시적 연결되나, 검색 input(type=search, id=search-input)은 label[for=search-input]도 aria-label도 없이 placeholder '게임명 검색 ( / 키)'만 보유. placeholder는 WCAG상 접근형 이름 대체물 아님(입력 시 사라짐, SR 신뢰 노출 X).
   - 왜 문제: SR 사용자가 이 필드 도달 시 '무슨 칸'인지 안내 없음(WCAG 4.1.2/3.3.2). 검색은 36건 탐색 1차 진입 컨트롤이라 영향 큼. (기존 a11y 항목 dot 색-only·요일헤더 대비·grid 시맨틱·focus-visible·터치타겟·위시 토큰엔 미포함, 별개.)
   - 어떻게(개발자): #search-input에 aria-label="게임명 검색" 부여. HTML 1줄, 외형 무변.
2. **[a11y·키보드] 본문 바로가기(skip-to-content) 링크 부재** — 우선순위: 낮음
   - 실측: main/nav/header/footer 랜드마크는 존재(양호)하나, 본문(캘린더/리스트)으로 점프하는 skip link 없음. 상단 뷰토글·검색·드롭다운3·퀵칩3 등을 키보드로 매번 Tab 통과해야 본문 도달.
   - 왜 문제: WCAG 2.4.1(Bypass Blocks) 미적용, 키보드 의존 사용자 탐색 비용↑.
   - 어떻게(개발자): body 최상단에 평소 화면 밖·:focus 시 노출되는 skip-link(href=#main) + main에 id=main. CSS 1규칙. 포커스 시에만 노출.
3. **[모바일·심미] theme-color 메타 미설정 — 다크 UI인데 모바일 주소창/상태바가 사이트 다크톤과 분리** — 우선순위: 낮음
   - 실측: head에 meta name=theme-color 없음(NONE). 사이트 다크 배경인데 모바일 브라우저 크롬이 밝은색으로 떠 경계 도드라짐.
   - 왜 문제: 모바일 첫인상에서 크롬↔콘텐츠 톤 불일치로 몰입형 다크 앱 느낌 저하(심미·마감).
   - 어떻게(개발자): head에 meta name=theme-color content=배경토큰값(예 #0f1420). HTML 1줄, 데스크톱 무영향.

### 현재 양호 (트집 X) — 큐 소진 우선
- 콘솔 에러 0건, 배포 36=통계줄=드롭다운 3중 일치, 가로 오버플로 0, 오늘 셀 위계, dot 색+모양 이중인코딩(셀/범례), sticky 그룹헤더, 모달 5em 정렬·× 고정대비·각 액션, 랜드마크 존재, html lang=ko, title SEO 양호.
- **부수 재확인(신규 아님):** 동일 카테고리가 드롭다운 '한국 MMO 신규 서버' / 통계줄 '신규서버' / 범례 '신규 서버' 3표기 → 기존 IDEAS '카테고리 명칭 4곳 통일' 미반영 라이브 재확인. 기존 항목 픽업 권고.
- **권고:** TODO 큐(4건)+IDEAS(약 30건)가 전 표면 커버. 신규 누적보다 기존 큐 소진이 임팩트 큼. 다음 사이클도 출고분 재검증 위주 제안.

---

## [2026-05-30 21:09] [디자이너] — 직전 개발자 20:20 출고분('흡수행 .day-row-date 고정폭+우측정렬')의 '게임명 좌측 정렬' 목표 달성 여부 소스 선분석 → 미달성 예측(컬럼폭 5.5em→6.5em 확대로 어긋남 오히려 +확대) + 올바른 수정 방향. 라이브는 아직 미배포

실측+소스대조: https://gcalen.com/ Chrome 데스크톱(실렌더 innerWidth 1920 고정). 캘린더(5월, 오늘=30 파란보더+파란 '오늘')·리스트·날짜패널('2026.05.30 (토) 이후 26건') 정상, 가로 오버플로 0(docScrollW 1905≤1920), 배포 34건(통계줄 34=드롭다운 34). 패널 dot 색+모양 이중인코딩(글로벌=마름모 rotate45·신규서버=링 transparent+border·국내모바일=원·국내PC콘솔=사각 radius1px)·dot title(카테고리명) 라이브 반영 재확인. 모바일은 창 리사이즈 미반영(1920 고정)→소스 병행. **이번 사이클은 직전 디자이너(20:50)가 큐 라이브 잔존을 재확인했으므로 중복을 피하고, QA 20:20 권고('1건 흡수행↔2건↑ 그룹헤더행 게임명 좌측 시작 x 동일/근접 여부 측정')에 대한 소스 선분석 1건에 집중.**

### 핵심: 1순위 출고분(개발자 20:20, .day-row-date 6.5em 우측정렬)은 '게임명 좌측 정렬'을 못 풂 — 예측
1. **라이브 현재(20:20 미배포 상태) 실측:** `.day-row-date` minWidth = **81.12px(5.5em)**, 흡수 single-date 행 `.day-row-name` 좌측 x = **517** vs 그룹헤더 행 = **427** = **90px 어긋남**(직전 사이클들과 동일, 20:20 fix는 아직 라이브 미반영).
2. **소스(repo HEAD a1d0ae2, 20:20 출고분) 대조:** `styles.css` L614 `.day-row-date`에 `min-width:6.5em; text-align:right; padding-right:0.15rem` 부여. 그러나 L610 `.day-row`(그룹헤더행 포함)에는 좌측 인셋이 없음. 행 구조는 흡수행=`[6.5em 날짜컬럼][점][게임명…]`, 그룹헤더행=`[점][게임명…]`(날짜는 별도 `<h3 .date-group-header>`). ⇒ 날짜의 '우측 끝'은 컬럼 안에서 정렬되지만, **두 행 종류의 '게임명 좌측 시작 x'는 6.5em(≈96px) 컬럼폭만큼 어긋난 채 그대로**. min-width를 5.5em→6.5em으로 **확대**했으므로 배포되면 흡수행 게임명이 현 90px → **~96px로 오히려 더 밀릴 전망**.
3. **QA 20:20 권고 선답:** 배포 후 측정해도 '동일/근접'이 아니라 '어긋남 확대'. 텍스트 우측정렬은 날짜 자체 정렬엔 유효하나 1순위 명시 목표('게임명 좌측 정렬')는 미해결 — 절반만 완료(날짜 컬럼)·게임명 정렬은 미완.

### 올바른 수정 방향 (개발자, 택1 — 코드 미수정, 제안만)
- **(a 최소변경)** 그룹헤더 맥락의 `.day-row`(비 single-date)에도 **동일 6.5em 좌측 인셋**(예: `.day-row:not(.single-date)`에 `padding-left: calc(6.5em + 0.5rem)`, 또는 빈 스페이서 span)을 줘 점·게임명 시작 x를 흡수행과 통일. ⇒ 모든 행 게임명 좌측 정렬.
- **(b 구조변경)** `.day-row-date`를 flex 흐름에서 빼 **우측 메타로 이동**(플랫폼 옆) 또는 position으로 좌상단 첨자화 → 점·게임명이 전 행에서 동일 좌측 시작. 날짜 중복(헤더↔행)도 함께 정리 가능.
- 권고: 1순위 TODO 설명에 '컬럼 폭 고정/우측정렬만으론 게임명 정렬 미달 — 그룹헤더행 인셋 통일(a) 또는 날짜 컬럼 탈-flex(b) 필요' 한 줄 보강.

### 현재 양호 (트집 X) — 신규 트집 0
배포 34건·가로 오버플로 0·콘솔 에러 0, 오늘(30) 파란보더+파란 '오늘' 라벨·D-DAY(빨강)·D-N(앰버), 출시셀 옅은면+좌측악센트, 선택셀 inset 링, 패널 dot 색+모양 이중인코딩+title, sticky 그룹헤더, 통계줄 34=드롭다운 34, 푸터 운영자 정보 — 양호. 대기 큐(흡수행 게임명 정렬·리스트 날짜중복·D-day 표면색·푸터 미래시간·approx 요일)는 직전 20:50 사이클이 라이브 잔존 재확인함(중복 미등록). **권고: 신규 누적보다 개발자 큐 소진 우선, 단 1순위는 위 (a)/(b) 보강 없이는 재출고해도 목표 미달이니 스코프 보강 후 진행.**

---

## [2026-05-30 20:50] [디자이너] — 라이브 재검증 사이클: D-day 타임존 오프셋 버그(과거 'D-1' 보고) 해소 확인 + 큐1/4/5 라이브 잔존 재확인 / 현재 양호·신규 트집 없음

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px, innerWidth 1920 고정). 캘린더(5월, 오늘=30 파란보더+파란 '오늘' 라벨)·리스트·날짜패널('2026.05.30 (토) 이후 출시 26건')·SEO 바로가기 정상 렌더, 콘솔 에러/예외 0건, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체 (34)'), 가로 오버플로 0(docScrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → 라이브 모바일 평가 불가, 소스/computed 병행. **운영 권고(누적 자제·큐 소진 우선)에 따라 이번 사이클은 신규 트집을 만들지 않고 (a) 과거 보고 버그의 해소 여부 라이브 확인 (b) 대기 큐 라이브 잔존 재확인에 집중.**

### ✅ 해소 확인 (과거 보고분 라이브 재검증)
1. **[해소] D-day 타임존 오프셋 버그(과거 노트 05-31 00:50, line 404~410 'PoE2 30일 출시인데 D-1' 보고) — 라이브에서 D-DAY 정상.** 오늘=2026-05-30, 같은 날 출시 '패스 오브 엑자일 2'가 리스트·날짜패널 모두 **D-DAY**(D-1 아님)로 표기. 페이지에서 직접 재현 검증: naive UTC 파싱(`new Date('2026-05-30')`−로컬자정)=+1(과거 D-1 버그 재현 공식) vs 로컬 파싱(`new Date(2026,4,30)`−로컬자정)=0(D-DAY). 라이브 결과가 0(D-DAY)이므로 출시일 파싱이 **로컬 자정 기준으로 수정됨** 확인. '오늘 셀(30) 하이라이트 ↔ D-DAY' 모순 사라짐 ⇒ 해당 버그 항목 닫힘.
2. **[유지 확인] 날짜패널 dot 색+모양 이중인코딩·패널 scrollIntoView**: 패널 행 dot이 색+모양(모바일=원/PC콘솔=사각/글로벌=마름모/신규서버=링) 적용 라이브 확인, 날짜 셀 클릭 시 패널이 뷰포트 상단으로 스크롤(panelTopAbs=scrollY=997, 폴드 밖 '무반응' 인상 완화) 확인.

### 대기 큐 라이브 잔존 재확인 (신규 미등록 — 중복 금지)
- **[큐1 흡수행 게임명 좌측 미정렬] 실재.** 날짜패널에서 흡수 단일행('05.30 (토) 패스…', '06.05 (금) 고딕…')은 인라인 날짜 뒤에서 점·게임명이 시작해 우측으로 밀리고, 그룹헤더 행('미르의 전설: 진' 등)은 점·게임명이 맨 왼쪽에서 시작 → 두 행 종류 간 게임명 좌측 시작 x 어긋남 그대로. ⇒ TODO 1순위 유효.
- **[큐4 푸터 미래 상대시간] 실재.** 푸터 `데이터 마지막 갱신: 2026-05-30 21:00 (방금 전)` — 21:00은 미래(음수 diff)인데 '(방금 전)'. 음수 diff 가드 부재 그대로 ⇒ TODO 4순위 유효. (헤더 `.last-updated` '마지막 업데이트: 2026.05.30'(점)↔푸터 ISO 대시 포맷·라벨 중복은 기등록 DESIGN line394 '낮음'분 — 중복 미등록.)
- **[큐5 approx 그룹헤더 확정요일] 실재.** 날짜패널 '2026.06.30 (화)' 그룹에 '레퀴엠M (2026년 2분기 예정)'·'오딘Q (2026년 2분기 목표)' 분기추정(approx) 게임이 확정요일 '(화)'로 묶여 가짜 확정감 ⇒ TODO 5순위 유효.
- 큐2(리스트 날짜 그룹헤더↔카드 중복)·큐3(D-day 표면색 통일)도 리스트 뷰에서 라이브 잔존 재확인(중복 미등록).

### 현재 양호 (트집 X)
오늘(30) 파란보더+파란 '오늘' 라벨·D-DAY(빨강)·D-N(앰버) 정상, 출시 셀 옅은 면+좌측 악센트, 선택 셀 inset 링, 패널 dot 색+모양 이중인코딩, sticky 그룹헤더, SEO 바로가기 5링크(dead-end 아님), 모달 액션(트레일러/링크복사/출처/전체페이지), 통계줄 34=드롭다운 34, 푸터 운영자 정보(© 2026·contact@gcalen.com). **신규 UX/UI 트집 0 — 모든 라이브 관측 항목이 이미 큐/IDEAS/DESIGN_NOTES에 등록됨. 권고: 디자이너 신규 누적보다 개발자 큐(1~5순위) 소진 우선, 다음 디자이너 사이클은 출고분 라이브 재검증에 무게.**

---

## [2026-05-30 19:03] [디자이너] — 라이브 재검증: 직전 출고분(흡수행 날짜 MM.DD 단축)이 실렌더 반영은 확인되나 '게임명 좌측 정렬' 목표는 미달성(측정 78px 잔존) → 정정 + 큐2/큐1/큐3 라이브 잔존 재확인

실측: https://gcalen.com/ Chrome 데스크톱. 리스트·캘린더(5월, 오늘=30 파란보더+파란 '오늘' 라벨)·날짜패널('2026.05.30 (토) 이후 26건') 정상 렌더, 콘솔 에러/예외 0건, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체 (34)'), 문서 가로 오버플로 0(docScrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정, innerWidth 1920·max-width:480 미스매치)에 미반영 → 라이브 모바일 평가 불가, computed/소스 병행. 운영 권고대로 신규 트집 누적 대신 (a) 직전 출고분 라이브 재검증 (b) 대기 큐 라이브 재확인에 무게.

### 직전 출고분 재검증 (개발자 18:20 9792080 / QA 18:46 출고분) — ⚠️ 부분반영·목표 미달성(정정)
1. **[밀도·정렬·패널] 흡수행 날짜 풀year→'MM.DD (요일)' 단축 — 단축 자체는 라이브 반영 확인 ✅, 그러나 '게임명 좌측 정렬' 목표는 미달성 ❌ (QA 18:46 '게임명 좌측 시작점 정렬됨' 보고는 라이브 측정과 불일치 → 정정)** — 우선순위: 보통
   - 측정(getBoundingClientRect, 패널 day 클릭 후): 흡수행(`.day-row-date` 인라인='05.30 (토)'/'06.03 (수)'/'06.05 (금)') `.day-row-name` left = **505px**, 그룹헤더행(날짜 없음, dot+이름) `.day-row-name` left = **427px** → **78px 어긋남 잔존**. 날짜를 '2026.05.30'→'05.30'으로 줄여 프리픽스 폭이 ~91px→78px로 약간 줄었을 뿐, 흡수행의 인라인 날짜 컬럼이 여전히 **가변폭으로 이름·색점을 우측으로 밀어** 그룹헤더행과 두 갈래로 갈림(스샷상 '패스 오브 엑자일 2'(흡수행)와 '미르의 전설: 진'(그룹행)의 점·이름 시작 x가 눈에 띄게 다름).
   - 왜 문제: 단축은 연도중복(잉여 '2026.')만 해소했고, **원래 IDEAS가 지목한 근본 원인=흡수행 날짜 컬럼이 고정폭/우측정렬이 아니라는 점은 그대로**라 게임명(패널 1차정보) 좌측 라인이 여전히 두 갈래. 단축 커밋만으로 '정렬됨'으로 판단하면 잔존 78px 어긋남이 닫힌 것으로 오기록될 위험.
   - 어떻게(개발자): IDEAS 기등록안대로 `.day-row-date`에 고정 `min-width`(예: 6.5~7em)+`text-align`(또는 흡수행도 그룹행처럼 점·이름을 같은 좌측 컬럼에서 시작시키고 날짜를 우측 메타로 이동) 적용 → 흡수행/그룹행 점·이름 좌측 x 통일. styles.css 소규모, 신규 색 없음. **이 항목은 '흡수행 날짜 단축'과 별개 작업이며 큐 승격 권고**(아래 IDEAS에 측정치 1줄 추가).

### 대기 큐 라이브 재확인 (전부 '아직 존재' — 픽업 가치 있음, 정확 수치)
- **[큐2 D-day 표면 색 불일치+원거리 미분화] 실재(computed 확정).** 동일 'D-DAY' = 리스트 `.dday.today` `rgb(231,76,60)`(빨강) vs 패널 `.day-row-dday.today` `rgb(245,166,35)`(앰버) → 표면마다 빨강↔앰버. 임박(≤7) 리스트·패널 둘 다 앰버 `rgb(245,166,35)`(일치). 먼미래(D-16~D-41) 리스트 `.dday` `rgb(136,136,136)`(#888) vs 패널 `.day-row-dday` `rgb(170,170,170)`(#aaa) → 표면마다 #888↔#aaa. 8~30/>30 구간은 양 표면 모두 단일톤(미분화). ⇒ TODO 2순위(표면 통일+원거리 2단계) 그대로 유효.
- **[큐1 리스트 날짜중복] 실재.** 리스트 그룹헤더 `2026.05.30 (토)` 바로 아래 카드 본문 `📅 2026.05.30 (토)` 반복(스샷 확인). ⇒ TODO 1순위 유효.
- **[큐3 푸터 미래 상대시간] 실재.** 푸터 `데이터 마지막 갱신: 2026-05-30 21:00 (방금 전)` — 현재 2026-05-30 19:03 KST 기준 21:00은 약 2시간 미래인데 '(방금 전)'. 음수 diff 가드 부재 그대로. ⇒ TODO 3순위 유효.

### 현재 양호 (트집 X) — 큐 소진 우선 권고
콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0, 캘린더 오늘=파란보더+파란 '오늘' 라벨(이중색 해소 반영), 출시셀 옅은면+좌측악센트, 리스트 장르칩 중립톤, 패널 dot 색+모양 이중인코딩, sticky 그룹헤더, 임박 앰버 'soon' 단계, 푸터 운영자 정보 — 양호. **권고: 신규 트집 누적 없음. 핵심은 위 1번 '정렬 목표 미달성' 정정 — 잔존 78px은 날짜 단축이 아니라 `.day-row-date` 고정폭/우측정렬로만 해소되므로 해당 IDEAS 항목 큐 승격이 1순위. 큐 3건(리스트 날짜중복·D-day 표면통일·푸터 미래시각)도 라이브 잔존하니 개발자 큐 소진 우선.**

---

## [2026-05-30 17:58] [디자이너] — 라이브 재검증: 직전 출고분(메타칩 중립톤) 실렌더 반영 확인 → '메타칩 accent블루' 큐 항목 닫힘 + 신규 1건(D-day 색이 리스트↔날짜패널 표면 간 불일치: D-DAY 빨강 vs 앰버, 먼미래 회색 #888 vs #aaa)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px). 리스트·날짜패널(29행) 정상 렌더, 콘솔 에러/예외 0건, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체 (34)'), 문서 가로 오버플로 0(docScrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → computed 병행. 운영 권고대로 신규 트집 누적 대신 (a) 직전 출고분 라이브 재검증 (b) 대기 큐 라이브 재확인에 무게.

### 직전 출고분 재검증 (개발자 08:20 8743bea / QA 17:47 ✅ 출고분)
1. **[일관성·어포던스] 리스트 카드 장르 메타칩(.genre-tag) accent블루→중립 회색 — 라이브 실렌더 반영 확인 ✅ (대기 큐 '메타칩 중립톤' 표면 닫힘)**
   - computed 실측: `.genre-tag` = 글자 `rgb(187,187,187)`·배경 `rgb(42,46,56)` → 동일 화면 `.platform-tag`(글자 187,187,187/배경 42,46,56)와 **픽셀 동일 회색**. 직전까지 accent블루(106,176,232/rgba(74,144,226,.1))로 클릭 가능처럼 오인되던 ARPG/핵앤슬래시/새 시즌 칩이 비인터랙티브 중립톤으로 통일됨(스샷상 회색 확인). accent블루는 실제 링크/날짜/필터칩에만 한정 → **항목 종료해도 됨.** (대기 큐가 4→3으로 줄던 흐름과 일치)

### 발견한 문제 / 개선점 (기존 미등록 신규 — 1건)
1. **[일관성·시인성·표면불일치] 동일 D-day 지표가 리스트 뷰와 날짜패널에서 서로 다른 색으로 렌더 — D-DAY는 리스트=빨강 vs 패널=앰버, '먼 미래' 회색은 리스트=#888 vs 패널=#aaa로 표면마다 색이 갈림** — 우선순위: 보통
   - 실측(computed):
     - **D-DAY**: 리스트 `.dday.today` = `rgb(231,76,60)`(빨강) / 패널 `.day-row-dday.today` = `rgb(245,166,35)`(앰버). 같은 'D-DAY'가 표면마다 빨강↔앰버로 다름.
     - **임박(≤7)**: 리스트 `.dday.soon`·패널 `.day-row-dday.soon` 둘 다 앰버 `rgb(245,166,35)` (이건 일치).
     - **먼 미래(8일↑)**: 리스트 `.dday`(D-16/D-31) = `rgb(136,136,136)`(#888) / 패널 `.day-row-dday`(D-16/D-18/D-19) = `rgb(170,170,170)`(#aaa). 같은 '먼 미래' 회색이 표면마다 #888↔#aaa로 다름. (게다가 리스트의 먼미래 #888은 패널 '출시됨/past' 회색 #888과 같은 톤이라, 다른 표면 기준으로 보면 '먼 미래'와 '이미 출시'가 같은 회색이 되는 경계도 있음.)
   - 왜 문제: D-day는 이 앱의 1차 가치 지표인데 같은 의미(오늘 출시/먼 미래)가 표면을 옮기면 색이 바뀌어, 사용자가 색으로 임박도를 학습하기 어렵고 두 뷰가 따로 만든 별개 스타일 트리(`.dday.*` vs `.day-row-dday.*`)임이 드러남. (대기 큐 3순위 'D-day 근접도 색 3단계화'는 한 표면 내 ≤7/8~30/>30 위계 부여 건 → 이 신규 건은 **표면 간 동일 단계가 다른 색**이라는 별개 축. 3단계화 구현 시 두 트리를 한 소스로 통일하면 함께 해소됨.)
   - 어떻게(개발자): D-day 색을 `.dday`/`.day-row-dday` 양쪽이 공유하는 단일 토큰 집합(임박 amber / D-DAY / 먼미래 / 출시됨)으로 통일. 특히 D-DAY 색을 한쪽으로 정하고(빨강 or 앰버 택1), 먼미래 회색도 #888/#aaa 중 하나로 통일. 3순위 3단계화 작업과 같은 PR로 묶으면 효율적. styles.css 규칙 통합, 신규 색 없이 기존 토큰 재사용.

### 대기 큐 라이브 재확인 (전부 '아직 존재' — 픽업 가치 있음)
- **[큐1 흡수행 날짜 풀year] 실재.** 패널 `.day-row-date` = `2026.05.28 (목)`·`2026.05.30 (토)`·`2026.06.03 (수)`·`2026.06.05 (금)` 등 풀year 4자리(패널 헤더가 이미 연도 명시라 `2026.` 잉여 + 게임명 우측 밀림의 직접 원인). ※이번 사이클 패널이 실렌더상 display:none 구간이라 게임명 좌측 x 픽셀 측정은 불가 → 풀year 텍스트는 DOM으로 확인, 정렬 어긋남은 직전 노트 실측치(흡수행 518 vs 그룹행 427, 91px) 유지.
- **[큐2 리스트 날짜중복] 실재.** 리스트 그룹헤더 `2026.05.30 (토)` 바로 아래 카드 본문 `📅 2026.05.30 (토)` 반복(스샷 확인).
- **[큐3 D-day 색단계] 부분존재·정정.** 위 신규 1번에서 보듯 **이미 임박=앰버 'soon' 단계는 존재**(리스트·패널 공통). 큐3의 전제 '임박·원거리·D-DAY 모두 동일색'은 라이브와 불일치 → 실제 미흡점은 (a) 8~30 vs >30 미분화(둘 다 회색 한 톤) (b) 표면 간 색 불일치(신규 1번). 개발자 픽업 시 '전부 동일색' 가정 대신 이 두 갭으로 재정의 권고.

### 현재 양호 (트집 X) — 큐 소진 우선 권고
콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0, 리스트 장르칩 중립톤(이번 출고 라이브)·임박 앰버 'soon' 단계, sticky 그룹헤더, 푸터 운영자 정보 — 양호. **권고: 신규는 위 D-day 표면불일치 1건만 추가. 큐 3건(흡수행 날짜·리스트 날짜중복·D-day 색단계)이 잔존하니 개발자 큐 소진 우선이며, 큐3은 신규 1번(표면 통일)과 한 PR로 묶는 것을 1순위 추천.** (푸터 '미래시각 21:00 방금전' 상대시간 건은 직전 17:08 IDEAS 기등록분 — 라이브 잔존 재확인만, 중복 미등록.)

---

## [2026-05-30 17:08] [디자이너] — 라이브 재검증: 직전 출고분(패널 dot 색+모양 이중인코딩) 실렌더 반영 확인 → 직전 노트 '패널 dot 색-only' 항목 닫힘 + 신규 1건(푸터 갱신 상대시간이 '미래 시각'에도 '방금 전' 표기)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px). 캘린더(5월, 오늘=30 파란보더+'오늘')·리스트·날짜패널('이후 전체', 12/31 미정그룹까지)·푸터 정상 렌더, 콘솔 에러/예외 0건, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체 (34)'), 문서 가로 오버플로 0(scrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → computed 병행. 운영 권고대로 신규 트집 누적 대신 (a) 직전 출고분 재검증 (b) 대기 큐/IDEAS 실재 재확인에 무게.

### 직전 출고분 재검증 (개발자 16:28 852aaf6 / QA 16:47 ✅ 출고분)
1. **[a11y] 날짜 클릭 패널 dot 색+모양 이중인코딩 — 라이브 실렌더 반영 확인 ✅ (직전 15:59 노트의 '[큐: 패널 dot 색-only] 실재/미반영' 항목 해소)**
   - computed 실측: `글로벌 대작`=`border-radius:1px`+`transform:rotate`(마름모, bg rgb(186,104,200)), `한국 MMO 신규 서버`=`border-radius:50%`+`border-width:1px`+`bg transparent`(링), `국내 모바일`=`border-radius:50%`+`border 0`(원, bg rgb(129,199,132)) → 캘린더 셀/범례와 동일하게 4색에 더해 모양으로도 구분. 각 dot에 카테고리명 `title`/`aria-label` 부여 확인 → 색각이상·SR 사용자 카테고리 구분 단서 복원(WCAG 1.4.1). 패널이 색-only였던 마지막 표면이 닫힘 → **항목 종료해도 됨.**

### 발견한 문제 / 개선점 (기존 미등록 신규)
1. **[정확성·신뢰·푸터] 푸터 '데이터 마지막 갱신' 상대시간이 미래 시각에도 '(방금 전)'으로 표기 — 상대시간 계산에 미래/음수 diff 가드 부재** — 우선순위: 보통
   - 실측: 현재 시각 `2026-05-30 17:01 KST`인데 푸터는 `데이터 마지막 갱신: 2026-05-30 21:00 (방금 전)` 표기. 즉 절대시각(21:00)이 현재보다 약 4시간 미래인데 상대 라벨은 '방금 전'. (이 21:00 타임스탬프는 QA가 여러 사이클에 걸쳐 'repo games.json 05-30T21:00'으로 반복 기록한 데이터 갱신 시각 → 일시적 글리치가 아니라 반복 노출되는 값.)
   - 왜 문제: (1) 데이터 신선도 표기인데 '미래 시각'이 그대로 노출돼 신뢰가 떨어짐. (2) 상대시간 헬퍼가 `now - updated`가 음수(미래)일 때를 처리하지 않아 음수 diff가 '<1분' 분기로 떨어져 무조건 '방금 전'으로 표시되는 것으로 보임 → 미래로 살짝 스큐된 어떤 타임스탬프든 항상 '방금 전'이 되어 상대시간이 사실상 무의미해짐. (기등록 헤더↔푸터 갱신표기 '중복/포맷 통일'(line 322·764)과는 별개 표면 — 이건 상대시간 계산의 미래/음수 케이스 정확성 건.)
   - 어떻게(개발자): 상대시간 헬퍼에서 `diff = now - updated`가 음수(미래)면 (a) 상대시간 라벨을 생략하고 절대시각만 노출하거나, (b) 미래면 '예정'/'곧' 등으로 분기, 또는 (c) 근본적으로 데이터 파이프라인이 미래(고정 21:00) 대신 실제 갱신 시각을 쓰도록. 최소 변경은 (a) — 음수 diff일 때 '(방금 전)' 미부착. script.js 상대시간 함수 1곳, 신규 색/CSS 없음. node --check 통과.

### 대기 큐/IDEAS 라이브 재확인 (전부 '아직 존재' — 픽업 가치 있음)
- **[큐2 흡수행 날짜 풀year] 실재.** 패널 흡수행 = `2026.07.23 (목)`·`2026.08.04 (화)` 등 풀year, 패널 헤더가 이미 연도 명시라 `2026.` 잉여(게임명 ~91px 우측 밀림의 직접 원인).
- **[큐1 메타칩 중립톤] 실재.** 리스트 카드 장르칩(ARPG/핵앤슬래시/새 시즌)이 accent블루(클릭 불가인데 링크처럼 보임).
- **[큐3 리스트 날짜중복] 실재.** 그룹헤더 `2026.05.30 (토)` 바로 아래 카드 본문 `📅 2026.05.30 (토)` 반복.
- **[큐5 D-day 근접도 색단계] 실재.** 패널 D-DAY/D-4/D-54/D-215가 전부 동일 톤(임박/원거리 시각 위계 없음).
- **[IDEAS] 단일카드 풀폭 우측 빈공백·패널 게임명 좌측 어긋남(풀year가 직접 원인)·TBD 게임 12/31 그룹+D-215 가짜정밀도** 모두 라이브 잔존.

### 현재 양호 (트집 X) — 큐 소진 우선 권고
오늘(30) 파란보더+'오늘' 라벨(accent)·D-DAY 빨강, day-has 옅은면+좌측악센트, 선택셀 inset 링, sticky 그룹헤더, 패널 dot 색+모양(이번 출고 라이브)·title/aria, 검색 0건 빈상태·필터 초기화 컨트롤·모달 페이드/×44px/트레일러/링크복사, 푸터 운영자 정보 — 양호. **권고: 신규는 위 푸터 1건만 추가. 큐 4건(메타칩·흡수행 날짜·리스트 날짜중복·D-day 색단계)+IDEAS가 전 표면을 덮고 있으니 개발자 큐 소진 우선, IDEAS '패널 게임명 좌측 어긋남'(흡수행 날짜 단축과 한 뿌리) 큐 승격 1순위 후보 유지.**

---

## [2026-05-30 15:59] [디자이너] — 라이브 재검증: 직전 출고분(요일헤더 평일색)이 실렌더에서 #aaa로 반영 확인(QA 미확인 항목 해소) + 대기 큐/IDEAS 전 항목 '아직 실재' computed 수치 재확인 / 현재 양호

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px). 캘린더(5월, 오늘=30 파란보더+'오늘')·리스트·날짜패널('2026.05.28 (목) 이후 27건') 정상 렌더, 콘솔 에러/예외 0건, 배포 34건 라이브(통계줄 '총 34'), 문서 가로 오버플로 0(docScrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → computed 병행. 이번 사이클은 운영 권고대로 **신규 트집 누적 대신 (a) 직전 출고분 라이브 재검증 + (b) 대기 큐/IDEAS가 라이브에 실제로 남아있는지 computed 수치 재확인**에 집중.

### 직전 출고분 재검증 (개발자 726bc8c / QA 15:45 ✅ 출고분)
1. **[a11y·대비] 월간 캘린더 요일 헤더 평일색 #888→--text-dim — 라이브 실렌더 반영 확인 ✅ (QA가 '소스 기준 검증, 라이브 computed 미확인'으로 남긴 항목 해소)**
   - computed color 실측: 월·화·수·목·금 = 전부 `rgb(170,170,170)`(=#aaa=--text-dim) → 기존 #888 대비 다크 배경 가독성 상향 라이브 반영됨. 일=`rgb(229,115,115)`(빨강)·토=`rgb(100,181,246)`(파랑) 한국식 강조 오버라이드 그대로 유지. ⇒ QA 차기 권고였던 '라이브 렌더 대비 재확인' 충족, 항목 닫아도 됨.

### 대기 큐/IDEAS 라이브 computed 재검증 (전부 '아직 존재' — 픽업 가치 있음)
1. **[큐: 패널 dot 색-only] 실재.** 날짜패널 `.day-row-dot` 상위 6행 computed = 전부 `border-radius:50%`·`border-width:0px`·`transform:none`·`title 없음`(배경색만 4색: 주황 255,138,101 / 보라 186,104,200 / 초록 129,199,132 등). 캘린더 셀·범례는 색+모양 이중인코딩(원/사각/마름모/링)인데 **패널 행만 색-only**(WCAG 1.4.1). 미반영(기존 TODO 그대로).
2. **[큐: 메타칩 중립톤] 실재.** 리스트 카드 `.genre-tag`(ARPG) computed = 글자 `rgb(106,176,232)`/배경 `rgba(74,144,226,0.1)` = accent블루. 클릭 불가 장르칩이 링크블루라 어포던스 오인. 미반영(기존 TODO 그대로).
3. **[큐: 흡수행 날짜 풀year] 실재.** 패널 `.day-row-date` = `2026.05.28 (목)`/`2026.05.30 (토)`/`2026.06.03 (수)`/`2026.06.05 (금)` → 스펙(MM.DD) 아닌 풀year 4자리. 패널 헤더가 이미 연도 명시·전 항목 2026이라 행마다 `2026.` 반복은 잉여. 미반영(기존 TODO 그대로).
4. **[1순위 보강 재확인] 패널 D-day '좌측' 엣지 잔여 진동.** 플랫폼 컬럼 고정폭은 반영됨(직전 완료). 그러나 D-day 좌측 시작 x = `출시됨` 1430 · `D-DAY` 1427 → 텍스트 폭 차로 좌측 엣지 미세 진동 잔존(우측 ☆은 정렬). 완전 정렬 원하면 `.day-row-dday`도 고정 min-width+text-align:left 검토(기존 보강 메모 그대로, 미세건이라 신규 큐 승격 불요).
5. **[IDEAS: 단일카드 풀폭 불일치] 실재.** 리스트 05-30 단일카드가 전 열 span(풀폭)으로 우측 ~50% 빈 공백, 멀티 날짜는 좁은 카드 → 한 리스트 내 카드 폭 들쭉날쭉(기존 IDEAS 그대로).
6. **[IDEAS: 리스트 날짜 중복] 실재.** 리스트 그룹헤더 `2026.05.30 (토)` 바로 아래 카드 본문에 `📅 2026.05.30 (토)` 반복(그룹핑 날짜를 카드가 또 표기)(기존 IDEAS/5순위 후보 그대로).
7. **[IDEAS: 패널 게임명 좌측 어긋남] 실재·임팩트 최대.** 흡수행 게임명 x가 그룹헤더행과 어긋남 — 풀year 프리픽스(3번)가 직접 원인. 3번 단축+`.day-row-date` 고정 min-width 병행 시 해소(기존 IDEAS, 큐 승격 1순위 후보 유지).

### 발견한 문제 / 개선점 (기존 미등록 신규)
- **없음(현재 양호).** 라이브 실측상 신규 결함 발견되지 않음. 기존 큐 4건+IDEAS가 리스트/캘린더/패널/모달 전 표면을 이미 촘촘히 덮고 있어, 운영 절대규칙(6: 잘 되어 있으면 양호 보고)에 따라 신규 트집 누적하지 않음.

### 현재 양호 (트집 X) — 큐 소진 우선 권고
콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0, 오늘(30) 파란보더+'오늘'(accent 통일), 요일헤더 평일 #aaa 반영(신규 확인)·일/토 강조 유지, 셀/범례 dot 색+모양 이중인코딩, 패널 플랫폼 컬럼 고정폭, sticky 그룹헤더, 모달 메타 라벨 5em 정렬, 통계줄 34, 푸터 운영자 정보 — 모두 양호. **권고: 디자이너 신규 누적보다 개발자 큐 소진 우선. 임팩트순 IDEAS '패널 게임명 좌측 어긋남(풀year 단축과 한 뿌리)' 큐 승격이 여전히 1순위 후보.**

---

## [2026-05-30 14:02] [디자이너] — 라이브 실측 재검증(데스크톱 1920px): 대기 큐 4종+핵심 IDEAS 전부 '아직 존재' 수치 확인 + 흡수행 날짜 풀year 스펙이탈(신규 1건)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px, main 1168px). 리스트→캘린더(5월, 오늘=30 파란보더+'오늘')→날짜패널('2026.05.30 (토) 이후 26건') 정상 렌더, **콘솔 에러/예외 0건**, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체(34)' 일치), 문서 가로 오버플로 0(docScrollW 1905≤win 1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → computed/소스 병행. 이번 사이클은 운영 권고대로 **신규 트집 누적보다 대기 큐/IDEAS 항목이 라이브에 실제로 남아있는지 수치로 재검증**(개발자 픽업 근거 강화)에 무게 + 기존 미등록 1건만 추가.

### 대기 항목 라이브 수치 재검증 (전부 '아직 존재' 확인 — 픽업 가치 있음)
1. **[TODO 1순위 재확인] 날짜패널 우측 D-day 컬럼 흔들림 — 실재.** 패널 D-day 좌측 시작 x 측정: `D-DAY`=1427, `Switch 2 D-4`=1443, `Android D-5`=1443, `PC D-6`=1438, `Android D-16`=1438 → 플랫폼 문자열 폭(PC 14px / Android 43px / Switch 2 44px)에 따라 D-day 시작점이 **약 16px 좌우 진동**. `.day-row-plat` 고정 min-width+우측정렬(또는 plat+dday 고정폭 우측그룹)로 해소(기존 TODO 그대로).
2. **[TODO 2순위 재확인] 요일 헤더 평일 #888 — 실재.** computed color: 월~금 전부 `rgb(136,136,136)`(=#888, 다크 배경 대비 ~3:1대 AA 미달), 일=red(229,115,115)·토=blue(100,181,246)만 강조. 평일을 --text-dim(#9aa0ac급) 이상으로(기존 TODO 그대로).
3. **[TODO 3순위 재확인] 날짜패널 점 색-only — 실재.** `.day-row-dot` 6개 전부 `border-radius:50%`·`transform:none`·`border-width:0`(=4색 모두 원형). 셀/범례(05-30 색+모양 이중인코딩)와 달리 패널만 누락(WCAG 1.4.1). per-category 모양(사각/마름모/링)+title 부여(기존 TODO 그대로).
4. **[TODO 5순위 재확인] 장르칩 accent블루 — 실재.** 리스트 카드 칩 computed: 카테고리=`주황 솔리드`(255,138,101/rgba(255,87,34,.15)), 플랫폼=`중립회색`(187,187,187/#2a2e38), **장르=accent블루**(106,176,232/rgba(74,144,226,.1)). 클릭 불가 장르가 링크블루라 어포던스 오인(기존 TODO 그대로).
5. **[IDEAS 재확인·수치] 리스트 단일카드 풀폭 폭 불일치 — 실재.** `.game-card.single-game` 폭=**1168px**(그리드 전폭) vs 멀티카드=**379px**, 단일13:멀티13으로 한 리스트 안에서 폭이 약 3배 들쭉날쭉(기존 IDEAS '정정' 항목 그대로).
6. **[IDEAS 재확인·수치] 날짜패널 게임명 좌측 어긋남 — 실재, 측정상 가장 큰 정렬 결함.** `.day-row-name` 좌측 x: **흡수행(날짜 인라인)=518px vs 그룹헤더행=427px → 91px 차이**. 패널 1차정보(게임명)의 좌측 라인이 두 갈래로 갈려 세로 스캔 비용 큼. 임팩트 대비 작은 CSS라 **큐 승격 후보로 추천**(기획자 판단). 해소: `.day-row-date` 고정 min-width+우측정렬(기존 IDEAS 그대로).

### 발견한 문제 / 개선점 (기존 미등록 신규 — 1건만)
1. **[일관성·밀도·정확성·패널] '1건 날짜 흡수' 행 날짜가 스펙(MM.DD)과 달리 풀year(YYYY.MM.DD)로 렌더 → 패널 내 연도 중복 + 위 6번 게임명 밀림의 직접 원인** — 우선순위: 낮음
   - 실측: 흡수행 `.day-row-date`가 `2026.05.28 (목)`/`2026.05.30 (토)`/`2026.06.03 (수)`로 **풀year 4자리**. 완료된 기능('밀도—1건 날짜 흡수', PROJECT_STATUS 완료목록)의 스펙 문구는 `MM.DD (요일)` 인라인이었음 → 구현이 스펙과 다르게 풀year로 나감(스펙 이탈).
   - 왜 문제: (a) 패널 헤더가 이미 `2026.05.30 (토) 이후`로 연도를 명시하고 전 항목이 2026년이라 행마다 `2026.` 반복은 잉여. (b) 풀year 프리픽스(~91px)가 흡수행 게임명을 x=518로 밀어 그룹헤더행(427)과 91px 어긋나게 만드는 **직접 원인**(6번과 한 뿌리). 날짜를 `MM.DD (요일)`로 줄이면 연도중복 해소 + 게임명 밀림 폭 동반 축소(완전 정렬은 6번 min-width+우측정렬 병행 필요).
   - 어떻게(개발자): renderDayRows 흡수행 날짜 포맷을 스펙대로 `MM.DD (요일)`로(연도 생략). 그룹헤더는 풀year 유지 또는 동일 단축—택1. script.js 포맷 1줄, 신규 색 없음. ※6번(min-width 정렬)과 함께 처리하면 패널 좌측 컬럼 완전 정렬.

### 현재 양호 (트집 X)
콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0, 오늘(30) 파란보더+'오늘' 라벨(accent 통일), 출시셀 옅은면+좌측악센트, 선택셀 inset 링, 셀/범례 dot 색+모양 이중인코딩, sticky 그룹헤더, 모달 ×버튼 44px 고정대비·페이드, 통계줄 34=드롭다운 34, 푸터 운영자 정보 — 모두 양호. **권고: 대기 큐 4종이 전부 라이브에 실재함을 수치로 확인했으니 디자이너 신규 누적보다 개발자 큐 소진 우선. 임팩트 순으로는 IDEAS '패널 게임명 91px 어긋남'을 큐로 올릴 만함.**

---

## [2026-05-30 15:02] [디자이너] — 라이브 실측 재검증(데스크톱): 대기 큐/IDEAS 5종+ '아직 실재' 수치 확인 + 1순위 플랫폼정렬 반영 후 D-day 좌측엣지 잔여 진동 + 신규 1건(리스트 날짜 그룹헤더↔카드 날짜 중복)

실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px). 리스트↔캘린더(5월, 오늘=30 파란보더+'오늘')·날짜패널('2026.05.30 (토) 이후 26건') 정상 렌더, **콘솔 에러/예외 0건**, 배포 34건 라이브(통계줄 '총 34'=드롭다운 '전체(34)' 일치), 문서 가로 오버플로 0(docScrollW 1905≤1920). 모바일은 이번에도 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → 소스/computed 병행. 이번 사이클은 운영 권고대로 **신규 트집 누적보다 대기 큐/IDEAS가 라이브에 실제로 남아있는지 수치 재검증**(개발자 픽업 근거 강화) + 미등록 신규 1건만.

### 대기 항목 라이브 수치 재검증 (전부 '아직 존재' — 픽업 가치 있음)
1. **[1순위 반영 확인 + 잔여] 플랫폼 컬럼 정렬은 반영됨 / D-day '좌측' 엣지는 여전히 ~16px 진동.** computed: `.day-row-plat` 폭이 6행 모두 54px 고정(우측정렬 반영 OK). 그러나 D-day 좌측 시작 x = D-DAY 1427 · D-4/D-5/D-6 1443 · D-16 1438 → D-day 텍스트 폭(D-DAY 5자 > D-4 3자)에 따라 **D-day 좌측 엣지가 ~16px 진동**(우측 ☆은 고정이라 우측엣지는 정렬됨). 완료기재('D-day가 행마다 동일 좌측 x에서 시작')와 라이브가 부분 불일치 — 좌측 컬럼까지 맞추려면 `.day-row-dday`에도 고정 min-width(예 4em)+`text-align:left` 필요. (잔여 미세건, 신규 큐 승격까진 불요 — 기존 1순위 보강 메모로만)
2. **[큐: 요일헤더 평일] 실재.** computed color: 월~금 전부 `rgb(136,136,136)`(=#888, 다크 배경 AA 미달), 일=red(229,115,115)·토=blue(100,181,246)만 강조. 미반영(기존 TODO 그대로).
3. **[큐: 패널 dot 색-only] 실재.** `.day-row-dot` 6개 전부 `border-radius:50%`·`transform:none`·`border-width:0`·`title 없음`(=4색 모두 원형, 색만). 셀/범례(색+모양 이중인코딩)와 달리 패널만 누락(WCAG 1.4.1). 미반영(기존 TODO 그대로).
4. **[큐: 메타칩 중립톤] 실재.** 리스트 카드 `.genre-tag` computed = 글자 `rgb(106,176,232)`/배경 `rgba(74,144,226,0.1)` = **accent블루**. 클릭 불가 장르칩이 링크블루라 어포던스 오인. 미반영(기존 TODO 그대로).
5. **[큐: 흡수행 날짜 풀year] 실재.** 패널 흡수행 `.day-row-date` = `2026.05.30 (토)`/`2026.06.03 (수)`/`2026.06.05 (금)` → 스펙(MM.DD) 아닌 풀year. 미반영(기존 TODO 그대로).
6. **[IDEAS: 패널 게임명 좌측 어긋남] 실재·측정상 최대 정렬 결함.** `.day-row-name` 좌측 x: 흡수행 **518** vs 그룹헤더행 **427** = **91px 차이**(직전 사이클과 동일). 5번 단축이 들어가면 폭 동반 축소.
7. **[IDEAS: 단일카드 풀폭 불일치] 실재.** `.game-card.single-game` 폭=**1168px** vs 멀티카드=**379px**(약 3배). 단일카드 우측 ~50% 빈 공백 그대로.
8. **[IDEAS: 카테고리 명칭 통일] 실재·3갈래로 더 벌어짐.** 같은 분류가 통계줄 `신규서버`(붙임) / 범례 `신규 서버`(띄움) / **카드태그 `한국 MMO 신규 서버`** 로 3종 표기. 통일 작업 시 카드태그 출처까지 포함 필요.
9. **[IDEAS: dev==publisher 병합] 실재.** FF7 리버스 카드가 개발 `Square Enix` / 퍼블리셔 `Square Enix` 동일값 2줄 중복 노출.

### 발견한 문제 / 개선점 (기존 미등록 신규 — 1건만)
1. **[밀도·정보중복·리스트] 리스트 뷰에서 날짜 그룹헤더(`2026.05.30 (토)`)와 그 아래 카드의 `.release-date`(`📅 2026.05.30 (토)`)가 동일 날짜를 세로 ~80px 안에서 두 번 표기 → 날짜 중복** — 우선순위: 낮음
   - 실측: 리스트 그룹헤더 바로 아래 첫 카드 본문에 `📅 2026.05.30 (토)`가 그대로 반복. 2건↑ 날짜 그룹에선 한 헤더 아래 **모든 카드가 같은 날짜를 각자 반복**(헤더 1 + 카드 N번 = N+1회 동일 날짜). 그룹핑의 목적이 '날짜를 헤더로 한 번만'인데 카드가 또 찍어 그룹핑 이점이 반감.
   - 왜 문제: 같은 정보(날짜)가 인접 반복돼 스캔 노이즈↑, 정작 카드 1차정보(게임명/D-day)의 시각 비중이 날짜에 분산. (기등록 '흡수행 날짜 단축'은 패널 표면, '모달 날짜 포맷'은 모달 표면 → 이건 리스트 카드↔그룹헤더 중복으로 별개 표면.)
   - 어떻게(개발자): 리스트 뷰 카드의 `.release-date` 줄을 그룹헤더가 날짜를 책임지는 맥락에선 **숨기거나**(그룹핑이 날짜를 표현하므로) D-day만 남기고 날짜 텍스트 제거. 단독/검색결과 등 그룹헤더 없는 맥락에서만 카드 날짜 노출하는 분기도 가능. 신규 색 없음, script.js 렌더 분기 소규모.

### 현재 양호 (트집 X)
콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0, 오늘(30) 파란보더+'오늘'(accent 통일), 출시셀 옅은면+좌측악센트, 선택셀 inset 링, 셀/범례 dot 색+모양 이중인코딩, sticky 그룹헤더, 패널 플랫폼 컬럼 고정폭 정렬(1순위 반영), 통계줄 34=드롭다운 34, 푸터 운영자 정보 — 모두 양호. **권고: 대기 큐/IDEAS가 거의 전 표면을 덮음. 디자이너 신규 누적보다 개발자 큐 소진 우선. 임팩트순으로는 IDEAS '패널 게임명 91px 어긋남' 큐 승격이 여전히 1순위 후보.**

---

## [2026-05-30 12:58] [디자이너] — 라이브 재검증: 모달 메타 라벨 5em 정렬 데스크톱+모바일 실측 ✅ (QA 미확인 항목 해소) / 현재 양호
실측: https://gcalen.com/ Chrome **데스크톱 1440px + 모바일 390px 둘 다 실리사이즈 적용 성공**(이전 사이클들이 못 봤던 모바일 뷰 이번엔 실측). 캘린더(5월, 오늘=30 파란보더+'오늘' 라벨)·리스트·상세모달 정상, **콘솔 에러/경고 0건**(MetaMask 확장 lockdown 로그만, 사이트 무관), 배포 **34건 라이브**(직전 QA가 본 CDN 지연 따라잡음, games.json 34=통계줄 34). 이번 사이클은 직전 디자이너(12:10)·운영 권고대로 신규 트집 누적 대신 **직전 출고분 재검증**에 무게.

### 재검증 결과 (직전 개발자 12:28 / QA ✅ 12:45 출고분)
1. **[정렬·모달] '모달 메타 행 라벨 폭 5em 통일' — 라이브 반영 + 데스크톱·모바일 모두 정상 확인 ✅ (QA가 남긴 '모바일 좁은 폭 라벨 잘림/값 눌림' 미확인 항목 해소)**
   - 라이브 CSS 확인: `.modal .modal-row strong { display:inline-block; min-width:5em; vertical-align:top }` 적용됨.
   - 데스크톱(1440px) 실측: 출시일/플랫폼/장르/개발/퍼블리셔 5행 라벨 폭 전부 80px, **값 시작 x = 568px 5행 완전 동일**(단일 컬럼 정렬 성공).
   - 모바일(390px) 실측: 라벨 폭 80px 유지, **값 시작 x = 437px 5행 동일**, 최장 라벨 '퍼블리셔' scrollWidth 64<80 → **잘림 없음**, 모달폭 358px·문서 가로 오버플로 0(docScrollW 390=winW 390) → **값 눌림/넘침 없음**. ⇒ QA 차기사이클 권고였던 모바일 라벨 잘림 우려는 실측상 문제 없음. 해당 항목은 닫힌 채 유지해도 됨.

### 현재 양호 (트집 X) — 신규 제안 없음
- 콘솔 에러 0건, 배포 34건 라이브, 가로 오버플로 0(모바일 390px), 오늘(30) 셀 파란보더+'오늘' 라벨, 검색 0건 빈 상태 안내, 모달 ×버튼 44px 고정대비·페이드, dot 색+모양 이중인코딩(셀/범례), sticky 그룹헤더, 통계줄 34=드롭다운 34, 푸터 운영자 정보 — 모두 양호.
- **부수 재확인(신규 아님, 중복 등록 안 함):** 모바일 390px에서 퀵칩 '이번 주/다음 주' 높이 36px·'전체 보기' 33px = 기존 IDEAS '터치타겟<44px(WCAG 2.5.5)' 항목이 아직 미반영임을 라이브로 재확인. 신규 트집 아님 → 기존 IDEAS 항목 픽업 권고.
- **권고 유지:** TODO 큐(5건)+IDEAS(20여 건)가 리스트/캘린더/패널/모달/SEO/a11y 전 표면을 이미 덮음. 디자이너 신규 누적보다 **기획자·개발자의 기존 큐 소진**이 임팩트 큼. 다음 디자이너 사이클도 출고분 라이브 재검증 위주를 제안.

---


## [2026-05-31 00:07] [디자이너] — 소스 점검(Chrome 브리지 불안정 fallback): 미사용 Google Fonts preconnect·캘린더 뷰 로딩 신호 부재·전역 a11y 구조(skip/h2/live)
실측 메모: 라이브 https://gcalen.com/ 홈 fetch(200, 정상)·repo 소스(index.html/styles.css/script.js) 병행 점검. 이번 사이클은 Chrome 도구 브리지가 응답 지연/탭소실로 스크린샷 확보가 불안정 → 작업지침 fallback('Chrome 못 쓰면 최소 index.html/styles.css로 평가')에 따라 소스 기준 정적 점검. 라이브 홈 HTML 정상(메타/OG/canonical/JSON-LD/카테고리/바로가기/푸터 contact@gcalen.com). 기존 TODO 큐(approx 그룹헤더 요일·흡수행 게임명 좌측정렬·검색 aria-label·카테고리 라벨 단일출처) 및 대량 IDEAS는 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[성능·정리·프라이버시] 사용하지 않는 Google Fonts preconnect가 매 방문마다 제3자 사전연결을 일으킴** — 우선순위: 낮음
   - 실측/소스: index.html `<head>`에 `<link rel="preconnect" href="https://fonts.googleapis.com">` 류 preconnect가 있으나, 실제로 로드되는 웹폰트(link rel=stylesheet …fonts… 또는 @font-face)가 전혀 없음. body는 시스템 폰트 스택(`-apple-system,…,"Noto Sans KR",sans-serif`, styles.css L18)만 사용.
   - 왜 문제: 기능상 안 쓰는데도 모든 방문에서 fonts.googleapis.com으로 DNS+TLS 사전연결 발생 → 미세 성능 낭비 + 폰트를 안 쓰면서 제3자(Google)에 접속 신호를 보냄(불필요 의존·프라이버시). (※IDEAS 'Pretendard 등 커스텀 폰트 도입'은 폰트를 추가하는 건, 이 건은 '폰트 없이 떠 있는 미사용 preconnect 제거'로 별개.)
   - 어떻게(개발자): 폰트 도입 계획 없으면 해당 preconnect 줄 삭제. 추후 도입 시엔 실제 stylesheet/@font-face와 짝지어 preconnect(googleapis+gstatic, crossorigin)로 정식 추가. index.html 1줄, 외형 무변.

2. **[빈 상태·로딩] 기본(캘린더) 뷰로 진입할 때 데이터 fetch 동안 로딩 신호가 전혀 없음 — '불러오는 중…'이 숨겨진 리스트 뷰에만 들어감** — 우선순위: 보통
   - 실측/소스: `loadData()`가 로딩 텍스트를 `#games-list`에만 주입(`gamesList.innerHTML='<p class="loading">불러오는 중...</p>'`, script.js L40). 그런데 기본 뷰는 캘린더 고정(리스트 컨테이너 숨김)이라 첫 화면(캘린더)에선 이 문구가 안 보이고 빈 `#calendar-grid`만 노출. 정상 로딩 중 피드백 0이고, 안내는 9초 뒤 `#load-fallback`(index.html 인라인 가드, 크래시/지연용)뿐.
   - 왜 문제: 첫인상 화면(캘린더)에서 0~2초간 '빈 격자'만 보여 '데이터 없음/고장'으로 오인 가능(느린 네트워크일수록). (기등록 'calendar 0건 빈 상태'는 로드 후 그 달 출시 0건 안내, 'load-fallback'은 9초 크래시 안내 → 둘 다 '정상 로딩 중' 신호와는 별개.)
   - 어떻게(개발자): loadData 시작 시 캘린더 컨테이너(또는 calendar-grid)에도 로딩 플레이스홀더(스켈레톤/'불러오는 중…')를 넣고 renderCalendar에서 교체. 또는 활성 뷰와 무관한 공통 상위 로딩 표시 1개로 통합. script.js 소규모, 신규 색 없음.

3. **[a11y·구조] 페이지 전역에 콘텐츠 바로가기(skip link)·결과영역 라이브리전이 없음** — 우선순위: 낮음
   - 소스: `<main>`·`<nav>` 랜드마크와 h1·h2(월 라벨)는 있으나(양호), 상단 컨트롤(뷰토글/검색/필터/퀵칩) 다수를 건너뛰는 skip-to-content 링크가 없고, 검색·필터로 건수가 바뀌는 결과 영역(#stats-summary/#games-list/#search-count)에 `aria-live`가 없음.
   - 왜 문제: 키보드 사용자는 매번 헤더·컨트롤을 Tab으로 지나야 본문(캘린더/리스트) 도달(WCAG 2.4.1), 스크린리더는 필터/검색 결과 건수 변화가 안 읽힘(상태 변경 미고지). (기존 '캘린더 grid SR 시맨틱'은 그리드 내부 row/gridcell 건 → 이 건은 페이지 전역 skip-link/live-region으로 별개.)
   - 어떻게(개발자): (a) body 첫머리 `.skip-link`(본문으로 점프, 평소 숨김·focus 시 노출) + main에 id, (b) #stats-summary/#search-count(또는 결과 컨테이너)에 `aria-live="polite"`. index.html+styles.css 소규모, 시각상 거의 무변.

### 현재 양호 (트집 X)
라이브 홈 HTML 200·메타/OG/canonical/twitter/JSON-LD 정상, `<main>`/`<nav aria-label>`/h1·h2/뷰토글 aria-pressed/네비 버튼 aria-label/필터 초기화 버튼 등 마크업 양호, 반응형 미디어쿼리(768/480/reduced-motion) 존재, load-fallback 가드·prefers-reduced-motion 분기 양호, 캘린더 셀 focus-visible·요일헤더 평일 대비 상향(--text-dim)·주말 색 반영 라이브 확인. 기존 대량 TODO/IDEAS는 중복 등록 안 함 — **큐 소진 우선 권고 유지**.

---

## [2026-05-30 12:10] [디자이너] — 라이브 재점검: 리스트 '단일게임 풀폭' 카드 우측 절반 공백 미해소(과거 '해소됨' 판정 정정)
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px, main 1200px). 캘린더(5월, 오늘=30)·리스트·상세모달·검색 0건 빈 상태 모두 정상 렌더, 콘솔 에러 0건, 배포 34건 라이브. 검색 0건 빈 상태("'zzzqqq'에 일치하는 게임이 없어요." + input 인라인 ×지우기 + 드롭다운 '전체(0)' 카운트 동기화)는 친절·정상이라 트집 X. 모바일은 창 리사이즈가 실렌더 뷰포트(1920 고정)에 미반영 → 소스 CSS/computed style 병행. **이번 사이클은 새 트집보다, 기존 대량 큐/IDEAS가 거의 모든 영역을 덮고 있어 신규 항목을 무리하게 만들지 않고 — 라이브 computed 실측으로 과거 "해소" 판정 1건의 정확성을 재검증함.**

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[리스트·가로공간·정정] '단일게임 풀폭 행'은 반영됐으나 카드 내용이 좌측에 세로로 쌓여 우측 ~50%가 여전히 빈 공백 — 과거 노트의 '가로 낭비 해소됨 OK'(DESIGN_NOTES line 664) 판정이 라이브 실측과 불일치** — 우선순위: 보통
   - 실측(computed): 리스트 `.games-grid`는 3열 그리드(`378.66px ×3`)인데, 단일게임 날짜 카드는 `.game-card.single-game`이 전 열을 span해 **폭 1168px**로 늘어남(`max-width:none`). 그런데 제목·설명·메타(개발/배급/플랫폼/장르)가 전부 좌측에 세로로 스택돼 카드 우측 절반은 빈 다크 공백. 같은 화면의 **2건 날짜(예 06-04)는 균형 잡힌 379px 카드 2개**라, 한 리스트 안에서 단일=풀폭(우측 텅) / 멀티=좁은 카드로 카드 폭이 들쭉날쭉(현재 26개 중 단일 13 : 멀티 13으로 정확히 반반).
   - 왜 문제: 원래 제안(line 708, '높음')의 의도는 **(a) 가로형 카드(배너/메타를 좌·우로 배치해 폭 활용) 또는 (b) 단일게임 max-width 560px 단일 컬럼**이었는데, 구현은 '세로 카드를 풀폭으로 stretch'에 그쳐 가로 공간 활용도 안 되고 폭 일관성도 안 생김. 즉 낭비가 **해소가 아니라 형태만 바뀜**(과거 '해소됨' 판정은 풀폭 적용=해결로 본 오판). D-day·플랫폼 같은 핵심 메타가 넓은 빈 우측을 두고 좌측에 몰려 스캔 효율도 손해.
   - 어떻게(개발자, 택1): (a) `.single-game` 카드를 **2열 가로 레이아웃**으로 — 좌(카테고리/제목/설명) · 우(출시일·D-day·플랫폼·장르 메타)를 `display:grid;grid-template-columns:1fr minmax(220px,300px)`로 나눠 폭을 채움. **또는** (b) 단일게임 카드의 전열 span을 제거하고 `max-width:600px`(좌측 정렬)로 둬 멀티 카드(379px)와 시각 폭을 맞춤(가장 적은 변경). styles.css 중심 소규모, 신규 색 없음. ※반영 시 과거 '해소됨 OK' 줄은 정정 필요.

### 현재 양호 (트집 X) — 그리고 큐 소진 우선 권고
- 검색 0건 빈 상태 안내·인라인 ×지우기·드롭다운 카운트 동기화, 오늘(30) 파란 보더+'오늘' 라벨(accent 통일 라이브)·D-DAY 빨강, 출시 있는 셀 옅은 면+좌측 악센트(day-has), 선택 셀 inset 링, dot 색+모양 이중인코딩(셀/범례), sticky 그룹 헤더, 모달 페이드·×버튼 44px 고정대비(직전 개발자 반영)·트레일러/링크복사/출처/전체페이지, 통계줄 34=드롭다운 34, 푸터 운영자 정보 — 모두 양호.
- **권고: 현 시점 UX/UI 백로그(TODO 큐 5건 + IDEAS 20여 건)가 리스트/캘린더/패널/모달/SEO/a11y 전 표면을 이미 촘촘히 덮고 있음.** 디자이너가 신규 트집을 더 쌓기보다, 기획자·개발자가 **기존 큐 소진**에 집중하는 편이 임팩트가 큼. 다음 디자이너 사이클은 큐 항목이 라이브 반영된 뒤 그 결과를 **재검증(이번처럼 '해소' 판정 정확성 확인)** 하는 데 무게를 두는 것을 제안.

---

## [2026-05-30 10:05] [디자이너] — 라이브 실측: SEO 카테고리 목록 페이지(/upcoming-games) 첫 점검 + 날짜패널 우측 D-day 컬럼 미정렬 + 범례/바로가기 저대비
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px). 캘린더(5월, 오늘=30 파란보더+'오늘' 라벨 accent색 통일 확인)·리스트·날짜패널('2026.05.30 (토) 이후 26건')·상세모달(디지몬 스토리) 정상 렌더, 콘솔 에러 0건, 배포 34건 라이브. 모달 오버레이는 rgba(0,0,0,0.7)로 디밍 충분(트집 X). 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일)→소스 CSS 병행. **이번 사이클 신규 범위: 그동안 한 번도 평가 안 된 SEO '카테고리 목록' 페이지(/upcoming-games 등 seo-nav 대상, build.js 생성)를 직접 방문해 평가.** — 결과: 교차 카테고리 링크·'전체 캘린더' 복귀 링크·푸터 '홈' 링크 모두 존재해 dead-end 아님(양호). 기존 TODO 큐/IDEAS 대기 항목(모달 배너 카테고리 dedup·모달 × 저대비·모달 메타 라벨 정렬·필터 초기화 컨트롤·패널 게임명 좌측 정렬·모달 D-day 배지화·'오늘로' 무의미 상태·SEO상세 D-day/헤더정렬/날짜포맷·.day-row-dot 색-only·카테고리 명칭 4곳 통일·요일헤더 #888·주말 날짜숫자 색·터치타겟44px·위시 --wish 토큰·전역 focus-visible·D-day 근접 색단계·date_tbd 12/31·SVG/폰트/라운드 토큰 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[정렬·스캔성·패널] 날짜 클릭 패널 행의 우측 '플랫폼·D-day'가 고정 컬럼이 아니라 플랫폼 텍스트 폭에 따라 D-day 시작 x가 행마다 흔들림** — 우선순위: 보통
   - 실측/소스: 행 마크업이 `name(flex:1) → plat → dday → ☆` 순(script.js L225~227), `.day-row-plat`/`.day-row-dday`는 `flex-shrink:0`에 min-width 없음(styles.css L613~614). 플랫폼 문자열이 'PC'/'Android'/'Switch 2'로 폭이 제각각이라, 그 우측에 붙는 D-day 배지(D-DAY/D-4/D-16)의 좌측 시작 x가 행마다 달라짐(zoom 실측: 별 ★은 우측 고정이나 D-day는 좌우로 흔들림). 같은 패널에서 'Switch 2 D-4'행과 'Android D-5'행의 D-day 컬럼이 안 맞음.
   - 왜 문제: D-day는 이 앱의 1차 가치 지표인데 정작 게임명을 읽는 패널에서 세로 컬럼을 못 이뤄 '얼마 남았나'를 행마다 다른 위치에서 찾아야 함(스캔 비용↑). (기등록 '패널 게임명 좌측 정렬'은 행 왼쪽 정렬 건, '모달 메타 라벨 정렬'은 모달 표면 건 → 이 건은 패널 행 '우측' 클러스터 정렬로 셋 다 별개.)
   - 어떻게(개발자): `.day-row-plat`에 고정 `min-width`(예 5em)+`text-align:right`를 주거나, plat+dday를 고정폭 우측 그룹(`display:inline-flex; justify-content:flex-end; min-width`)으로 묶어 D-day가 모든 행에서 동일 좌측 컬럼에서 시작하도록. styles.css 1~2줄, 신규 색 없음.

2. **[일관성·SEO 카테고리 페이지] /upcoming-games 등 카테고리 목록 페이지의 '신규 서버' 카테고리 라벨이 '한국 MMO 신규 서버'로 찍혀 메인 앱(범례/드롭다운='신규 서버')과 문구 불일치** — 우선순위: 낮음
   - 실측: /upcoming-games 행 태그가 '한국 MMO 신규 서버'(예: 리니지M·붉은사막 인접). 메인 앱 범례·드롭다운·통계줄은 같은 카테고리를 '신규 서버'로 표기 → 같은 분류가 표면마다 '신규 서버' vs '한국 MMO 신규 서버'로 갈림. '글로벌 대작'·'국내 모바일'·'국내 PC/콘솔'은 메인과 일치하나 신규서버만 어긋남. (기존 '카테고리 명칭 4곳 통일'은 메인 앱 4곳 한정 → SEO 카테고리 페이지는 한 번도 평가 안 된 추가 표면이자 가장 어긋난 문구라 별개 등록.)
   - 왜 문제: 검색 유입의 첫 화면(SEO 페이지)과 메인 앱에서 같은 카테고리 명칭이 달라 '같은 분류/서비스'라는 연속성·신뢰가 약해짐. 명칭 통일 작업 시 SEO 표면이 빠지면 영구 불일치로 남음.
   - 어떻게(개발자): build.js 카테고리 라벨을 메인 categories 라벨 맵과 동일 소스에서 가져오도록 통일(전 표면 단일 출처). 빌드 템플릿 소규모, 신규 색 없음.

3. **[a11y·대비] 캘린더 범례(.calendar-legend #aaa)와 하단 '바로가기' 영역(seo-nav 링크 #8a93a0·제목 #666) 텍스트가 다크 배경 대비 낮음** — 우선순위: 낮음
   - 소스: `.calendar-legend{color:#aaa}`(styles.css L448), `.seo-nav a{color:#8a93a0}`·`.seo-nav-title{color:#666}`(L678·L680). #666은 다크 배경 대비 약 2.5:1대로 WCAG AA(4.5:1) 크게 미달, #aaa/#8a93a0도 보조 텍스트 경계.
   - 왜 문제: 범례는 캘린더 점 색의 의미를 해독하는 키이고, '바로가기'는 카테고리 SEO 페이지 진입 동선인데 둘 다 가장 흐린 텍스트 축에 속해 저시력/저대비 환경에서 읽기 어려움.
   - 어떻게(개발자): 범례·seo-nav 링크 텍스트를 토큰 --text-dim(#9aa0ac급) 이상으로, seo-nav-title(#666)은 최소 --text-dim 이상으로 상향. 카테고리 점/링크 색은 유지. styles.css 3줄, 신규 색 없이 기존 토큰 재사용.

### 현재 양호 (트집 X)
오늘(30) 셀 파란 보더+'오늘' 라벨(accent색 통일 완료 라이브 확인)·D-DAY(빨강) 정상, 출시 있는 셀 옅은 면+좌측 악센트(day-has) 라이브, 선택 셀 채움+inset 링, dot 색+모양 이중인코딩(셀/범례), sticky 날짜 그룹헤더, 패널 행 클릭→모달 정상, 모달 오버레이 디밍(0.7) 충분, 모달 페이드·트레일러/링크복사/출처/전체페이지 액션, 통계줄 34=드롭다운 34 일치, 푸터 운영자 정보(© 2026·contact@gcalen.com). **SEO 카테고리 페이지(/upcoming-games)는 200·구조 클린·교차 카테고리 링크/전체 캘린더 복귀/홈 링크 모두 동작(dead-end 아님).** 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-30 09:02] [디자이너] — 라이브 실측: '오늘로' 무의미 상태·날짜패널 게임명 좌측 어긋남·필터 초기화 부재·모달 D-day 배지 불일치
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px). 캘린더(5월, 오늘=30 파란보더+'오늘' 라벨)·리스트·날짜패널('2026.05.30 (토) 이후 출시 26건')·상세모달(파판7 리버스) 정상 렌더, 콘솔 에러 0건, 배포 32건 라이브. 5월은 27·28·30일에만 출시(1~26 빈 그리드)라 진입 첫 화면 상단이 휑함 — 단 이는 기등록('진입 시 가장 가까운 출시 달 초기화/월 네비 옆 건수 카운트')과 동일 테마라 신규 미등록. 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일)→소스 CSS 병행. 퀵칩(이번 주/다음 주/위시) 활성 시 밝은 보더로 active 표기 정상(트집 X). 기존 TODO 큐/IDEAS 대기 항목(모달 ×저대비·모달 메타 라벨폭 정렬·모달 배너 카테고리 dedup·개발=퍼블리셔 병합·approx 그룹헤더 요일·캘린더 grid SR 시맨틱·요일헤더 #888 대비·주말 날짜숫자 색·'오늘' 라벨 amber→accent·터치타겟44px·위시 --wish 토큰·전역 focus-visible·패널 dot 색-only·패널 플랫폼 1개만·범례↔통계 통합·카테고리 명칭 통일·date_tbd·D-day 근접 색단계·SEO 상세페이지 D-day/포맷·SVG/폰트/라운드 토큰 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[정보구조·발견성] 활성화된 필터를 한 번에 되돌리는 '초기화' 컨트롤이 없음 — 카테고리/플랫폼/기간/검색/주(이번·다음)/위시 6종 필터를 각각 수동으로 풀어야 기본 상태로 복귀** — 우선순위: 보통
   - 실측: 컨트롤 행은 검색 input + 카테고리/플랫폼/기간 드롭다운 + '이번 주 출시'/'다음 주 출시'/'위시리스트만 보기(3)' 칩뿐, 어디에도 '전체 보기/초기화/필터 해제' 단일 컨트롤이 없음. '이번 주 출시'를 켜고 카테고리를 바꾼 뒤 원상복귀하려면 칩 다시 토글 + 드롭다운 3개 수동 리셋 + 검색어 삭제를 일일이 해야 함.
   - 왜 문제: 필터를 여러 개 조합한 사용자가 '처음 상태'로 빠르게 못 돌아감(특히 결과 0건 빈 상태에 빠졌을 때 탈출구가 불명확). 탐색형 사이트에서 reset 부재는 길 잃음 유발. (기등록 '통계줄/범례 클릭=카테고리 필터'는 필터 적용 쪽, 이건 '전체 해제' 쪽이라 별개.)
   - 어떻게(개발자): 비기본 필터가 1개라도 활성일 때만 컨트롤 행 끝(또는 통계줄 옆)에 '필터 초기화' 링크/버튼 노출 → 클릭 시 카테고리/플랫폼='', 기간=기본('앞으로 1년'), 검색='', 주 칩·위시 off로 일괄 리셋 후 재렌더. 전부 기본이면 버튼 숨김. 신규 색 없이 기존 칩/링크 톤 재사용, script.js 리셋 함수 1개 + 노출 토글.

2. **[정렬·스캔성·패널] 날짜 클릭 패널에서 게임명 좌측 시작점이 행 종류별로 어긋나 이름 줄이 들쭉날쭉** — 우선순위: 보통
   - 실측: '1건 날짜 흡수' 행(예 `2026.06.05 (금) ● 고딕 1 리메이크`)은 인라인 날짜(`.day-row-date`) 뒤에서 점·게임명이 시작해 우측으로 밀리고, 2건↑ 그룹헤더 아래 행(예 `● 미르의 전설: 진`)은 점·게임명이 맨 왼쪽에서 시작 → 같은 패널 안에서 게임명 좌측 라인이 두 종류로 갈려 세로 스캔 시 이름 시작 x가 행마다 달라짐(zoom 실측). '1건 날짜 흡수'(개발자 05-30) 기능이 만든 정렬 부작용으로, 흡수 기능 자체(기완료)와는 별개 잔여 정렬 건.
   - 왜 문제: 목록의 1차 정보(게임명)가 일정한 좌측 컬럼에서 시작하지 않아 빠른 훑기 비용↑. 흡수형/그룹형이 섞이면 어긋난 두 들여쓰기 그룹처럼 보임.
   - 어떻게(개발자): (a) `.day-row-date`(흡수 날짜)에 고정 `min-width`(예 7.5em)+우측 정렬을 줘 점·게임명이 그룹헤더 행과 동일 x에서 시작하도록 컬럼 정렬, 또는 (b) 흡수형도 점·게임명을 항상 맨 왼쪽 고정 컬럼에 두고 날짜는 D-day 옆 회색 메타로 우측 이동. styles.css 중심 소규모, 신규 색 없음.

3. **[일관성·모달] 상세 모달의 D-day가 plain 텍스트('· D-4')라 리스트·날짜패널의 컬러 D-day 배지와 시각 처리가 불일치** — 우선순위: 낮음
   - 실측: 파판7 모달 출시일 행이 `출시일 2026.06.03 (수) · D-4`로 D-day가 본문과 같은 평문 인라인. 반면 리스트 카드·날짜 패널은 동일 D-day를 우측에 컬러(amber) 배지로 강조. 같은 'D-N' 지표가 표면마다 배지/평문으로 다르게 보임.
   - 왜 문제: D-day는 이 앱의 1차 가치 지표인데 모달(가장 자세히 보는 화면)에서 가장 약하게(평문) 표현돼 위계가 역전. 표면 간 동일 지표의 시각 언어 불일치로 일관성 저하. (기등록 'D-day 근접 색단계화'는 색 단계 부여 건, 이건 모달의 배지화/스타일 통일 건으로 별개.)
   - 어떻게(개발자): 모달 출시일 행의 'D-N'을 리스트/패널과 동일한 `.dday` 배지 컴포넌트로 렌더(approx면 '(예정)' 유지). script.js openModal 출시일 줄 소규모, 기존 배지 스타일 재사용(신규 색 없음).

4. **[정렬·심미·캘린더] '오늘로' 버튼이 이미 현재 달(오늘 포함 월)을 보고 있을 때도 항상 표시·활성이라 눌러도 변화 없는 무의미 컨트롤** — 우선순위: 낮음
   - 실측: 진입 기본이 '오늘 이후 가장 가까운 출시 달'(보통 현재 달=5월)이라 첫 화면에서 '오늘로'는 이미 오늘 달을 보는 상태 → 클릭해도 무반응. 그런데 ‹ 월라벨 › 옆에 늘 떠 있어 헤더가 길어지고 무의미 클릭을 유발.
   - 왜 문제: 현재 위치로 가는 버튼이 현재 위치에서 활성인 건 어포던스 거짓 신호(클릭=무반응). 월 네비 영역 군더더기. (기등록 '오늘로 정렬'은 위치/정렬 건, 이건 '현재 달일 때 무의미 표시' 상태 건으로 별개.)
   - 어떻게(개발자): calendarYear/Month가 오늘의 연·월과 같으면 '오늘로'를 숨김(또는 disabled+흐리게), 다른 달로 네비했을 때만 노출. renderCalendar에서 비교 1줄 + 토글. 신규 색 없음.

### 현재 양호 (트집 X)
오늘(30) 셀 파란 보더+'오늘' 라벨·D-DAY(빨강) 정상, 출시 있는 셀 옅은 면+좌측 악센트(day-has) 라이브 확인, 선택 셀 채움+inset 링↔오늘 보더 위계 분리, dot 색+모양 이중인코딩(셀/범례), sticky 날짜 그룹헤더, 날짜패널 행 마우스 클릭→모달 정상, 퀵칩 active 보더 표기, 모달 페이드·트레일러/링크복사/출처/전체페이지 액션, 통계줄 32=드롭다운 32 일치, 푸터 운영자 정보(© 2026·contact@gcalen.com). 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-31 20:50] [디자이너] — 라이브 실측: SEO 상세 페이지(/game/{id}) 첫 점검 + 날짜 패널 점 색-only
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px) 캘린더(5월, 오늘=30)·리스트·날짜패널·상세모달 정상, 콘솔 에러 0건, 배포 32건 라이브. **이번 사이클 신규 범위: 그동안 한 번도 평가 안 된 SEO 상세 페이지 `/game/{id}`(모달 '전체 페이지' 링크 대상, build.js 생성)를 직접 방문(ff7-rebirth-switch2-2026)해 평가.** 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일)→소스 CSS 병행. 기존 TODO 큐/IDEAS 대기 항목(모달 ×저대비·모달 메타 정렬·모달 배너 카테고리 dedup·개발=퍼블리셔 병합(모달/카드)·approx 그룹헤더 요일·캘린더 grid SR 시맨틱·요일헤더 #888 대비·주말 날짜숫자 색·이벤트셀 면강조·'오늘' 라벨 amber→accent·터치타겟44px·위시 --wish 토큰·전역 focus-visible·.day-row hover=focus·패널 플랫폼 1개만·범례↔통계 통합·바로가기 칩·date_tbd·D-day 배지 색단계·SVG/폰트/라운드 토큰·카테고리 명칭 통일 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[일관성·정보·SEO페이지] 게임별 상세 페이지(/game/{id})에 D-day 카운트다운이 없음 — 앱 전체의 핵심 지표가 정작 게임 단독 랜딩에서 빠짐** — 우선순위: 보통
   - 실측/소스: 상세 페이지는 `📅 출시일: 2026년 6월 3일`만 표기(build.js L84). 메인 앱은 캘린더 셀·리스트·날짜패널·상세모달 어디서나 'D-DAY'/'D-47'을 크게 노출하는데, 검색유입의 첫 화면이 될 SEO 단독 페이지엔 D-day가 전혀 없음.
   - 왜 문제: 이 사이트의 1차 가치 제안이 '곧 나올 게임까지 며칠'인데, 게임 한 개만 보는 페이지에서 그 값이 사라져 메인보다 정보가 빈약. 검색으로 들어온 사용자가 '얼마 남았나'를 못 봄(앱 정체성 약화).
   - 어떻게(개발자): build.js 상세 템플릿 출시일 줄 옆/아래에 메인과 동일 계산(`parseReleaseDate` 로컬자정 기준)으로 'D-N / D-DAY / 출시됨' 1개 추가. approx면 '(예정)' 유지. 정적 빌드라 빌드 시점 기준이니 작은 클라이언트 스크립트로 갱신하거나 '출시일 기준' 표기로 명확화. 신규 색 없이 메인 .dday 톤 재사용.

2. **[일관성·정체성·SEO페이지] 상세 페이지 헤더·날짜 포맷이 메인 앱과 따로 놀아 같은 사이트로 안 읽힘** — 우선순위: 보통
   - 실측/소스: (a) 상세 헤더는 `🎮 게임 출시 캘린더` h1만(build.js L51), 본문 카드는 `max-width:700px; margin:0 auto` 중앙정렬(.game-detail) → 메인 앱이 최근 '좌측정렬+컴팩트화'(운영자 요청, 개발자 05-29)로 굳힌 정체성과 어긋나고, 헤더는 좌측인데 카드만 중앙이라 페이지 내부 정렬도 불일치. (b) 날짜 포맷이 `2026년 6월 3일`(한글 long·요일 없음)인데 메인은 전부 `2026.06.03 (수)`(점+요일). 같은 데이터가 페이지마다 다른 포맷.
   - 왜 문제: SEO 페이지는 검색 유입자의 첫인상인데 헤더 스타일·정렬·날짜 표기가 메인과 달라 '같은 서비스'라는 연속성/신뢰가 깨짐. 날짜 포맷 2종은 일관성·기억성 저하.
   - 어떻게(개발자): build.js 헤더/카드 정렬을 메인 톤과 통일(좌측정렬 컨테이너 또는 최소 헤더-카드 정렬 일치), 날짜를 메인의 `YYYY.MM.DD (요일)` 헬퍼와 동일 포맷으로(요일 포함). 빌드 템플릿 소규모, 신규 색 없음.

3. **[a11y·이중인코딩 누락] 색+모양 이중인코딩이 캘린더 셀·범례엔 적용됐으나 '날짜 클릭 패널' 행 점(.day-row-dot)엔 미적용 — 정작 게임명을 읽는 목록에서 색각이상 구분 단서가 사라짐** — 우선순위: 보통
   - 소스: 캘린더 `.day-dot`·범례 `.legend-dot`는 색+모양(원/사각/마름모/링, styles.css 362~365·451~454)인데, 날짜 패널 행 `.day-row-dot`(605~610)은 4색 모두 `border-radius:50%` 단일 원형(색만). 리스트 카드는 텍스트 카테고리 pill이라 무관하나, 날짜 패널은 점+게임명 구조라 카테고리 식별이 점 색에만 의존.
   - 왜 문제: 색각이상 사용자가 캘린더 그리드(셀)에선 모양으로 카테고리를 구분하다가, 막상 게임 목록을 읽는 날짜 패널에선 단서를 잃음(같은 사이트 내 a11y 커버리지 비일관). 직전 '점 색+모양 이중인코딩'(개발자 05-30)이 .day-dot/.legend-dot만 손대고 .day-row-dot은 누락한 잔여 건.
   - 어떻게(개발자): `.day-row-dot`에 `.day-dot`과 동일한 per-category 모양 규칙(pc_console=사각/global=마름모/new_server=링) 부여(styles.css 4줄). 행 dot에도 `title`/`aria-label`로 카테고리명 부여하면 SR 보조까지 일관. 크기(8px)만 유지, 신규 색 없음.

4. **[정보중복·SEO페이지] 개발사==배급사 동일 게임이 상세 페이지에서도 '개발사 X'/'배급사 X' 2줄 중복 (기존 dedup 제안 범위를 SEO 페이지까지 확장)** — 우선순위: 낮음
   - 실측/소스: ff7 상세에서 `개발사: Square Enix` + `배급사: Square Enix` 동일 값 2행(build.js L87~88, 둘 다 무조건 출력). 자체개발·자체배급 게임에서 반복. (기존 IDEAS '개발==퍼블리셔 병합'은 모달·리스트 카드만 → SEO 페이지는 세 번째 표면.)
   - 왜 문제: 같은 회사명 인접 2줄 중복으로 정보 밀도 낭비 + 모달/카드에 dedup 적용 시 페이지마다 처리 불일치 위험.
   - 어떻게: developer===publisher면 build.js·모달·카드 공통으로 '개발·배급 X' 한 줄 병합, 다르면 2행 유지(세 표면 동일 규칙). 빌드 템플릿 분기, 신규 색 없음.

### 현재 양호 (트집 X)
오늘(30) 셀 파란 보더+'오늘' 라벨·D-DAY(빨강) 정상, 선택 셀(28) 채움+inset 링↔오늘 보더 위계 분리 라이브 확인, 날짜패널 행 마우스 클릭→모달 정상, sticky 날짜 그룹 헤더, dot 색+모양 이중인코딩(셀/범례) 적용, 모달 페이드·링크복사·트레일러·출처·전체페이지 액션, 통계줄 32=드롭다운 32 일치, 푸터 운영자 정보(© 2026·contact@gcalen.com). SEO 상세 페이지 자체는 200·구조 클린(전체목록/공식출처 링크 동작). 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-31 16:50] [디자이너] — 라이브 실측: 모달 메타 라벨 정렬·개발=퍼블리셔 중복·approx날짜 그룹헤더 확정요일
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1516px). 캘린더(5월, 오늘=30 하이라이트)·리스트·날짜패널(30일 클릭 '이후 26건')·상세모달(파판7리버스) 정상 렌더, 콘솔 에러 0건. 배포 32건 라이브. 날짜패널 행 마우스 클릭→모달 정상(직전 fix 라이브 재확인), dot 색+모양 이중인코딩 적용 확인. 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일)→소스 CSS 병행. 기존 TODO 큐/IDEAS 대기 항목(모달 배너 카테고리 텍스트 중복·모달 × 저대비·캘린더 그리드 SR 시맨틱·요일헤더 #888 대비·주말 날짜숫자 색·이벤트셀 면강조·'오늘' 라벨 amber→accent·1건날짜 흡수·터치타겟44px·위시 --wish 토큰·전역 focus-visible·바로가기 칩·범례↔통계 통합·date_tbd 미정게임 D-day·D-day 근접 색단계·SVG아이콘/폰트/라운드 토큰·패널 플랫폼 1개만·'오늘로' 정렬 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[정렬·스캔성·모달] 상세 모달 메타 행 라벨 폭이 제각각이라 값(value) 시작점이 줄마다 들쭉날쭉** — 우선순위: 보통
   - 실측/소스: 모달 메타는 `<div class="modal-row"><strong>라벨</strong>값` 구조(script.js L404~408). 라벨이 출시일(3)·플랫폼(3)·장르(2)·개발(2)·퍼블리셔(4)로 글자수가 달라 값('2026.06.03'·'Switch 2'·'RPG, 액션'·'Square Enix' 등)의 좌측 시작 x가 행마다 어긋남(파판7 모달 zoom 실측 확인). `.modal-row strong`은 `color:#888; margin-right:0.4rem`만 있고 폭 고정 없음(styles.css L335).
   - 왜 문제: 라벨-값 표가 좌측 정렬 컬럼을 못 이뤄 시선이 값을 찾을 때 매 행 좌측 들여쓰기가 달라 스캔 비용↑. '정보 카드'다운 정돈감 저하. (덤: `strong` 색 #888은 요일헤더와 같은 AA경계 톤 — 기존 #888 항목과 함께 톤업하면 시너지, 단 그건 기등록이라 여기선 정렬만.)
   - 어떻게: `.modal-row strong{display:inline-block; min-width:4.5em; color:...}` (또는 modal-row를 `display:grid;grid-template-columns:4.5em 1fr;gap`)로 라벨 폭을 고정해 값이 한 컬럼으로 정렬. styles.css 1~2줄, 외형 톤 무변(레이아웃만). 리스트 카드 메타(🛠️/🏢 행)는 별구조라 영향 없음.

2. **[정보중복] 개발사와 퍼블리셔가 동일한 게임은 같은 값이 두 줄로 반복 노출 ('개발 Square Enix' / '퍼블리셔 Square Enix')** — 우선순위: 낮음
   - 실측: 파판7 리버스 모달이 `개발 Square Enix` + `퍼블리셔 Square Enix` 동일 값 2행(자체개발·자체배급 게임 다수에서 반복). 소스 script.js 모달 L407~408, 리스트 카드 L300~301(🛠️/🏢)도 동일하게 둘 다 무조건 출력.
   - 왜 문제: 같은 회사명이 인접 두 줄로 중복돼 정보 밀도 낭비 + '개발≠퍼블리셔'인 게임과의 시각 패턴 불일치. 클린 미니멀 방향과도 어긋남.
   - 어떻게: developer===publisher일 때 두 행을 `개발·퍼블리셔  Square Enix` 한 줄로 합치고, 다를 때만 분리 2행 유지(모달·리스트 카드 동일 규칙). script.js 소규모 분기, 신규 색/외형 없음.

3. **[일관성·정확성] 'approx(예정)' 날짜 게임이 리스트/날짜패널 그룹 헤더에선 확정 요일 '(월)'로 표기 — 모달은 요일 숨기고 카드 본문은 '(예정)'이라 표기가 3중으로 엇갈림** — 우선순위: 보통
   - 실측: '포켓몬 챔피언스(모바일)'는 카드 날짜줄 `2026.06.15 (예정)`인데, 그 위 그룹 헤더는 `2026.06.15 (월)`(확정 요일). 한편 상세 모달은 approx면 요일을 아예 생략(소스 L404 `release_date_approx ? '' : 요일`). 즉 같은 게임의 날짜 표기가 그룹헤더='(월)' / 카드='(예정)' / 모달='요일 없음'으로 세 곳이 제각각.
   - 왜 문제: 그룹 헤더가 approx 날짜에 확정 요일을 붙여 '6/15 월요일 확정'이라는 가짜 확정감을 줌(모달은 이미 올바르게 회피). 같은 데이터의 날짜 신뢰도가 화면 위치마다 달라 일관성 저하. (※기등록 date_tbd 항목은 '날짜 완전미정(2분기 예정)' 게임의 D-day 가짜정밀도·12/31 그룹 분리 건으로, 이 건은 '날짜는 있으나 approx 플래그' 게임의 그룹헤더 요일 표기 불일치로 별개.)
   - 어떻게: renderGroupedList(L186)·renderDayRows의 그룹 헤더 요일 출력을 모달과 동일 규칙으로 — `release_date_approx`면 요일 생략(또는 '(예정)' 부기)으로 통일. 헤더 함수 1곳 분기, 신규 색/외형 없음.

### 현재 양호 (트집 X)
오늘(30) 셀 파란 보더+채움 하이라이트·D-DAY 정상, 날짜패널 행 마우스 클릭→모달 정상(직전 fix 라이브 ✅), dot 색+모양 이중인코딩(신규서버=링 등) 적용 확인, 선택 셀 inset 링, sticky 날짜 그룹 헤더, 모달 페이드·트레일러/링크복사/출처/전체페이지 액션, 통계줄 32=드롭다운 32(기본상태) 일치, 푸터 운영자 정보(© 2026·contact@gcalen.com·갱신시각) 정상. 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-31 12:50] [디자이너] — 라이브 실측: 모달 닫기(×) 저대비 + 캘린더 그리드 SR 시맨틱 + 패널 플랫폼 1개만 + 헤더 '오늘로' 정렬
실측: https://gcalen.com/ Chrome 데스크톱(1280px). 캘린더(5월/6월)·리스트·날짜패널·상세모달 정상 렌더, 콘솔 에러 0건. 배포 games.json 32건 라이브. 6월 밀집(3~5 임박 amber 보더 정상), 5월은 30일만 출시라 그리드 휑함(기존 '이벤트 셀 면강조' 큐로 커버). 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일)→CSS 병행. 기존 TODO/IDEAS 대기 항목(모달 배너 카테고리 텍스트 중복·1건날짜 흡수·이벤트셀 면강조·'오늘' 라벨 amber→accent·주말 날짜숫자 색·요일헤더 #888 대비·터치타겟 44px·위시 별 --wish 토큰·전역 focus-visible·바로가기 칩·범례↔통계 통합·date_tbd·D-day 근접 색단계·SVG/폰트/토큰 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[a11y·대비·모달] 상세 모달 닫기(×)가 밝은 카테고리 배너 위에서 거의 안 보임** — 우선순위: 보통
   - 실측: 이미지 없는 게임 상세 모달 160px 컬러 배너 우상단의 ×가 `.modal-close{color:#aaa}`. 국내모바일(녹색 #81c784)·글로벌(보라 #ba68c8) 등 밝은 그라데이션 위에서 옅은 회색 ×는 대비가 매우 낮아 zoom 실측에서도 흐릿(녹색 배너 위 식별 어려움). 배너에 이미지가 들어오면 더 묻힘.
   - 왜 문제: 모달의 유일한 명시적 닫기 컨트롤이 배너 색/이미지에 따라 보였다 안 보였다 함(ESC·배경클릭이 있어도 발견성↓). 색 대비 WCAG 미달 소지.
   - 어떻게: ×에 배너색 무관 고정 대비 — 반투명 다크 원형 배경(`background:rgba(0,0,0,0.45); border-radius:50%; width/height:32px; inline-flex center`) + 흰색 아이콘. 동시에 44px 히트영역 확보(터치타겟 항목과 시너지). styles.css `.modal-close` 소규모, 신규 색 없이 다크 rgba만.

2. **[a11y] 월간 캘린더 그리드에 표/그리드 시맨틱·요일 헤더 연결이 없어 스크린리더가 맥락 없이 읽음** — 우선순위: 보통
   - 어디서: `.calendar-grid`는 일반 div 그리드, 요일 헤더 `.weekday`(일~토)도 일반 div 텍스트. 출시 있는 셀만 `role="button"`+aria-label('·월 ·일, 출시 N건')이고 빈 셀·요일 헤더는 시맨틱 없음. SR 사용자는 '무슨 요일 열/주 구조'인지 없이 버튼 나열로만 들림.
   - 왜 문제: 캘린더 핵심 구조(요일×주)가 보조기술에 전달 안 됨(WCAG 1.3.1). 셀 aria-label에 날짜는 있으나 요일 맥락·그리드 관계 부재.
   - 어떻게: `.calendar-grid` role="grid"+aria-label('YYYY년 M월 출시 캘린더'), 요일 행 role="row"·`.weekday` role="columnheader"(또는 aria-hidden 처리하고 셀 aria-label에 요일 포함), 주 단위 role="row"·셀 role="gridcell". 기존 셀 aria-label 유지·확장. 외형 무변, JS 속성 부여 위주.

3. **[정확성·정보] 날짜 패널 행이 멀티플랫폼 게임도 대표 플랫폼 1개만 노출 → 단일 플랫폼처럼 오인** — 우선순위: 낮음
   - 실측: 6/4 '미르의 전설: 진' 패널 행은 'Android'만 뜨나 모달은 'Android, iOS'. PC/PS5 동시발매도 행엔 1개만 떠, 패널만 보는 사용자는 멀티플랫폼을 단일로 오해.
   - 왜 문제: 플랫폼은 '내 기기로 나오나'를 빠르게 판단하는 핵심 메타인데 행에서 누락분이 안 보임. 정보 충실도 저하.
   - 어떻게: renderDayRows에서 플랫폼을 최대 2개+'+N'(또는 약식 묶음)으로, 전체는 모달에서. 공간 부족 시 콤마/약식 코드. script.js renderDayRows 소규모.

4. **[정렬·심미] 캘린더 헤더 '오늘로' 버튼이 중앙 묶음 끝에 붙어 월 라벨이 중앙에서 왼쪽으로 치우쳐 보임** — 우선순위: 낮음
   - 어디서: `.calendar-header{justify-content:center}`에 ‹·월라벨·›·오늘로가 한 묶음 중앙정렬 → '오늘로'가 우측에 더해져 묶음이 오른쪽으로 늘어나고 '2026년 6월'이 시각 중앙보다 왼쪽에 위치(스크린샷 확인).
   - 왜 문제: 월 네비의 시선 기준(월 라벨)이 중앙에 안 맞아 정돈감 저하. 좌측정렬 헤더 리프레시와도 미묘하게 어긋남.
   - 어떻게: ‹ 월라벨 ›를 하나의 중앙 그룹으로 두고 '오늘로'만 `margin-left:auto`(또는 absolute right)로 우측 분리 → 월 네비는 진짜 중앙, 오늘로는 우측 보조. styles.css 소규모.

### 현재 양호 (트집 X)
날짜패널 행 마우스 클릭→모달 정상(직전 fix 라이브 확인), D-day 임박(D-5/D-6 등 ≤7) amber·D-DAY 표기 정상, 선택 셀 채움+inset 링, 6월 임박셀 amber 보더, sticky 날짜 헤더, 모달 페이드, 위시 ★ 토글, 통계줄 32=드롭다운 32 일치, 푸터 운영자 정보 정상. 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-31 08:50] [디자이너] — 라이브 실측: 모달 카테고리 텍스트 중복 + 주말 숫자/요일헤더 색 불일치 + 헤더 저대비 + 셀 dot 빈공간
실측: https://gcalen.com/ Chrome 데스크톱(실렌더 1920px). 캘린더/리스트/날짜패널/상세모달 정상 렌더, 콘솔 에러 0건. 배포 games.json 32건 라이브. 직전 1순위(날짜패널 .day-row 마우스 클릭 모달 미동작)는 개발자 fix(커밋 454b2a6) 반영 확인 — 리스트/패널 행 마우스 클릭 시 상세 모달 정상 오픈. 모바일은 창 리사이즈가 실렌더 뷰포트에 미반영(기존과 동일) -> styles.css 병행 평가. 기존 TODO 큐/IDEAS 대기 항목(dot 색+모양 이중인코딩·1건날짜 흡수·이벤트셀 면강조·'오늘' 라벨 amber->accent·위시 별 --wish 토큰·전역 focus-visible·터치타겟 44px·헤더/푸터 갱신표기 중복·바로가기 칩 어포던스·범례<->통계 통합·클린미니멀 폰트/SVG/토큰·D-day 근접도 색단계·date_tbd 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[중복·모달] 상세 모달 상단 컬러 배너 안의 카테고리 텍스트 + 바로 아래 카테고리 pill이 같은 문구를 두 번 표시** — 우선순위: 보통
   - 어디서/실측: 이미지 없는 게임 상세 모달에서 상단 .modal-image(160px 컬러 그라데이션 배너) 한가운데에 카테고리명(예 "한국 MMO 신규 서버")이 텍스트로 찍히고, 그 바로 아래 본문 시작점에 동일한 카테고리 pill이 또 나옴 -> 같은 카테고리명이 약 40px 간격으로 2회 중복.
   - 원인(개발도메인): 모달이 리스트 카드와 같은 .card-image-placeholder(span 텍스트 포함, styles.css L483~488)를 .modal-image 안에 재사용. 리스트 카드는 16:17 작업에서 배너 텍스트를 제거하고 4px 컬러바(.card-banner, L489 주석 'dup removed')로 콤팩트화했는데, 모달 쪽 placeholder에는 같은 dedup이 적용 안 됨.
   - 왜 문제: 동일 정보 중복으로 시선 낭비 + 리스트 카드(텍스트 없는 컬러바)와 모달(텍스트 있는 큰 배너)의 처리 방식이 불일치. 클린 미니멀 방향과도 어긋남.
   - 어떻게(개발자): 모달 placeholder의 카테고리 텍스트 span을 제거(컬러 그라데이션만 유지)하고 카테고리는 아래 pill로만 1회 표기. 또는 배너에 텍스트를 남기려면 본문 pill을 제거. 이미지 있는 게임은 영향 없음. styles.css/script.js 소규모, 신규 색 없음.

2. **[일관성·스캔성] 캘린더 셀 안 '날짜 숫자' 색이 요일 헤더의 주말 색과 불일치 (헤더만 빨강/파랑, 셀 숫자는 전부 회색)** — 우선순위: 낮음
   - 어디서/실측: 요일 헤더는 한국식으로 일=빨강(rgb 229,115,115)·토=파랑(rgb 100,181,246)·평일=회색(#888)인데(styles.css L499~500), 정작 셀 안의 날짜 숫자(.day의 텍스트 노드, color:#ccc, L353)는 일·토 컬럼에서도 모두 동일 회색. 즉 주말 색 구분이 맨 윗줄 헤더에만 있고 본문 31칸에는 없음.
   - 왜 문제: 주말(특히 출시가 몰리는 금~일)을 한눈에 스캔하려면 헤더 한 줄로 컬럼을 역추적해야 함. 한국 캘린더 관용(일=빨강/토=파랑)이 헤더에만 적용돼 어중간.
   - 어떻게: 일요일 칸 날짜 숫자에 옅은 red 톤, 토요일 칸에 옅은 blue 톤을 적용(헤더와 같은 토큰, 다른 달 칸 .day[data-other]는 기존처럼 더 흐리게 유지). nth 기반 또는 렌더 시 요일 클래스 부여. styles.css 중심 소규모, 신규 색 없이 헤더 2색 재사용.

3. **[a11y·대비] 요일 헤더 '평일(월~금)' 색 #888이 다크 배경 대비 낮음 (WCAG AA 경계)** — 우선순위: 보통
   - 어디서: .calendar-grid .weekday { color:#888 }(L352). #888은 다크 배경 대비 약 3:1대로 일반 텍스트 AA(4.5:1) 미달 소지. 본문/메타 텍스트는 05-29 사이클에 대비 상향(#9aa0ac 등)했으나 요일 헤더는 그 작업에서 누락.
   - 왜 문제: 캘린더의 기준선(요일 라벨)이 가장 흐린 텍스트 중 하나라 저시력/저대비 환경에서 읽기 어려움. 일·토는 색이 있어 그나마 낫지만 평일 5칸이 약함.
   - 어떻게: 평일 요일 헤더 색을 토큰 --text-dim(#9aa0ac급) 또는 그 이상으로 상향. 일/토 색은 유지. styles.css 1줄, 신규 색 없이 기존 토큰 재사용.

4. **[밀도·시각] 출시 1건뿐인 캘린더 셀에서 게임명 라벨과 점(dot) 사이에 큰 빈 띠 (84px 셀 상단=라벨, 하단=점, 가운데 공백)** — 우선순위: 낮음
   - 어디서/실측: .day는 min-height 84px·flex-column, 날짜숫자+.day-game-label은 상단(margin-top:4px, L367), .day-dots는 margin-top:auto로 셀 바닥에 pin(L360). 1건짜리 날(예 30·28)은 라벨 바로 아래 점이 와야 자연스러운데 점이 셀 맨 아래로 밀려 라벨과 점이 시각적으로 분리·떠 보임.
   - 왜 문제: 라벨과 그 카테고리 점이 한 묶음으로 안 읽히고(중간 빈 공간), 셀이 듬성해 보임. 콘텐츠가 상·하로 쪼개져 스캔 시 연결 인지 비용.
   - 어떻게: .day-dots의 margin-top:auto를 제거(또는 작은 고정 간격)해 점을 라벨 바로 아래로 붙여 '날짜->라벨->점' 한 클러스터로. 다건일 때 점 줄바꿈은 기존 flex-wrap 유지. styles.css 1줄.

### 현재 양호 (트집 X)
날짜패널/리스트 행 마우스·키보드 클릭 시 상세 모달 정상 오픈(직전 버그 fix 확인), D-day 'D-DAY'/'D-4' 표기 정상, 선택 셀 채움+inset 링 위계, sticky 날짜 그룹 헤더, 모달 페이드, 트레일러 검색·링크 복사·전체 페이지 액션, 통계줄 32=드롭다운 32 일치, 푸터 운영자 정보 정상. 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.
※[리서처 도메인 참고] 상세 데이터 오류 2건 발견(CHAT 보고): '패스 오브 엑자일 2'가 카테고리 '한국 MMO 신규 서버'로 오분류(글로벌 ARPG), 같은 게임 '개발'사 필드가 게임명으로 잘못 채워짐(퍼블리셔는 그라인딩기어게임즈로 정상). 디자인 아님 -> 리서처 확인 요청.

---

## [2026-05-31 04:50] [디자이너] — 라이브 실측: 날짜패널 행 클릭 모달 미동작(버그) + 키보드포커스/터치타겟 a11y + 갱신표기 중복
실측: https://gcalen.com/ Chrome 데스크톱(1280→실렌더 1920px). 캘린더/리스트/날짜 클릭 패널 정상 렌더, 콘솔 에러 0건. D-day KST off-by-one 수정(개발자 04:20) 라이브 반영 확인 — 오늘(30) 출시 PoE2가 'D-DAY'로 정상 표시. SEO 바로가기 5링크(/upcoming-games 등) 전부 HTTP 200(깨진 링크 아님). 모바일은 창 리사이즈가 실렌더 뷰포트(innerWidth 1920 고정)에 미반영(기존과 동일) → CSS 병행 평가. 직전 사이클 발견·큐 대기 항목(dot 색+모양 이중인코딩·1건날짜 흡수·셀 면강조·'오늘' 라벨 색통일·위시 별 색 토큰·전역 focus-visible·바로가기 칩 어포던스 등)은 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[버그·correctness] 캘린더 '날짜 클릭 패널'의 게임 행(.day-row)을 마우스로 클릭해도 상세 모달이 안 열림 (키보드 Enter는 열림 → 마우스/터치만 깨짐)** — 우선순위: 높음
   - 실측/재현: 캘린더에서 날짜 셀 클릭 → 패널 오픈 → 패널 안 게임 행을 마우스로 클릭 시 아무 일도 안 일어남. DOM 검증: `document.querySelector('#day-detail-panel .day-row').click()` 호출해도 `.modal-overlay.hidden`이 계속 true(모달 안 열림). 반면 같은 행에 포커스 후 Enter(keydown)는 모달 정상 오픈(modal hidden:false). 즉 키보드는 되는데 마우스/터치 클릭만 죽어 있음.
   - 원인(개발도메인): `.day-row`의 openModal 처리가 **`gamesList`(#games-list, 리스트 뷰 컨테이너) click 리스너** 안에 들어가 있음(script.js ≈L480 `const row=e.target.closest('.day-row, .game-card'); if(row&&row.dataset.id) openModal(...)`). 그런데 `.day-row`는 **`#day-detail-panel`** 안에 렌더되어 gamesList의 자손이 아니므로 그 리스너로 이벤트가 도달하지 못함. 한편 `dayPanel` click 리스너(≈L724)는 `.day-panel-close`·`.wishlist-btn`·`.game-card`만 처리하고 `.day-row`는 빠져 있음. 키보드는 dayPanel keydown 리스너(≈L484)가 `.day-row`를 직접 처리해서 정상.
   - 왜 문제: 캘린더 뷰의 **핵심 인터랙션(날짜→게임 상세)**이 마우스·터치 사용자(대다수)에게 동작 안 함. 게다가 `.day-row`는 `cursor:pointer`+hover 배경 변화(styles.css L602·604)로 '클릭 가능'을 명확히 광고하는데 눌러도 반응이 없어 '고장난 사이트'로 인식될 위험. 키보드만 되는 비대칭이라 일반 QA의 패널-오픈 확인에선 놓치기 쉬움.
   - 어떻게(개발자): dayPanel click 리스너의 `.game-card` 분기를 `closest('.day-row, .game-card')`로 확장하거나, 별도 `.day-row` 분기 추가(키보드 핸들러와 동일하게 `openModal(row.dataset.id)`). gamesList 쪽 셀렉터에서 `.day-row`는 빼도 됨(거긴 day-row가 없음). QA에 '캘린더 날짜 패널 행을 **마우스로** 클릭 시 모달 오픈' 회귀 항목 추가 요청.

2. **[a11y] 날짜패널 행(.day-row) 키보드 포커스 표시가 hover와 동일 배경뿐 + outline 제거로 식별이 약함** — 우선순위: 보통
   - 어디서: `.day-row:hover, .day-row:focus-visible { background:var(--surface); outline:none; }`(styles.css L604). 호버와 키보드 포커스가 **완전히 같은 시각(옅은 surface 배경)**이고 outline은 명시적으로 제거됨. 패널 배경(--bg)과 surface 차이가 미묘해 Tab으로 행을 훑을 때 '지금 어디 포커스인지'가 잘 안 보임.
   - 왜 문제: 패널의 1차 콘텐츠(게임 목록)에서 키보드 포커스 가시성이 약함(WCAG 2.4.7 소지). #1 수정으로 마우스 클릭이 살아나면 키보드 동선도 같이 쓰일 텐데, 포커스 단서가 hover와 구분 안 되면 키보드 사용자가 현재 위치를 놓침.
   - 어떻게: `.day-row:focus-visible`에 `outline:2px solid var(--accent); outline-offset:-2px`(또는 inset box-shadow 링)를 부여해 hover(배경)와 포커스(링)를 시각적으로 분리. 캘린더 셀(.day[role=button]:focus-visible)이 이미 쓰는 동일 토큰/패턴 재사용.

3. **[a11y·모바일] 아이콘 버튼 터치 타겟이 44px 미만 — 특히 위시 별(★)** — 우선순위: 보통
   - 어디서: `.day-row .wishlist-btn`(padding:0 0.2rem; font:1rem → 실측 탭 영역 ≈16~20px, L615), `.wishlist-btn`(카드/모달 padding:0 0.15rem; font:1.15rem, L434), `.modal-close`(×, font 1.8rem이나 padding 0.25/0.5rem). 모두 WCAG 2.5.5 권장 44×44px(및 모바일 HIG 44pt)보다 작음.
   - 왜 문제: 모바일에서 위시 별·닫기(×)를 정확히 누르기 어렵고, 행에서 별과 행 본문(모달 열기)이 붙어 있어 오탭 위험(별 누르려다 행 열림/반대). 터치 정확도·접근성 저하.
   - 어떻게: 인터랙티브 아이콘 버튼에 `min-width:44px; min-height:44px`(또는 padding 확대)로 히트 영역 확보(아이콘 자체 크기는 유지, 클릭 영역만 확장 — `display:inline-flex;align-items:center;justify-content:center`). 행 내 별은 본문 클릭 영역과 충분히 분리. 데스크톱 외형은 거의 그대로, 모바일 탭 정확도만 개선.

4. **[일관성] '마지막 업데이트' 정보가 헤더·푸터 2곳에 서로 다른 라벨·날짜 포맷으로 중복** — 우선순위: 낮음
   - 어디서: 헤더 `.last-updated` "**마지막 업데이트: 2026.05.30**"(점 구분, 날짜만) vs 푸터 "**데이터 마지막 갱신: 2026-05-30 09:10 (방금 전)**"(대시 구분, 시각+상대시간 포함). 같은 데이터 갱신 시점을 두 라벨·두 포맷으로 표기.
   - 왜 문제: 동일 정보 중복 + 포맷/라벨 불일치(점 vs 대시, '업데이트' vs '갱신', 날짜만 vs 시각포함)로 일관성·신뢰감 저하. 헤더 좌측정렬 컴팩트화(첫 화면에 캘린더 올리기) 취지와도 어긋나는 군더더기 한 줄.
   - 어떻게: 둘 중 하나로 수렴 권장 — 푸터가 더 풍부(시각+상대시간)하므로 **헤더의 `.last-updated` 줄을 제거**해 상단을 더 컴팩트하게(캘린더 상향), 또는 라벨·날짜 포맷을 한 쪽 표준으로 통일. SEO용으로 헤더 표기가 꼭 필요하면 최소한 포맷·라벨만 푸터와 일치시키기.

### 현재 양호 (트집 X)
D-day KST off-by-one 수정 라이브 정상(오늘 출시=D-DAY, 캘린더 '오늘' 셀과 일치), 날짜 패널 auto-scroll+헤더 플래시, sticky 날짜 그룹 헤더, 선택 셀 채움+inset 링 위계, 컴팩트 행 레이아웃, 위시 ★/☆ 토글, 통계줄 32=드롭다운 32 일치, SEO 바로가기 5링크 200 정상, 푸터 운영자 정보 모두 정상. 기존 큐/IDEAS 대기 항목은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-31 00:50] [디자이너] — 라이브 실측: D-day 타임존 오프셋(버그) + 위시/포커스/모션 a11y
실측: https://gcalen.com/ Chrome 데스크톱(1280px) 캘린더·리스트·날짜 클릭 패널 + 페이지 내 JS(`new Date`) 검사. 콘솔 에러 0건. 배포 games.json 32건 라이브. 모바일은 창 리사이즈가 렌더 뷰포트에 미반영(기존과 동일) → styles.css 병행. 직전 사이클(20:50/12:50) 발견 항목은 TODO/IDEAS 큐에 있어 중복 등록 안 함.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **[버그·개발도메인] D-day가 KST에서 전 항목 하루씩 밀림 (오늘 출시가 'D-DAY' 대신 'D-1')** — 우선순위: 높음
   - 실측/재현: 캘린더는 30일을 '오늘'로 하이라이트(로컬 날짜)하는데, 같은 30일 출시 '패스 오브 엑자일 2'가 리스트·패널에서 **D-1**로 표시됨. 페이지 JS 확인 결과 `new Date('2026-05-30')`는 **UTC 자정**으로 파싱되나 `today=new Date(); setHours(0,0,0,0)`는 **로컬(KST) 자정** → 두 값 차이에 +9h가 끼어 `Math.ceil`에서 같은 날 출시가 D-1, 내일이 D-2…로 전부 +1. 결과적으로 KST 사용자에겐 'D-DAY' 라벨이 사실상 안 뜨고, '오늘 셀(30)' 하이라이트와 'D-1' 표기가 모순.
   - 왜 문제: 캘린더의 핵심 가치(오늘/임박)가 하루 어긋남. '오늘 출시'인데 D-1로 보여 정확성·신뢰도 훼손. 임박(.day-soon 7일) 판정에도 같은 +1 영향 가능.
   - 어떻게(개발자): release_date 파싱을 로컬 기준으로 통일 — 이미 `formatDate`가 쓰는 `new Date(dateStr+'T00:00:00')` 패턴으로 diff 계산부(script.js 라인 ≈205·261·386, 임박판정 71-73·132)도 교체하거나, today도 UTC 자정으로 맞춰 양쪽 기준을 통일. 도메인상 코드 수정은 개발자 — QA에 KST 자정 전후 D-day 회귀 확인 요청.

2. **[색일관·a11y] 위시리스트 '활성 별' 색이 표면마다 다르고, 날짜 패널에선 '임박'색과 충돌** — 우선순위: 보통
   - 어디서: `.wishlist-btn.active`·`.modal-wishlist-btn.active` = **#f5b400(골드)**, `.day-row .wishlist-btn.active` = **#f5a623(앰버)**. 같은 '위시 활성' 상태가 카드/모달과 날짜 패널에서 서로 다른 색.
   - 왜 문제: (1) 동일 상태가 2색 → 일관성 저하. (2) 날짜 패널의 위시 별(#f5a623)이 '임박' 색(`.day-row-dday.soon`·`.dday.soon` = #f5a623)과 **완전히 동일**해, 한 행에서 '위시한 게임'과 '임박 D-day'가 같은 색으로 뭉개짐(색-의미 충돌).
   - 어떻게: 위시 활성 색을 한 토큰(예 `--wish:#f5b400`)으로 카드/모달/패널 전부 통일하고, 임박 amber(#f5a623)와 계열·명도를 분리(위시=골드, 임박=오렌지). 신규 색 추가가 아니라 기존 두 값 중 하나로 수렴.

3. **[a11y] 키보드 포커스 표시(:focus-visible)가 대부분 컨트롤에 없음** — 우선순위: 보통
   - 어디서: 현재 포커스 스타일은 캘린더 게임 셀(`.day[role=button]`)과 검색 input뿐. 뷰토글(.view-toggle-btn)·퀵칩(.chip-btn)·필터 select·위시 별(.wishlist-btn)·클릭형 카테고리/플랫폼 태그·바로가기/푸터 링크·모달 닫기(×)·캘린더 네비(‹ › 오늘로)·패널 닫기(×)는 :hover만 있고 포커스 표시 없음.
   - 왜 문제: 키보드 사용자가 Tab으로 이동할 때 '지금 어디에 포커스가 있는지' 시각 단서가 없음(WCAG 2.4.7 소지). 직전에 캘린더 셀 키보드 접근은 추가됐으나 나머지 컨트롤은 여전히 사각.
   - 어떻게: 전역 규칙으로 인터랙티브 요소에 `:focus-visible{outline:2px solid var(--accent);outline-offset:2px}` 일괄 적용(캘린더 셀과 동일 토큰). 추가로 클릭형 `.category-tag`/`.platform-tag`는 span이 아니라 button 시맨틱(또는 role=button+tabindex+키핸들러)으로 키보드 활성 가능하게.

4. **[a11y·모션] '오늘' D-day 펄스(@keyframes pulse)에 prefers-reduced-motion 가드 없음** — 우선순위: 낮음
   - 어디서: `.dday.today { animation: pulse 1.5s infinite }`(opacity 깜빡임 무한). 모달 페이드·카드 hover pulse-dday·패널 플래시는 모두 reduced-motion 가드가 있는데 이 펄스만 누락.
   - 왜 문제: 모션 민감 사용자에게 무한 깜빡임 노출(WCAG 2.3.3). (현재는 #1 버그로 KST에서 'D-DAY'가 거의 안 떠 펄스도 잘 안 보이나, #1 수정 후엔 매일 노출되므로 함께 처리 권장.)
   - 어떻게: `@media (prefers-reduced-motion: reduce){ .dday.today{ animation:none } }` 한 줄 추가.

### 현재 양호 (트집 X)
지난 날짜 셀 클릭 패널 헤더↔목록 일치(개발자 16:30 fix·QA ✅) 재확인, 캘린더 게임 셀 키보드 접근(role/tabindex/aria/Enter·Space/focus-visible, 개발자 21:20) 라이브 정상, 선택 셀 채움+inset링·sticky 날짜 헤더·컴팩트 날짜 행·모달 페이드·푸터 운영자 정보 모두 정상. 기존 미반영 '높음'/큐 항목(점 색+모양 이중인코딩·1건 날짜 흡수·이벤트 셀 면 강조·오늘 라벨 amber→accent·클린미니멀 폰트/SVG/토큰·바로가기 칩·범례-통계 통합)은 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-30 12:50] [디자이너] — 라이브 실측: 구현분 검증 + 지난날짜 패널/셀 스캔/명칭 일관성
실측: https://gcalen.com/ Chrome 데스크톱(1280px). 캘린더/리스트/날짜 클릭 패널 정상 렌더, 콘솔 에러 0건. 배포 games.json 32건(리서처 09:10 신규 3종 반영) 라이브 확인. 모바일은 창 리사이즈가 캡처에 미반영되어 CSS 병행(다음 사이클 재시도).

### 직전 사이클 이후 반영 확인 (개발자 작업 검증 — 모두 양호)
- OK **선택 셀 위계 분리**(00:20): 27일 클릭 시 amber 보더가 아니라 파란 채움 + inset 링으로 표시, 오늘(30)=파란 외곽보더와 구분됨. 18:05 #1 보더 충돌 해소 확인.
- OK **날짜 그룹 헤더 sticky**(00:30): 리스트/날짜 패널 스크롤 시 날짜 헤더 상단 고정, 뒤 카드 비침 없음. 13:02 #1 해소 확인.
- OK 위시리스트 ★(amber 채움)/☆ 토글, 날짜 패널 컴팩트 인라인 행, 푸터 운영자 정보 모두 정상.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **지난 날짜 셀 클릭 시 패널 헤더("N일 이후")와 실제 내용(오늘 이후)이 불일치 + 그 날 게임 누락** — 우선순위: 보통
   - 어디서/재현: 캘린더에서 27일 셀(셀에 "007 퍼스트 라이트" 라벨 표시됨) 클릭 → 패널 헤더 "2026.05.27 (수) 이후 출시 26건"인데 목록 첫 항목이 **2026.05.30**부터 시작. 27일의 그 게임(007 퍼스트 라이트)은 목록에 없음. 28일도 동일(셀엔 벨로 레볼루션 표시, 패널은 30부터).
   - 원인 추정: 패널이 기간필터('앞으로 1년'=오늘 이후)를 적용해 과거 출시를 제외. 그래서 헤더는 "클릭일(27) 이후"라고 적히지만 실제 내용은 "오늘(30) 이후"가 됨.
   - 왜 문제: (1) 헤더 날짜와 실제 목록 시작일이 어긋나 혼동. (2) 셀에 게임이 보여서 클릭했는데 정작 그 게임이 패널에 없음 → "클릭이 잘못됐나?" 오인. 캘린더는 과거 셀도 게임을 보여주므로 클릭 결과와 모순.
   - 어떻게(택1): (a) 지난 날짜 셀 클릭 시엔 **그 날짜의 게임을 포함**해 보여주기(클릭일 기준, 기간필터와 별개로 해당일 출시 노출), 또는 (b) 지난 날짜 셀은 클릭 비활성/흐림(opacity↓, cursor:default)로 만들고 헤더 문구도 "오늘 이후"로 정정. 데이터 성격상 (a)가 일관적(과거 셀에 라벨을 보여주는 이상 클릭 결과도 그 게임을 보여야 함). → 개발자/QA에 기간필터-패널 경계 동작 확인 요청.

2. **이벤트 있는 셀 ↔ 빈 셀의 배경이 동일 → "이 달 어디에 출시가 있나"가 한눈에 안 들어옴** — 우선순위: 보통
   - 어디서: 월간 그리드. 출시 있는 셀(27·28·30)과 빈 셀의 배경이 모두 `#0f1115`로 동일. 구분 단서가 작은 점(dot)·작은 라벨뿐이라, 5월처럼 32건 중 3일만 이벤트인 달은 셀들이 거의 균질해 보임.
   - 왜 문제: 캘린더의 핵심 가치(어느 날 뭐가 나오나)를 스캔으로 즉시 파악하기 어려움. 빈 달 휑함(기존 IDEAS)과 별개로, **있는 날을 도드라지게** 만드는 관점.
   - 어떻게: 이벤트 보유 `.day`에 옅은 배경 tint(예 `background:rgba(74,144,226,0.06)`) + 좌측 2px 카테고리색 악센트 보더. 색만이 아니라 면(背景)으로 "여기 콘텐츠 있음"을 전달. 오늘/선택/임박 강조와 겹치지 않게 우선순위는 그보다 약하게.

3. **카테고리 명칭이 화면마다 4가지로 제각각** — 우선순위: 낮음
   - 어디서: 필터 드롭다운 "한국 MMO 신규 서버" / 통계줄 "신규서버" / 캘린더 범례 "신규 서버" / 리스트 카드 태그 "한국 MMO 신규 서버". (다른 카테고리도 "국내 PC/콘솔" vs "국내 PC콘솔" 등 띄어쓰기 편차 점검 필요.)
   - 왜 문제: 같은 카테고리가 표기마다 달라 일관성·신뢰감 저하, 셀 범례↔필터↔태그를 눈으로 매칭할 때 미세한 인지 부하.
   - 어떻게: 카테고리 표시명을 **라벨 상수 한 곳**에서 관리하고 모든 표면에 동일 문자열 사용(짧은 "신규 서버"로 통일 권장, 띄어쓰기 포함). 클린 미니멀 방향과도 부합.

### 현재 양호 (트집 X)
다크 테마·카테고리 색 체계 일관, 컴팩트 날짜 행·인라인 확장, sticky 그룹 헤더, 선택/오늘/임박 셀 위계, 통계줄 32=드롭다운 32 일치, 리스트 풀폭 행, 위시리스트 토글, 푸터 운영자 정보 모두 정상. 기존 '높음' 미반영 건(헤더 로고화·색+모양 이중 인코딩 a11y·셀 키보드 접근·SVG 아이콘·클린 미니멀 폰트/토큰)은 TODO/IDEAS 큐에 이미 있어 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-30 20:50] [디자이너] — 라이브 실측: 페이지 하단 바로가기 칩 / 범례-통계 중복 / '오늘' 색 일관성
실측: https://gcalen.com/ Chrome 데스크톱(1280px). 캘린더/리스트/날짜 클릭 패널 정상 렌더, 콘솔 에러 0건. 배포 games.json 32건(리서처 09:10) 라이브 확인. 모바일은 창 리사이즈가 캡처 뷰포트에 미반영(기존과 동일) → 다음 사이클 CSS 병행. 직전 사이클(12:50) 발견 '지난 날짜 셀 클릭 패널 헤더↔내용 불일치'는 개발자 16:30 fix·QA 01:47 ✅ 확인됨 → 30일 클릭 시 '2026.05.30 (토) 이후 출시 26건' 헤더와 목록 시작일 일치 실측 확인(종결, 재등록 안 함).

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)

1. **페이지 하단 '바로가기' 칩이 (a) 필터 버튼과 똑같이 생겨 용도(필터인지 페이지 이동인지) 모호 + (b) 캘린더와 푸터 사이 넓은 빈 다크 밴드에 떠 있어 위계 약함** — 우선순위: 보통
   - 어디서/실측: 캘린더 그리드(끝 ~y495)와 푸터(~y700) 사이, 중앙에 '바로가기:' 라벨 + 5개 칩('신규 게임 출시 예정 일정' / '신규 서버 오픈 일정' / '국내 신규 모바일 게임 출시 일정' / '신규 PC·콘솔 게임 출시 일정' / '글로벌 대작 게임 출시 일정'). 위·아래로 약 90~110px씩 빈 다크 공간.
   - 왜 문제: (1) 칩 외형이 상단 컨트롤(이번 주/다음 주/위시리스트)·필터 버튼과 동일한 outline pill이라, 이 칩들이 '카테고리 필터'인지 'SEO/페이지 내 이동 링크'인지 어포던스가 모호(같은 모양=같은 동작 기대). (2) '바로가기:' 라벨이 매우 흐려(거의 #666) 섹션 제목 역할을 못 함. (3) 큰 빈 밴드에 외따로 떠 있어 '이게 푸터인가 콘텐츠인가' 시각적 소속 불명 + 첫인상에 죽은 공간.
   - 어떻게: (a) 이 칩이 카테고리 필터로 점프/적용하는 거면 상단 카테고리 컨트롤과 시각 언어를 명시적으로 구분(링크형=텍스트+밑줄/화살표, 또는 섹션 박스로 감싸 'SEO 탐색' 맥락 부여). (b) '바로가기:' 라벨 대비 상향(--text-dim 수준) + 칩 묶음을 카드/구분선 안에 넣어 푸터 위 별도 섹션으로 명확히. (c) 위아래 빈 여백(margin)을 줄여 캘린더↔바로가기↔푸터 간격을 일관된 리듬으로. → 개발자: 이 칩의 실제 동작(필터 적용? 앵커 이동?) 확인 후 외형을 동작에 맞춤.

2. **카테고리 참조가 화면 상단에 2곳 중복 — 통계줄(필터 위)과 캘린더 범례(그리드 위)** — 우선순위: 낮음
   - 어디서: (i) 헤더 아래 통계줄 '국내 모바일 9 · 국내 PC/콘솔 5 · 글로벌 12 · 신규서버 6 · 총 32'(좌측정렬, 흐린 텍스트), (ii) 캘린더 카드 안 범례 '●국내 모바일 ●국내 PC/콘솔 ●글로벌 대작 ●신규 서버'(중앙정렬, 색점). 둘 다 동일한 4개 카테고리를 ~150px 세로 간격 안에서 따로 나열.
   - 왜 문제: 같은 카테고리 집합이 두 번 등장해 정보 중복 + 시선 분산. 게다가 통계줄은 좌측정렬인데 범례는 중앙정렬이라 정렬 언어도 불일치(헤더 좌측정렬 리프레시와도 어긋남).
   - 어떻게: 둘을 하나로 통합. 통계줄에 카테고리 색점을 얹어 '●국내 모바일 9 · ●국내 PC/콘솔 5 …' 한 줄로 만들면 '건수 + 색 키'를 동시에 전달(범례 별도 제거 가능). 통합 줄은 좌측정렬로 통일. (기등록 '통계줄 클릭=카테고리 필터' 아이디어와 합치면 색점+건수+클릭필터가 한 줄에 모임 — 시너지.)

3. **'오늘' 셀이 두 가지 색으로 동시에 표시됨 — 셀 보더는 파랑(today), 셀 안 '오늘' 라벨은 amber/골드(임박색)** — 우선순위: 낮음
   - 어디서/zoom 실측: 30일 셀 = 파란 외곽 보더(오늘) + 흰 굵은 '30' + 그 아래 '오늘' 텍스트가 amber(#f5a623 계열). amber는 사이트에서 '임박(.day-soon 보더·.dday.soon)'에 쓰는 색.
   - 왜 문제: '오늘'을 파랑(보더)과 amber(라벨)로 이중 인코딩 → 같은 상태를 두 색으로 말함. amber가 '임박/곧 출시' 의미로 이미 쓰이는데 '오늘' 라벨에도 쓰여 색-의미 매핑이 흐려짐(오늘 vs 임박 색 경계 모호).
   - 어떻게: '오늘' 라벨 색을 today 보더와 같은 --accent(파랑)로 통일 → 오늘=파랑 한 가지로 일관, amber는 '임박'에만 한정. (색 토큰 재사용, 신규 색 없음. styles.css 셀 '오늘' 라벨 color 1줄.)

### 현재 양호 (트집 X)
지난 날짜 셀 클릭 패널 헤더↔내용 일치(개발자 16:30 fix 실측 확인), 선택 셀 채움+inset링 위계, sticky 날짜 그룹 헤더, 컴팩트 날짜 패널 인라인 확장, 위시리스트 ★/☆ 토글, 통계줄 32=드롭다운 32 일치, 리스트 카드 메타/태그, 푸터 운영자 정보·데이터 갱신 시각 모두 정상. 기존 미반영 '높음'/운영자 요청(헤더 로고화·클린미니멀 폰트/토큰/SVG 아이콘·셀 키보드 접근·색+모양 이중인코딩·1건 날짜 흡수·이벤트 셀 면 강조)은 TODO/IDEAS 큐에 이미 있어 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-30 00:50] [디자이너] — 운영자 요청: 이모지 전면 제거 -> SVG 라인 아이콘 세트
운영자 직접 요청 "이모티콘 쓰지 말고 최대한 SVG로". 클린 미니멀 방향과 정확히 부합(Linear/Vercel류는 전부 일관된 라인 SVG). 이모지는 OS/브라우저마다 색·크기·baseline이 달라 클린 미니멀의 '일관성'을 깨므로 인라인 SVG로 통일.

### 현재 이모지/기호 인벤토리 (전수)
- 🎮 헤더 제목 (index.html:53)
- 📅 캘린더 토글 / 📋 리스트 토글 (index.html:60-61)
- 📅 모달 출시일 (script.js:297)
- 🛠️ 개발사 / 🏢 배급사 메타 (script.js:300-301)
- 📄 전체 페이지 / ▶ 트레일러 검색 / 🔗 링크 복사 (modal-actions, script.js:405)
- ★/☆ 위시리스트 토글 (script.js 다수: 219·290·396·430-431·499-503·719-720, 빈상태 안내문 163·624)
- ‹ › 캘린더 이전/다음 (index.html:119·121) — 이모지는 아니나 SVG chevron이 더 또렷

### 제안 (우선순위 순)
1. **인라인 SVG 아이콘 세트 도입 + 이모지 전량 치환** — 우선순위: 보통
   - 단일 파일 유지를 위해 index.html에 숨김 SVG 스프라이트(`<svg style="display:none"><symbol id="ic-..." viewBox="0 0 24 24">...</symbol>...</svg>`) 1벌 정의 후, 사용처에서 `<svg class="icon" aria-hidden="true"><use href="#ic-calendar"/></svg>`로 참조. JS 템플릿(script.js)도 `<use>` 마크업으로 교체.
   - 스타일 규칙: **Lucide/Feather 톤**(viewBox 24, `stroke=currentColor`, `fill=none`, `stroke-width:1.8`, `stroke-linecap/linejoin:round`). 크기 토큰 `.icon{width:1em;height:1em;vertical-align:-0.125em}`로 텍스트와 정렬. `currentColor`라 hover/active 색 자동 상속.
   - 필요한 아이콘: calendar, list, wrench(개발사), building(배급사), file-text(전체 페이지), play(트레일러), link(링크 복사), gamepad 또는 로고(헤더), chevron-left/right(네비), star(위시).
   - a11y: 텍스트 라벨 있는 버튼의 아이콘은 `aria-hidden="true"`(라벨이 읽힘). 아이콘만 있는 버튼(위시·네비)은 기존 `aria-label` 유지.

2. **위시리스트 ★/☆ -> SVG star (채움/외곽 토글)** — 우선순위: 보통
   - 현재 JS가 `textContent='★'/'☆'` 교체 방식 -> SVG 한 개 두고 **`.active`일 때 `fill`만 토글**(외곽=fill:none/stroke, 활성=fill:currentColor[amber]). textContent 스왑 제거. aria-pressed 유지.
   - 이점: 글꼴별 별 모양 편차 제거, 클린한 라인 별, active 시 fill 트랜지션으로 미세 피드백(클린 미니멀의 절제된 마이크로 인터랙션).

3. **‹ › 네비 -> chevron SVG + 본문 문구 정리** — 우선순위: 낮음
   - 버튼 기호를 chevron SVG로. 단, 빈상태 안내문 "‹ ›로 다른 달을..."(index.html:132, script.js:625)의 '‹ ›'는 더 이상 화면 기호와 안 맞으니 "양옆 화살표로" 등 텍스트로 교체.

4. **메타 이모지(🛠️🏢📅)는 SVG로 바꾸거나 아예 제거 검토** — 우선순위: 낮음
   - 클린 미니멀에선 메타 행을 아이콘 없이 라벨만('개발사 OOO')으로 두는 것도 깔끔. SVG로 바꾸든 제거하든 택1(개발사/배급사는 라벨, 출시일만 calendar 아이콘 유지 권장).

### 비고
- 이건 **앞으로의 규칙**이기도 함: UI에 새 이모지 추가 금지, 아이콘은 SVG 세트에서. 다른 에이전트(개발/리서처)도 따르도록 AGENTS.md에 한 줄 규칙 추가를 기획자/개발자에게 권고(디자이너는 문서 권한상 직접 수정 안 함).
- 1번(스프라이트+치환)이 토대 -> 한 번에 도입하고, 2~4번을 같은 PR에 묶으면 효율적. 큰 색 변경 없음(currentColor 상속).

---

## [2026-05-30 00:30] [디자이너] — 운영자 요청: 클린 미니멀(Linear/Vercel 풍) 리프레시 방향
운영자(쌀먹닷컴) 직접 요청 "더 트렌디하게" -> 방향 확정: **클린 미니멀**. 막연한 폴리시가 아니라 아래 구체 실행안으로 분해. 개발자는 한 사이클에 1~2개씩 픽업(전부 CSS 중심, 일부 폰트 로드/소규모 JS). 큰 색 변경 없이 '톤·여백·타이포·라운드'로 세련도를 올리는 게 핵심.

### 방향 원칙
색을 더 쓰지 말고 **덜 쓰기**. 강조는 색이 아니라 여백·굵기·톤 차이로. 카테고리 4색은 유지하되 캘린더에선 채도를 낮춰 보조로.

### 구체 실행안 (우선순위 순)
1. **타이포: 모던 한글 가변폰트 도입 + 위계 정리** — 우선순위: 보통
   - 현재 system-ui만 사용 -> 본문/제목 인상이 평범. Pretendard(한글 가변, 무료, Linear/토스류가 쓰는 톤) 또는 Inter+Noto Sans KR 조합을 `@font-face`/CDN로 로드.
   - 제목 weight 700·`letter-spacing:-0.01em`, 부제·메타는 weight 400·톤 다운. body line-height 1.5 유지. 폰트 1개 교체만으로 세련도 체감 큼.

2. **라운드·여백 토큰 통일** — 우선순위: 보통
   - `border-radius` 6px -> 10~12px로 카드/패널/버튼/모달 통일(`--radius:10px` 토큰화). 칩/작은 버튼은 8px.
   - 카드 패딩·그리드 gap 소폭 상향(여백을 늘려 '숨 쉬는' 레이아웃). Linear/Vercel의 핵심은 넉넉한 여백.

3. **플랫 보더 -> hairline + 미세 깊이** — 우선순위: 보통
   - 강한 보더(`#2a2e38`)에 의존하던 분리를, 더 옅은 hairline(`rgba(255,255,255,0.06)`) + 아주 옅은 그림자(`0 1px 2px rgba(0,0,0,0.3)`)로. surface(`#1a1d24`)에 미세 상단 하이라이트 그라데이션(1px inset)으로 깊이감. 톤 차이로 위계.

4. **컨트롤 일관화 (세그먼트 컨트롤 느낌)** — 우선순위: 보통
   - 캘린더/리스트 토글·퀵칩·필터 버튼 높이·라운드·hover 트랜지션 통일. 활성=채움(filled, accent), 비활성=배경 없는 outline로 상태 규칙 단일화. hover는 보더 밝아짐+1px lift 정도로 절제.
   - 펄스 애니(@keyframes pulse-dday) 제거(별도 제안과 동일) -> 과한 모션 정리.

5. **캘린더 색 절제** — 우선순위: 낮음
   - 셀 카테고리 dot 채도를 한 단계 낮춰 배경과 덜 튀게(이름 라벨이 주, 색은 보조). 강조(today/임박)만 선명하게 남겨 대비. 화면 전체가 차분해짐.

### 비고
- 1·2번이 체감 임팩트 가장 큼(폰트+여백/라운드) -> IDEAS 상단에 올림.
- 라이트 모드(별도 IDEAS, 후속)와 묶어 생각하면, 2·3번의 토큰화가 라이트 모드의 선행작업과 겹침 -> 토큰(`--radius` 등) 정리를 먼저 해두면 일석이조.
- a11y(색-only)·헤더 로고화 등 기존 제안과 충돌 없음(같은 방향).

---

## [2026-05-30 00:05] [디자이너] — 라이브 재점검 (반영 확인 + 신규 a11y/밀도)
실측: https://gcalen.com/ Chrome 데스크톱. 캘린더 뷰·날짜 클릭 패널·셀 dot/보더 zoom 확대 확인. 창 리사이즈가 렌더 뷰포트에 반영 안 돼 모바일은 styles.css @media(max-width:480px) 기준 병행 평가.

### 직전 사이클 이후 반영 확인 (개발자 작업 검증 — 양호)
- OK **날짜 클릭 패널 = 한 줄 컴팩트 행** 정상: `색점·게임명·플랫폼·D-day·☆`, 내부 스크롤 사라지고 페이지 흐름으로 인라인 확장됨(스크롤 트랩 해소). 운영자 요청 2건(컴팩트 행/내부 스크롤 제거) 충족.
- OK **통계줄 '총 29' = 카테고리 드롭다운 '전체 (29)' 일치**. 18:05 #2로 보고했던 29 vs 25 불일치 해소됨 -> 해당 제안 종결(재등록 안 함).
- OK 진입 시 '오늘 이후 가장 가까운 출시 달' 자동 진입 정상(5월에 임박 출시 있어 5월 유지).
- TODO 여전히 미반영(중복 등록 안 함): 헤더 로고화/컴팩트(운영자), `.day.selected` amber 보더 <-> `.day-soon` amber 보더 색 충돌(18:05 #1), 셀 hover 배경/키보드 포커스(18:05 #4 — `.day{cursor:pointer}`만 들어가고 hover bg·focus-visible는 아직).

### 발견한 문제 / 개선점 (신규, 미등록 항목만)

1. **카테고리를 '색'으로만 구분 — 색각이상 접근성 미흡** — 우선순위: 보통
   - 어디서: 캘린더 셀 `.day-dot`(7px 원)과 상단 범례 `.legend-dot`. 4색이 green #81c784 / blue #64b5f6 / purple #ba68c8 / orange #ff8a65.
   - 왜: 카테고리를 오직 색상(hue)으로만 전달. 적·녹색약(deuteran/protan) 사용자는 green<->orange, 셀에 dot만 있고 라벨 없을 때(2건 이상 셀) green<->purple 구분이 어려움. 색 외 단서가 전혀 없음(WCAG 1.4.1 '색만으로 정보 전달 금지' 위반 소지).
   - 개선:
     - 셀 `.day-dot`에 카테고리명을 `title`/`aria-label`로 부여(현재 셀 전체 title만 있음) -> 마우스/스크린리더 보조 단서.
     - 더 확실히: dot에 **모양 차이** 추가(모바일=원, PC/콘솔=사각 `border-radius:1px`, 글로벌=마름모 `rotate(45deg)`, 신규서버=링 `border만`). 색+모양 이중 인코딩이면 색각이상에도 구분 가능.
     - 범례도 동일 모양 적용(텍스트 라벨은 이미 있음 -> 모양만 맞추면 셀<->범례 매칭 명확).

2. **날짜 패널: 출시 1건인 날짜마다 날짜 헤더 1줄이 따로 붙어 세로 반복 과다** — 우선순위: 보통
   - 어디서: "OO 이후 출시 N건" 패널. 1건만 있는 날짜도 `YYYY.MM.DD (요일)` 헤더 + 행 1개 구조라, 25건 패널이 사실상 헤더/행/헤더/행 반복(06.03->1건, 06.04->1건, 06.05->1건…)으로 절반이 날짜 헤더.
   - 왜: 스캔 시 날짜 헤더가 시각 노이즈가 되고 세로가 불필요하게 길어짐. 컴팩트 행으로 만든 이점이 헤더 반복으로 일부 상쇄.
   - 개선: **1건 날짜는 날짜를 행 안으로 흡수** -> `06.04(목) · ●게임명 · Android · D-7 · ☆` 한 줄로. 독립 날짜 헤더는 **2건 이상인 날짜만** 유지. (renderDayRows에서 dateCounts 기반 분기 — 리스트 뷰 single-game 처리와 같은 패턴 재사용.)

3. **'오늘로' 버튼: 이미 현재 달을 보고 있을 때도 활성처럼 보임** — 우선순위: 낮음
   - 어디서: 캘린더 상단 `‹ 2026년 5월 › [오늘로]`. 현재 달을 보는 중에도 '오늘로'가 똑같이 클릭 가능한 외형(눌러도 변화 없음).
   - 개선: calendarMonth가 현재 달과 같으면 버튼 `disabled` + `.calendar-today-btn:disabled{opacity:0.4;cursor:default}`. 다른 달로 네비게이트하면 다시 활성. 무의미 클릭 방지 + 현재 위치 단서.

4. **빈 주(week)가 데스크톱에서 화면 한 칸 가까이 세로 점유** — 우선순위: 낮음
   - 어디서: `.calendar-grid .day{min-height:84px}` × 6주. 5월처럼 출시가 3일뿐인 달은 위 4주(1~26일)가 전부 빈 칸 84px씩 -> 첫 화면이 빈 격자로 길어짐.
   - 왜: '가까운 출시 달 자동 진입'으로 완화됐지만 그 달 안에서도 선행/후행 빈 주가 높이를 그대로 차지해 콘텐츠가 아래로 밀림(헤더 컴팩트 작업과 함께 첫 화면 밀도에 영향).
   - 개선(택1): (a) 데스크톱 `.day` min-height 84->68px(라벨 있는 셀은 내용으로 자연 확장 유지), 또는 (b) 게임 0건 선행/후행 '주' 행만 축약. (a)가 단순·안전. 헤더 컴팩트화와 묶으면 첫 화면에 캘린더 대부분이 들어옴.

### 현재 양호 (트집 X)
다크 테마/카테고리 색 체계 일관, 컴팩트 날짜 행·인라인 확장, 통계=드롭다운 일치, 임박 셀 강조, 모달 페이드, 리스트 풀폭 행·푸터 운영자 정보 모두 양호.

---

## [2026-05-29 23:04] [디자이너] — 라이브 실측: 리스트 카드 칩/메타 일관성 + 컨트롤 디테일
실측: https://gcalen.com/ Chrome 데스크톱(1516px) — 캘린더/리스트/날짜 클릭 패널 정상 렌더, 콘솔 에러 0건. 날짜 셀 클릭 패널 '한 줄 컴팩트 행·인라인 확장'·기본 캘린더 뷰·헤더 좌측정렬 컴팩트 모두 라이브 반영 확인. 모바일 뷰포트는 이번에도 resize가 렌더에 미반영 → styles.css 기준 병행. 직전 사이클 이후 '리스트 카드 시각 일관성·메타 표현·컨트롤 디테일' 관점에서 신규 관찰 항목만 등록(기존 노트/IDEAS 항목 중복 안 함).

### 발견한 문제 / 개선점 (신규)
1. **리스트 카드 안에 칩/태그 스타일이 3종 혼재 + 비클릭 장르 태그가 '링크 블루'라 클릭 가능처럼 보임** — 우선순위: 보통
   - 어디서: 리스트 뷰 카드. 한 카드 안에 (a) 카테고리 칩 "한국 MMO 신규 서버" = 솔리드 컬러 배경 pill, (b) 플랫폼 칩 "PC" = 회색 pill, (c) 장르 태그 "ARPG·핵앤슬래시·새 시즌" = 파란(accent) 텍스트 칩 — 세 가지 칩 디자인이 동시에 등장. 게다가 장르 태그의 파란색이 링크/날짜에 쓰는 accent 블루와 동일.
   - 왜 문제: (1) 칩 언어가 통일 안 돼 시각적으로 산만. (2) 파란 장르 태그가 클릭 가능한 링크(예: 장르 필터)처럼 보이지만 실제로는 비클릭 → 어포던스 거짓 신호. accent 블루는 '실제 링크/날짜'에만 쓰는 것이 위계상 맞음. (비고: 날짜 줄의 파란 링크 스타일·날짜 중복 건은 13:02 #3에 이미 등록 → 여기선 '칩 스타일 통일'과 '장르 태그 블루 남용'만 신규로 다룸.)
   - 개선: 비인터랙티브 메타 칩(플랫폼·장르)은 동일한 중립 톤(예: --surface 배경 + --text-dim 텍스트, 얇은 보더)으로 통일, accent 블루는 실제 링크/날짜에만 한정. 카테고리 칩만 카테고리 컬러 유지(정보 색). → 칩 3종을 2종(정보색 카테고리 / 중립 메타)으로 정리.

2. **카드 메타 아이콘(⚔ 시리즈 · 🏛 개발사)이 라벨 없이 아이콘만 → 의미 불명확 + 스크린리더 미스리딩** — 우선순위: 낮음
   - 어디서: 리스트 카드 본문. "⚔ 패스 오브 엑자일 2", "🏛 그라인딩기어게임즈" 처럼 이모지 아이콘 + 텍스트만 있고, 그 아이콘이 무엇(시리즈/개발사/배급사)을 뜻하는지 라벨이 없음.
   - 왜 문제: 사용자는 ⚔가 시리즈인지, 🏛가 개발사인지 배급사인지 추측해야 함. 스크린리더는 이모지를 그대로 읽어(예 "교차된 검") 의미 전달 실패.
   - 개선: 아이콘 옆에 짧은 라벨(예 "시리즈:", "개발사:")을 붙이거나, 최소한 각 줄에 aria-label/title("개발사 그라인딩기어게임즈") 부여. 시각적으로는 라벨을 흐린 톤으로 작게.

3. **"오늘로" 버튼이 현재 달을 보고 있을 때도 항상 활성(클릭해도 변화 없는 noop)** — 우선순위: 낮음
   - 어디서: 캘린더 월 네비게이션의 "오늘로" 버튼. 진입 시 이미 오늘이 포함된 달(현재 5월)을 보여주는데, 그 상태에서 "오늘로"를 눌러도 이미 그 달이라 화면 변화가 없음(개발자 동작 재확인 권장).
   - 왜 문제: 변화 없는 버튼이 평상시와 동일해 보여, 눌렀을 때 '반응 없음'으로 오인. 버튼 상태가 현재 컨텍스트를 반영 안 함.
   - 개선: 현재 보는 달에 오늘이 포함돼 있으면 "오늘로"를 disabled(흐림 + cursor:default + aria-disabled)로, 다른 달로 이동했을 때만 활성. 작은 JS(월 렌더 시 today 포함 여부로 disabled 토글).

4. **검색 placeholder "( / 키)" 단축키 힌트가 모바일에선 무의미 + placeholder 길어짐** — 우선순위: 낮음
   - 어디서: 검색 input placeholder "게임명 검색 ( / 키)". / 키로 검색창 포커스(데스크톱 단축키)는 좋은 기능이나, 터치 기기엔 물리 키보드 단축키가 없어 의미 없고 placeholder만 길어짐.
   - 개선: placeholder는 "게임명 검색"으로 단순화, / 단축키 안내는 데스크톱에서만 작게 보조 표기(또는 input title/툴팁). 단축키 기능 자체는 유지.

### 현재 양호 (트집 X)
캘린더 기본 뷰·헤더 좌측정렬 컴팩트·날짜 패널 컴팩트 인라인 행·통계줄 29=드롭다운 29·카테고리 색 체계·모달 페이드·푸터 운영자정보·로딩 fallback 가드 모두 정상. 기존 '높음' 미반영 건(헤더 로고화·날짜셀 auto-scroll·키보드 접근·sticky 그룹헤더·선택셀 위계·날짜미정 D-day·통계줄 클릭필터 등)은 TODO/IDEAS 큐에 이미 있어 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-29 13:02] [디자이너] — 라이브 실측: 복구 후 정상 확인 + 리스트/패널 스캔 폴리시
실측: https://gcalen.com/ Chrome 데스크톱(1516px) — 캘린더/리스트/날짜클릭 패널/검색/상세 모달 전부 정상 렌더, 콘솔 에러 0건. 개발자 12:30 헤더 좌측정렬·컴팩트화 라이브 반영 확인(캘린더 상향). 검색 → "2건 일치" 피드백·드롭다운 "전체(2)" 동기 정상. 모바일 뷰포트는 이번에도 resize가 렌더에 미반영 → styles.css 병행. 아래는 기존 노트/IDEAS에 없던 신규 관찰 항목만 등록(이미 등록된 auto-scroll·모바일라벨·날짜미정 D-day·통계줄클릭·헤더로고화·기본뷰캘린더·선택셀위계 등은 중복 안 함).

### 발견한 문제 / 개선점 (신규)
1. **긴 리스트/날짜 패널 스크롤 시 날짜 그룹 헤더가 사라져 "지금 보는 날짜" 맥락 상실** — 우선순위: 보통
   - 어디서: 리스트 뷰 + 날짜 클릭 패널. 29건(패널은 "이후 출시 25건")을 스크롤하면 `2026.06.18 (목)` 같은 날짜 그룹 헤더(`.date-group-header`)가 위로 떠내려가 사라짐. 화면 중간에서는 현재 행들이 어느 날짜 소속인지 알 수 없음(게임마다 D-day 배지는 있으나 절대 날짜는 재구성 필요).
   - 왜 문제: 날짜 기반 캘린더인데 정작 긴 목록을 훑을 때 날짜 컨텍스트가 끊겨 스캔성이 떨어짐.
   - 개선: `.date-group-header`에 `position:sticky; top:0`(+ 불투명 배경 `--bg`·약간의 z-index)를 주어 해당 그룹을 훑는 동안 날짜가 상단에 고정되게. 리스트 뷰·날짜 패널 공통 적용(모바일에서 특히 효과 큼).

2. **D-day 배지 색이 임박(D-2)·원거리(D-217) 구분 없이 동일 → 긴급도 시각 신호 없음** — 우선순위: 낮음
   - 어디서: 날짜 패널 + 리스트 행 우측 D-day 배지. D-2, D-6, D-21, D-171, D-217이 전부 같은 amber/주황 톤 → "임박 게임"과 "먼 게임"이 시각적으로 구분 안 됨.
   - 왜 문제: 캘린더의 핵심 가치는 "곧 나올 게임"인데 리스트에서 그게 눈에 안 띔. 캘린더 셀은 .day-soon(7일 내) 강조가 있지만 리스트/패널 배지에는 그 구분이 없음.
   - 개선: D-day 근접도로 배지 색 단계화 — 예) ≤7일 amber(기존 임박 톤 재사용), ≤30일 중립 텍스트, >30일 흐린 톤(--text-dim). 색만이 아니라 ≤7일은 굵게 등 보조 단서 함께.

3. **리스트 뷰 카드 내부의 출시일이 날짜그룹 헤더와 중복 표시 + 링크처럼 보이는 파란 스타일(클릭 어포던스 혼동)** — 우선순위: 낮음
   - 어디서: 리스트 뷰 카드. 카드 위에 날짜그룹 헤더 `2026.06.03 (수)`가 있는데, 카드 안에 또 `🗓 2026.06.03 (수)`가 파란·링크처럼 반복 표시됨. 그룹 헤더가 이미 날짜를 명시하므로 중복.
   - 왜 문제: (1) 동일 정보 중복으로 시각 노이즈. (2) 카드 전체가 클릭=모달인데 그 안에 파란 "링크처럼" 보이는 날짜가 있어 클릭 대상 어포던스가 혼동.
   - 개선: 카드 내 날짜 줄을 제거하거나, 날짜 대신 "출시 시각/플랫폼 출시 메모" 같은 비중복 정보로 교체(날짜 출처는 그룹 헤더로 일원화). 링크가 아니면 파란 링크 스타일 제거(메타 텍스트 톤으로).

4. **검색어 입력 시 컨트롤 바가 2줄로 reflow되며 퀵칩(이번주/다음주/위시리스트)이 우측 단독 행으로 분리 → 레이아웃 점프/정렬 불균형** — 우선순위: 낮음
   - 어디서: 검색창. 검색어가 비었을 때는 검색+드롭다운+퀵칩이 한 줄(1516px)이다가, 검색어를 치면(✕ 버튼·"N건 일치" 추가) 퀵칩이 두 번째 줄로 내려가 우측 단독 정렬됨. 필터 바가 상태에 따라 정렬/높이가 튀며 화면이 점프.
   - 개선: 컨트롤 컨테이너의 wrap 정책 고정 — 퀵칩이 줄바꿈될 때도 좌측 정렬 유지하거나, 검색 상태와 무관하게 항상 동일 그리드 레이아웃으로 고정해 점프 제거. (개발자: 1516px에서도 reflow되는지 재현 확인 권장.)

### 현재 양호 (트집 X)
헤더 좌측정렬·컴팩트화(개발자 12:30) 라이브 반영 확인. 날짜 패널 한 줄 컴팩트 행·인라인 확장·검색 피드백·통계줄 29=드롭다운 29 일치·모달 페이드·푸터 운영자정보·로딩 fallback 가드 모두 정상. 기존 "높음" 미반영 건(헤더 로고화·기본뷰 캘린더 고정·auto-scroll·날짜미정 D-day·통계줄 클릭필터·선택셀 위계)은 TODO/IDEAS 큐에 이미 있어 중복 등록 안 함 — 픽업 대기.

---

## [2026-05-29 20:50] [디자이너] — 라이브 실측: 캘린더 진입 경험(스크롤/빈달/모바일/키보드)
실측: https://gcalen.com/ Chrome 데스크톱(1516px) — 리스트(기본뷰)·캘린더·날짜 클릭 패널 정상 렌더, 콘솔 에러 0건. QA 22:40 TDZ 복구 + 개발자 fallback 가드 라이브 정상. 모바일 뷰포트는 이번에도 resize가 스크린샷 렌더에 미반영 → styles.css 기준 병행 평가. 직전 사이클(23:02) 이후 캘린더 '진입·클릭 경험' 관점에서 신규로 관찰된 항목만 등록(기존 등록 항목 중복 안 함).

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)
1. **날짜 셀 클릭 시 결과 패널이 시야 밖에서 열려 '아무 일도 안 일어난 듯' 보임 (auto-scroll 부재)** — 우선순위: 높음
   - 어디서: 캘린더에서 날짜 셀 클릭 → `#day-detail-panel`이 **달력 그리드 전체 아래**에 펼쳐짐(script.js openDayPanel, scrollIntoView 호출 없음 확인). 데스크톱에서 하단 행(예 30일)을 클릭하면 패널이 폴드 아래라 화면이 그대로 → 클릭이 먹혔는지 알 수 없음(실측 시 클릭 후 화면 무변화).
   - 왜 문제: 클릭→결과의 시각적 연결이 끊김. 사용자는 반응이 없다고 느껴 다른 날을 더 누르거나 이탈. 캘린더의 핵심 인터랙션이 '발견되지 않는' 상태.
   - 개선: 패널 렌더 직후 `dayPanel.scrollIntoView({behavior:'smooth', block:'start'})`(prefers-reduced-motion 시 'auto'). 더하여 패널 헤더에 0.3~0.5s 옅은 강조(예 배경 플래시) 1회로 "여기 열렸어요" 신호. 모바일도 동일 이점.

2. **빈 달 진입 시 거대한 수직 공백(첫 화면 대부분 빈 셀) → '데이터 없는 사이트' 인상** — 우선순위: 보통
   - 어디서: 캘린더 자동 진입 달(현재 2026년 5월). 출시는 27·28·30일에만 몰려 있고 1~23일은 전부 빈 셀(약 4행 × min-height 84px ≈ 340px+ 빈 그리드)이라 첫 스크린에 콘텐츠가 거의 안 보임.
   - 왜 문제: '가장 가까운 출시 달'로 자동 진입하지만 그 달이 희소하면 상단이 죽은 공간이 되어 첫인상이 휑함. 사용자가 "이 사이트 비었나?"로 오인.
   - 개선: (a) 월 라벨 옆에 `이 달 출시 N건` 보조 카운트 배지 → 빈 그리드의 이유를 설명하고 ‹ › 탐색 유도. (b) 빈 달이면 기존 `#calendar-empty` 안내를 그리드 상단(달력 안)에도 노출. (c) 더 적극적으로는 자동 진입을 '가장 가까운 출시일'이 아니라 '향후 출시가 가장 많은(또는 첫 출시가 상단행에 걸리는) 달' 기준으로 두는 것도 검토. 최소 (a)만으로도 휑함 완화.

3. **모바일(≤480px) 캘린더 셀 게임명 라벨 과도 truncation** — 우선순위: 보통
   - 어디서: `.day-game-label` 모바일 0.62rem + `white-space:nowrap; text-overflow:ellipsis`. 7열 고정 그리드(390px 폭 기준 셀 ≈ 48px)에서 날짜 숫자 + 게임명 1줄을 한 셀에 넣으면 게임명이 3~4글자 수준으로 잘려 거의 판독 불가(예 "패스 오…"). 점(dots)과도 세로로 겹쳐 답답.
   - 왜 문제: 모바일에서 라벨이 정보 전달을 못 하면서 셀만 빽빽 → 가독성/스캔성 저하. 좁은 화면 핵심 동선.
   - 개선: ≤480px에서는 `.day-game-label`을 숨기고 **카테고리 점(dots)만** 남겨 셀을 깔끔히(상세는 셀 클릭 패널로). 또는 라벨 대신 `●×N` 건수 칩 하나만. 데스크톱(라벨 표시)은 현행 유지.

4. **캘린더 날짜 셀 키보드/스크린리더 접근 불가 (role·tabindex 부재)** — 우선순위: 보통
   - 어디서: 캘린더 셀 마크업(script.js cells = `<div class="day" data-date=...>`). cursor:pointer는 반영됐으나 day-row(role="button" tabindex="0")·위시 버튼과 달리 **셀에는 role/tabindex/aria 없음** → 키보드 Tab 포커스 불가, 엔터로 패널 열기 불가, 스크린리더가 클릭 가능 요소로 인지 못 함.
   - 왜 문제: 마우스 외 사용자는 캘린더의 날짜 패널 기능에 아예 접근 불가(접근성 사각). 직전 hover/cursor 어포던스(반영됨)의 남은 절반.
   - 개선: 게임 있는 셀에 `role="button" tabindex="0" aria-label="5월 30일, 출시 N건"` 부여 + Enter/Space 키 핸들러를 기존 셀 클릭 핸들러에 연결, `:focus-visible` 링 추가. (#1 auto-scroll과 함께 적용하면 키보드 동선까지 자연스러움.)

### 현재 양호 (트집 X)
복구 후 리스트 풀폭 행·카테고리 색 체계·날짜 패널 컴팩트 인라인 확장·상세 모달(페이드/트레일러/링크)·푸터 운영자정보·로딩 fallback 가드 모두 정상. 통계 29 = 드롭다운 29 일치 확인. 기존 '높음' 미반영 건(헤더 로고화·헤더 컴팩트화·기본뷰 캘린더 고정·선택셀 위계 분리·날짜미정 D-day·통계줄 클릭필터)은 TODO/IDEAS 큐에 이미 있어 **중복 등록 안 함** — 픽업 대기.

---

## [2026-05-29 23:02] [디자이너] — 라이브 실측: 사이트 복구 후 정상 점검 (날짜 패널·모달·D-day)
실측: https://gcalen.com/ Chrome 데스크톱(1516px) — 캘린더/리스트/날짜클릭 패널/상세 모달 전부 정상 렌더, 콘솔 에러 0건. QA 22:40 TDZ 복구 라이브 확인됨. 직전 사이클(19:02 다운 상태) 이후 신규로 관찰 가능해진 항목 위주로 점검. (모바일 뷰포트는 이번에도 resize가 렌더에 미반영 → CSS 기준 병행, 신규 모바일 항목은 보류)
복구 후 확인된 신규 반영: 날짜 클릭 패널 '한 줄 컴팩트 행 + 내부 스크롤 제거→인라인 확장'(운영자 요청) 라이브 정상 동작 / 통계줄 29 = 드롭다운 '전체(29)' 숫자 일치(직전 버그 해소) 확인.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)
1. **'날짜 미정/유동적' 게임에 정확한 D-day(예 "D-217") 표시 → 가짜 정밀도** — 우선순위: 보통
   - 어디서: 날짜 클릭 패널 + 리스트 뷰 하단. "이클립스: 더 어웨이크닝 (2026 예정·날짜 미정)", "미트5 (2026 연내 목표·일정 유동적)", "나이트크로우2 (2026 연내 목표·날짜 미정)" 등 5건이 전부 **2026.12.31 (목)** 그룹으로 묶여 똑같이 **"D-217"** 배지를 달고 있음.
   - 왜 문제: 게임명에는 "날짜 미정/유동적"이라고 명시돼 있는데 옆 배지는 "D-217"이라는 확정 카운트다운을 보여줌 → 정보 모순. 사용자는 "12월 31일 확정 출시"로 오해하고, 캘린더에서도 12/31 셀에 몰려 표시될 위험.
   - 개선: 데이터에 `date_tbd`(또는 release_date가 미정 플레이스홀더인지) 플래그를 두고, 그런 항목은 D-day 배지 대신 **"미정"/"2026 예정"** 같은 라벨로 대체. 패널/리스트에서도 12/31 그룹이 아니라 맨 끝 **"날짜 미정 · 2026 예정"** 별도 그룹으로 묶기(정렬은 확정일 그룹 다음). 캘린더 셀에는 미배치(또는 월 상단 "이 달 예정(날짜미정) N건" 보조 표기).
   - 효과: 확정일 게임과 미정 게임의 신뢰도 구분 → "12/31 떼출시" 오인 제거.

2. **상세 모달 상단 배너: 카테고리 라벨 중복 + 큰 빈 그라데이션 블록(세로 낭비)** — 우선순위: 낮음
   - 어디서: 게임 카드 클릭 시 상세 모달. 상단에 약 140px 높이의 단색 그라데이션 배너가 있고 그 안에 카테고리명("한국 MMO 신규 서버")이 적혀 있는데, **바로 아래 줄에 동일한 카테고리 태그가 또 표시**됨. 배너는 이미지가 아니라 빈 색 블록이라 정보량 없이 세로만 크게 차지(제목/메타가 한참 아래로 밀림).
   - 비고: 리스트 뷰 카드 배너 중복(204행)은 4px로 이미 정리됨 — 이건 **모달 전용 배너(.modal-image 류)**로 별개 미반영 건.
   - 개선: (a) 배너 높이 140→약 64~80px로 축소하고 안의 카테고리 텍스트는 제거(아래 태그가 이미 전달) → 빈 블록 대신 얇은 카테고리 컬러 바 느낌. 또는 (b) 배너를 없애고 제목 좌측에 카테고리 컬러 점/바만. 첫 화면에 제목·출시일·CTA가 더 빨리 들어오게.

3. **날짜 패널 컴팩트 행: 데스크톱에서 게임명 ↔ 플랫폼/D-day/☆ 사이 빈 공간 과다(스캔 동선 길어짐)** — 우선순위: 낮음
   - 어디서: 날짜 클릭 패널의 한 줄 행. 데스크톱(1516px)에서 게임명은 좌측, 플랫폼·D-day·☆는 우측 끝(`justify-content:space-between` 추정)이라 그 사이에 약 600px 빈 공백. 시선이 이름→D-day로 멀리 이동해야 하고, 어떤 게임의 D-day인지 라인 추적이 필요.
   - 개선: 행 내용 컨테이너에 `max-width`(예 760~880px) + 가운데 정렬을 주거나, 플랫폼·D-day를 게임명 **바로 뒤(좌측 묶음)**로 당기고 ☆만 우측 고정. 모바일은 현행(꽉 참) 유지라 영향 없음.

4. **상단 통계줄(국내모바일 8 · … · 총 29)이 비클릭 텍스트 → 카테고리 진입 기회 손실** — 우선순위: 보통
   - 어디서: 헤더 아래 통계 요약 한 줄. 각 카테고리 건수가 보이지만 클릭 불가라, 사용자는 같은 분류를 보려면 다시 아래 '카테고리' 드롭다운을 찾아 선택해야 함(동작 중복).
   - 개선: 각 세그먼트("국내 모바일 8" 등)를 클릭 가능하게 → 클릭 시 해당 카테고리 필터 적용(드롭다운 값도 동기화), 현재 활성 카테고리는 굵게/밑줄로 표시. 키보드 접근(button 시맨틱 + focus-visible)·`aria-pressed`도 함께. "총 N"은 전체 해제.
   - 효과: 통계줄이 '읽기 전용 숫자'에서 '원클릭 카테고리 탐색'으로 → 정보 구조와 인터랙션 일치.

### 현재 양호 (트집 X)
복구 후 캘린더 셀(오늘 파랑/임박 amber)·리스트 풀폭 행·날짜 패널 컴팩트 인라인 확장·상세 모달(출처/트레일러/링크복사)·푸터 운영자정보·SEO 바로가기 칩 모두 정상. 기존 '높음' 미반영 건(헤더 로고화·헤더 컴팩트화·기본뷰 캘린더 고정·선택셀 위계 분리·에러상태 fallback)은 TODO 큐에 이미 있어 **중복 등록 안 함** — 기획자/개발자 픽업 대기.

---

## [2026-05-29 19:02] [디자이너] — 라이브 실측: 사이트 다운 상태에서 '로딩/에러 상태' UX 점검
실측: https://gcalen.com/ Chrome 데스크톱(1568px) 스크린샷 + 콘솔. 라이브가 현재 **완전 다운**(콘솔 `ReferenceError: Cannot access 'dayPanel' before initialization` script.js:477 → 스크립트 중단). 화면은 헤더·필터·범례만 뜨고 본문은 "불러오는 중..."에서 **영구 고착**, 푸터 "데이터 마지막 갱신: —". (이 회귀 자체는 QA 21:47이 이미 BUGS 1순위로 등재 → 디자이너는 중복 등록 안 함.)
캘린더/리스트/날짜패널 등 렌더 의존 항목은 사이트가 살아나야 평가 가능 → 이번 사이클은 **로딩/에러 상태(앱이 죽어도 보이는 영역)** 만 신규 제안.

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)
1. **로딩 실패/스크립트 크래시 시 '에러 상태' 부재 → 무한 "불러오는 중..."** — 우선순위: 높음
   - 어디서: 본문 로딩 플레이스홀더. 지금처럼 script.js가 죽거나 games.json fetch가 실패하면 "불러오는 중..." 텍스트가 **영원히** 남고, 사용자는 사이트가 멈췄는지/로딩 중인지 알 수 없음(오늘 라이브가 정확히 이 상태).
   - 왜: 로딩 상태에 종료 조건(성공/실패)이 없어 실패가 로딩처럼 보임. 사용자는 새로고침해야 한다는 단서조차 못 받음.
   - 개선: (a) `window.onerror`/`unhandledrejection` 전역 핸들러 + games.json fetch `.catch`에서 로딩 영역을 **에러 상태**로 교체 — 예 "데이터를 불러오지 못했어요. 잠시 후 새로고침 해주세요." + [새로고침] 버튼(`location.reload()`). (b) 전역 핸들러는 메인 스크립트와 **별도의 작은 inline \<script\>** 로 둬서 메인이 TDZ로 죽어도 에러 UI는 살아있게(중요 — 오늘처럼 같은 파일이 죽으면 내부 catch는 실행 안 됨).
   - 효과: 장애 시에도 '고장난 빈 화면'이 아니라 회복 경로(새로고침)를 제시 → 첫인상/신뢰도 보호.

2. **로딩 타임아웃 가드 부재** — 우선순위: 보통
   - 어디서: 동일 로딩 플레이스홀더.
   - 왜: #1과 별개로, 네트워크가 느리거나 응답이 끝내 안 와도 무한 로딩. 명시적 타임아웃이 없음.
   - 개선: 로딩 시작 시 `setTimeout`(예 8~10초) 걸어 그때까지 렌더 완료 안 되면 #1의 에러 상태로 전환. 정상 렌더되면 타이머 clear.

3. **로딩 인디케이터가 정적 텍스트뿐(움직임 없음)** — 우선순위: 낮음
   - 어디서: "불러오는 중..." 텍스트. 스피너/스켈레톤 없이 정적이라, 멈춘 화면인지 로딩 중인지 시각적으로 구분이 약함(오늘 다운 상태에서도 '로딩처럼' 보임).
   - 개선: 경량 CSS 스피너(회전 원) 또는 캘린더 그리드 스켈레톤(회색 셀 펄스) 추가. 움직이는 인디케이터는 '진행 중'을, 멈추면(에러 전환) '문제 발생'을 직관적으로 전달. #1·#2 적용 시 함께.

### 평가 불가 / 라이브 미반영 (기존 등록 — 중복 안 함)
사이트 다운으로 캘린더 셀/리스트/날짜패널 렌더 항목은 이번 사이클 실측 불가. 기존 '높음' 운영자 요청(헤더 텍스트제목→로고화·홈리셋, 날짜패널 컴팩트 행·내부스크롤 제거)과 캘린더 첫인상(가까운 출시 달 자동 진입은 직전 반영 확인됨) 등은 기존 노트 유지. **최우선은 QA 등재 회귀(script.js TDZ) 복구** — 그 전엔 모든 렌더 항목 검증 불가.

---

## [2026-05-29 18:05] [디자이너] — 라이브 재점검 (캘린더 셀 디테일)
실측: https://gcalen.com/ Chrome 데스크톱(1920px) 캘린더·리스트 뷰. 셀 보더/라벨/점을 zoom 확대해 확인. (모바일 뷰포트는 이번에도 리사이즈가 렌더에 반영 안 돼 CSS 기준 병행) 직전 사이클 이후 코드 변경: 개발자 '오늘 이후 가장 가까운 출시 달 자동 초기화' 반영 확인됨(5월 유지 정상 동작).

### 발견한 문제 / 개선점 (기존 미등록 신규 항목만)
1. **선택 셀 보더색 ↔ 출시 임박 셀(.day-soon) 보더색 충돌** — 우선순위: 보통
   - 어디서: 캘린더 셀. zoom 확대 결과 사용자가 클릭한 '선택 셀'(예 28일)은 주황 보더, '출시 임박 셀'(예 30일, D-2)은 거의 동일한 amber 보더. 오늘(29일)만 파란 보더로 구분됨.
   - 왜: 선택(사용자 액션 상태)과 임박(데이터 속성)이 같은 색 계열이라 "내가 누른 날"과 "임박한 날"을 구분 못 함 → 상태 위계 모호.
   - 개선: 셋을 명확히 분리. 예) 오늘=파란 보더(유지), 임박=amber 보더(유지), 선택은 보더가 아니라 **셀 배경 채움(예 #1c2330) + 2px 흰/파랑 링**으로. 색 하나가 아니라 '채움 vs 보더'로 위계를 다르게 줄 것.

2. **상단 통계줄 "총 29" ↔ 카테고리 드롭다운 "전체 (25)" 숫자 불일치** — 우선순위: 낮음
   - 어디서: 통계줄 "국내 모바일 8 · … · 총 29" 바로 아래 카테고리 셀렉트 "전체 (25)". 인접한 두 합계(29 vs 25)가 다름.
   - 왜: 통계줄은 데이터 전체(29), 드롭다운 카운트는 활성 기간필터('앞으로 1년') 적용 후(25) 기준으로 추정 → 같은 화면 두 숫자가 어긋나 혼란.
   - 개선: 둘을 같은 기준으로. 통계줄도 현재 활성 필터(기간 등) 기준으로 카운트하거나, 라벨로 구분(통계줄="총 29(전체 데이터)", 드롭다운="현재 조건"). 가장 깔끔한 건 둘 다 활성 필터 반영해 일치.

3. **단일 출시일 셀: 게임명 라벨 + 색점(dot) 중복 / 다건일 때 규칙 불일치** — 우선순위: 낮음
   - 어디서: 캘린더 셀. 1건 셀(28·30)은 게임명 1개 + 점 1개(중복), 2건 셀(27)은 게임명 1개만 + 점 2개. 이름과 점이 따로 놀고 1건/다건 규칙이 일관되지 않음.
   - 개선: 한 규칙으로 통일. 예) `● 게임명(말줄임)`(앞에 카테고리 색점 인라인), 2건 이상이면 `● 게임명 외 N건`. 별도 점 행을 없애 점·이름을 통합하고 1건/다건 모두 동일 패턴으로. (16:55 #4 '라벨 가독성'과 별개로 점/라벨 중복·규칙 정리 관점.)

4. **날짜 셀 클릭 어포던스 부재(hover/cursor 피드백 없음)** — 우선순위: 보통
   - 어디서: 캘린더 날짜 셀. 게임 있는 셀에 마우스 호버해도 배경/커서 변화 없음(zoom 확인). 셀을 누르면 패널이 열리지만 클릭 가능하다는 단서가 없어 인터랙션을 발견 못 할 수 있음.
   - 개선: 게임 있는 셀(또는 전체 셀)에 `cursor:pointer` + 호버 시 옅은 배경(예 #161a22)/보더 강조. 키보드 접근(셀 tabindex/role + focus-visible 링)도 함께 검토.

### 현재 양호 (트집 X)
'오늘 이후 가까운 출시 달 자동 진입'(개발자 신규 반영) 정상, 리스트 풀폭 행·카테고리 색 체계·퀵칩 토글 피드백·모달·푸터 운영자 정보 모두 양호. 기존 '높음' 운영자 요청(헤더 로고화·날짜 패널 컴팩트 행/내부 스크롤 제거)은 여전히 라이브 미반영 — 기획자 TODO 픽업 대기(중복 등록 안 함).

---


## [2026-05-29 18:04] [디자이너] — 운영자 요청: 날짜 클릭 패널 내부 스크롤 제거
운영자(쌀먹닷컴) 직접 요청. 라이브 실측으로 재현 확인.

### 개선점
1. **날짜 클릭 패널의 내부 스크롤(스크롤 속 스크롤) 제거 → 페이지 흐름으로 인라인 확장** — 우선순위: 높음 (운영자 요청)
   - 어디서: 캘린더에서 날짜 클릭 시 아래 펼쳐지는 "OO 이후 출시 N건" 패널.
   - 재현: 30일 클릭 → "2026.05.30 (토) 이후 출시 25건" 패널이 **고정 높이 박스 + 내부 overflow-y 스크롤**로 열림. 패널 오른쪽에 자체 스크롤바가 생겨, 페이지 스크롤과 별개로 패널 안에서 또 드래그해야 함(스크롤 트랩/스크롤 속 스크롤).
   - 왜 불편: 마우스 휠이 패널 안에 갇히거나, 작은 내부 스크롤바를 따로 드래그해야 함. 모바일에선 더 심각(중첩 스크롤 = 터치 스크롤 충돌).
   - 개선:
     - 패널의 `max-height`(약 60vh 추정) + `overflow-y:auto/scroll` 제거 → 내용이 페이지에 그대로 펼쳐지고 **페이지 전체 스크롤 하나로 통일**. 내부 스크롤바 소멸.
     - 이미 등록된 "한 줄 컴팩트 행"(색점·게임명·플랫폼·D-day·☆ → 클릭=상세 모달) 제안과 **묶어 처리** 권장. 컴팩트 행이면 25건도 세로가 짧아져, 인라인 확장해도 페이지가 과하게 길어지지 않음.
     - 패널 열리면 해당 패널 상단으로 부드럽게 `scrollInto(view)` 한 번만(선택). 닫기(×)는 현행 유지.
   - 비고: 16:31/IDEAS의 컴팩트 행 항목과 동일 작업 묶음. 개발자: (1) 컨테이너 고정높이/overflow 제거 + (2) 행 컴팩트화를 한 번에.

---


## [2026-05-29 17:58] [디자이너] — 운영자 요청: 텍스트 제목 → 로고(홈 기능)
운영자(쌀먹닷컴) 직접 요청. 채팅으로 제안받아 정식 등록. 기존 "헤더 좌측정렬·컴팩트화"(16:24 #1)를 이 방향으로 구체화함.

### 개선점
1. **상단 텍스트 제목 제거 → 로고로 교체, 로고를 '홈/리셋' 버튼으로** — 우선순위: 높음 (운영자 요청)
   - 어디서: 페이지 상단 헤더(현재 🎮+"게임 출시 캘린더" 큰 가운데정렬 제목·부제·"마지막 업데이트" 줄이 상단 ~310px 점유, 캘린더가 화면 한참 아래 시작).
   - 왜: 텍스트 헤더가 세로 공간을 크게 잡아 첫 화면에 달력이 다 안 들어옴. 로고화하면 공간 절감 + 브랜딩 + 홈 내비 일석삼조.
   - 개선:
     - 로고를 **좌측 상단에 작게**(높이 ~32–40px). 부제/"마지막 업데이트" 줄은 제거(업데이트 시각은 푸터에 이미 있음).
     - 로고 클릭 = **홈/리셋**: 캘린더 뷰로 전환 + 현재 월로 이동 + 카테고리/플랫폼/기간/검색/위시리스트 필터 초기화. (단순 페이지 새로고침보다 상태 리셋이 깔끔.)
     - 자산: 이미 있는 `favicon.svg`를 확장/재사용해 로고로(톤 일관). 가로형 워드마크가 필요하면 아이콘+작은 텍스트 조합.
   - **SEO/접근성 주의(중요)**: 텍스트 제목을 화면에서 빼더라도 사이트명 h1은 유지할 것 — 로고 `<img alt="게임 출시 캘린더">` 또는 시각적으로 숨긴 `<h1>`(visually-hidden) 사용. 로고 링크에 `aria-label="홈"`, 키보드 포커스 가능하게(`<a>`/`<button>` 시맨틱 + focus-visible 링).
   - 결과: 헤더가 1줄로 압축 → 캘린더 시작 위치 ~150–200px 상향(16:24 #1 목표와 동일).

### 비고
- 이 항목은 16:24 #1(헤더 컴팩트화)의 상위호환 구체안. 개발자는 둘을 하나의 작업으로 묶어 처리하면 됨.

---


## [2026-05-29 17:05] [디자이너] — 라이브 재점검 (검증 사이클)
직전 디자이너 사이클(16:55) 직후 재확인. Chrome 데스크톱(1280px) 실측 — 캘린더·리스트 뷰 + 신규 로드 기본 뷰 확인. 모바일 뷰포트는 이번에도 리사이즈가 스크린샷에 반영되지 않아 보류.

### 새 이슈: 없음 (억지 트집 회피)
한 사이클 사이 코드 변경 없음(개발자 21:20 "TODO 큐 비어있음, 기획자 결정 대기"). 신규 발견 사항 없음 → 신규 제안 등록 안 함(중복 방지).

### 검증 결과 — 기존 '높음'/운영자 요청 제안이 아직 라이브 미반영 (기획자 TODO 픽업 필요)
- (16:55 #1·#2) 진입 기본 달이 출시 거의 없는 현재 달(5월)이라 첫인상이 빈 캘린더 / 출시 0건 달의 캘린더 빈 상태 안내 부재 → **라이브 그대로**. 여전히 최우선 개선 기회.
- (16:24 #1, 운영자 요청) 헤더 좌측정렬·컴팩트화 → **미반영**. 라이브는 여전히 큰 가운데정렬 헤더가 상단을 점유, 캘린더가 화면 한참 아래에서 시작.
- (16:24 #2, 운영자 요청) 진입 시 캘린더 고정 → **재현 확인, 미반영**. 뷰 선택을 localStorage로 기억함(직전에 리스트를 봤으면 새로고침 시 리스트로 복귀하는 것 실제 재현). "항상 캘린더로 시작" 아님.
- (16:31, 운영자 요청) 날짜 클릭 패널 한 줄 컴팩트 행 전환 → **미반영**.

→ 위 항목은 이미 DESIGN_NOTES/IDEAS에 있으므로 중복 등록하지 않음. 개발 TODO 큐가 비었으니, 기획자가 운영자 요청 3건 + 캘린더 첫인상 2건 중 우선순위를 정해 큐에 넣어주면 바로 진행 가능.

### 현재 양호 (트집 X)
다크 테마·카테고리 색 체계 일관, 리스트 풀폭 행, 출시 임박 셀(.day-soon) 강조, 모달 페이드, 푸터 hover/운영자 정보·SEO 바로가기 — 직전 사이클 반영 확인됨.

---


## [2026-05-29 16:55] [디자이너]
실측: https://gcalen.com/ Chrome 스크린샷(데스크톱 1200~1516px, 캘린더·리스트 뷰 둘 다) + 퀵칩 활성 상태 확인.
모바일은 OS 창은 420px로 줄였으나 렌더 뷰포트가 데스크톱 폭 유지(스크린샷 미반영) → 모바일 실측은 이번에도 보류, CSS 기준 평가.

### 발견한 문제 / 개선점

1. **진입 시 현재 달이 거의 텅 비어 보임 (첫인상 = 빈 캘린더)** — 우선순위: 높음
   - 어디서: 캘린더 뷰 기본 화면. 항상 "오늘이 속한 달"(현재 2026년 5월)로 시작하는데, 5월은 출시가 27·28·30 단 3일뿐이고 위 4주(1~26일)가 전부 빈 칸. 첫 화면 거의 전체가 비어 "데이터 없는/고장난 사이트" 인상.
   - 왜: 출시 게임은 대부분 다음 달 이후에 몰려 있는데, 정작 첫 화면은 출시가 가장 적은 현재 달을 보여줌. 사용자가 가치를 못 느끼고 이탈 위험.
   - 개선: 진입 시 calendarMonth를 "오늘 이후 가장 가까운 출시(release_date>=today)가 있는 달"로 초기화(예: 5월에 임박 출시가 있으면 5월 유지, 없으면 다음 출시가 있는 달로 점프). 또는 현재 달 유지하되 월 네비게이션 옆에 "이 달 출시 N건 · 다음 달 N건" 카운트를 노출해 빈 칸이 "정상"임을 알려줌. 전자가 임팩트 큼.

2. **출시 0건인 달의 캘린더 빈 상태(empty state) 안내 부재** — 우선순위: 높음
   - 어디서: ‹ › 로 출시가 하나도 없는 달로 이동하면 날짜 숫자만 있는 빈 그리드만 표시(안내 문구 없음). 리스트 뷰엔 .empty-state가 있으나 캘린더 뷰엔 없음.
   - 개선: 그날 출시가 0건인 달에는 그리드 위(또는 아래)에 옅은 안내 한 줄 — 예 "이 달은 예정된 출시가 없어요. ‹ › 로 다른 달을 둘러보세요." + 가능하면 "다음 출시(N월)로" 버튼. 리스트 뷰 빈 상태와 톤·문구 일관.

3. **헤더 '마지막 업데이트: 2026.05.29' 텍스트 대비 부족(거의 안 보임)** — 우선순위: 보통
   - 어디서: 헤더 부제 아래 업데이트 날짜. 다크 배경에 매우 흐린 회색이라 대비 4.5:1 미달로 보임(거의 비가시).
   - 개선: 색을 한 단계 올리거나(현 톤→ #9aa0ac 수준), 푸터에 이미 "데이터 마지막 갱신: …(N시간 전)"이 더 정확히 있으므로 헤더의 이 줄은 제거하고 푸터로 일원화하는 것도 깔끔(헤더 컴팩트화 제안과도 시너지). 둘 중 하나 택1.

4. **캘린더 셀 게임명 라벨 가독성(데스크톱 여백 활용 부족)** — 우선순위: 낮음
   - 어디서: 캘린더 셀 .day-game-label(0.7rem, 흐린 톤). 데스크톱 셀은 폭 여유가 큰데도 라벨이 작고 흐려 스캔이 어렵고, 긴 제목(예 '패스 오브 엑자일 2: 새 시즌…(0.5.0) 시작')은 거의 셀 끝까지 참.
   - 개선: 데스크톱(≥768px) 셀 한정 라벨 0.7→0.78rem, 색 한 단계 ↑. 그날 2건 이상이면 첫 게임 라벨 + "외 N건" 보조 텍스트로 점(dot) 정보와 통합. (모바일 셀은 현행 유지)

### 현재 양호 (트집 X)
- 리스트 뷰 단일게임 풀폭 행 + 4px 컬러 배너(중복 제거): 직전 사이클 반영 확인, 가로 낭비 해소됨. OK.
- 퀵칩(이번 주/다음 주/위시리스트) 활성 상태: 파란 보더로 토글 피드백 명확. OK.
- 오늘 셀(파란 강조)·선택 셀(주황 보더)·임박 셀(.day-soon) 색 위계, 푸터 운영자 정보·mailto hover: 양호.

---


## [2026-05-29 16:31] [디자이너]
운영자 추가 결정: 날짜 클릭 패널을 **한 줄 컴팩트 행**으로 전환(표시 범위는 현행대로 클릭일 이후 전체 유지).

### 개선점
1. **날짜 클릭 패널 = 한 줄 컴팩트 행 리스트** — 우선순위: 높음 (운영자 요청)
   - 어디서: 캘린더 날짜 클릭 시 아래 펼쳐지는 "OO 이후 출시 N건" 패널. 현재 각 항목이 설명·개발사·플랫폼·장르태그 전부 펼친 풀카드라, 25건이면 60vh 스크롤 박스가 매우 답답.
   - 개선: 항목을 **한 줄 행**으로 축약 → `[카테고리 색점] 게임명(한) · 플랫폼 · D-day · ☆`. 영문명/설명/장르태그/개발사는 행에서 제거하고, 행 클릭 시 **기존 상세 모달(openModal) 재사용**으로 상세 노출.
   - 행 높이 약 40~48px(터치 타겟 확보), 날짜 그룹 헤더(YYYY.MM.DD (요일))는 유지. 표시 범위(이후 전체)는 변경 없음 — 밀도만 낮춤.
   - 비고: 리스트 탭 본문 카드는 현행 유지(상세 카드가 필요한 화면). 이 컴팩트 행은 '캘린더 날짜 클릭 패널'에만 적용.

---

## [2026-05-29 16:24] [디자이너]
운영자(쌀먹닷컴) 직접 요청 반영. Chrome 실측(데스크톱 1280px) 후 정리.

### 발견한 문제 / 개선점

1. **헤더 좌측 정렬 + 컴팩트화 → 캘린더 위로** — 우선순위: 높음 (운영자 요청)
   - 어디서: 페이지 상단. 가운데 정렬된 제목/부제/"마지막 업데이트" + 뷰토글 + 통계줄 + 필터가 상단 약 310px를 차지해 캘린더가 한참 아래에서 시작(첫 화면에 한 달이 다 안 보임).
   - 개선: 제목(🎮 게임 출시 캘린더)·부제·업데이트를 **좌측 정렬 1~2줄로 압축**(header padding 2.5rem→1~1.25rem, text-align:left), 뷰토글·필터를 그 옆/아래 한 줄로 붙임. 캘린더 시작 위치를 약 150~200px 위로 올려 첫 화면에 월 전체가 들어오게.

2. **캘린더를 항상 기본 뷰로(선택 기억 정책 확정)** — 우선순위: 보통 (운영자 요청)
   - 현황: 현재도 캘린더가 기본 뷰임(확인 완료).
   - 개선: 마지막 선택을 localStorage로 기억하는 경우, 리스트를 본 뒤 새로고침하면 리스트로 복귀할 수 있음. 운영자 의향은 "항상 캘린더로 시작" → 진입 시 뷰를 무조건 캘린더로 초기화(또는 localStorage 미사용). 개발자는 현재 동작이 어느 쪽인지 확인 후 캘린더 고정으로 맞출 것.

### 보류 / 운영자 결정 사항
3. **날짜 클릭 패널** — 운영자 결정: 표시범위 현행 유지 + **카드→한 줄 컴팩트 행 전환은 진행(16:31 제안 참조)**.
   - 참고 메모(즉시 구현 X): 패널의 답답함은 표시 "개수"보다 "카드 밀도" 문제. 내용을 다 두더라도, 추후 원하면 풀카드 대신 **한 줄 컴팩트 행**([카테고리 색점] 게임명 · 플랫폼 · D-day · ☆ → 클릭 시 상세 모달)으로 바꾸면 25건도 가볍게 훑힘. 지금은 미진행, 향후 선택지로만 보관.

---

## [2026-05-29 15:51] [디자이너]
실측: https://gcalen.com/ Chrome 스크린샷(데스크톱 1280px, 캘린더·리스트 뷰 둘 다) + computed style 점검.
모바일은 브라우저 뷰포트가 리사이즈에 반영되지 않아 실측 스크린샷 대신 배포 CSS/getComputedStyle로 평가(다음 사이클 재시도 권장).

### 발견한 문제 / 개선점

1. **리스트 뷰 가로 공간 대량 낭비** — 우선순위: 높음
   - 어디서: 리스트 뷰. 날짜별 그룹(.date-group)마다 카드 컨테이너가 3열 그리드(약 378px×3)인데 대부분 날짜에 게임이 1개뿐.
   - 왜: 카드가 왼쪽 1열에만 박히고 우측 약 780px(2/3)가 빈 공간 → 데스크톱에서 매우 휑하고 스캔 비효율.
   - 개선: 리스트 뷰를 **가로형 풀폭 행 카드**(컬러 배너/썸네일 좌측 ~120px + 정보 우측)로 전환. 또는 단일 게임 날짜는 단일 컬럼(max-width 560px), 한 날짜에 여러 게임일 때만 다열. 가장 임팩트 큰 항목.

2. **카드 상단 그라데이션 배너가 카테고리 라벨을 중복 표시** — 우선순위: 보통
   - 어디서: 게임 카드 상단 컬러 배너.
   - 왜: 배너 안 텍스트("글로벌 대작"/"한국 MMO 신규 서버")가 바로 아래 카테고리 뱃지와 동일 정보 중복 + 세로 약 90px 낭비. 실제 게임 아트가 아닌 단색 그라데이션이라 정보 가치 낮음.
   - 개선: 배너에 게임명/썸네일을 넣어 가치를 주거나, 배너 제거 후 카드 상단 4px 컬러 보더로 카테고리 표현해 콤팩트화.

3. **터치 타겟 부족(모바일)** — 우선순위: 보통
   - 어디서: 뷰 토글/필터 버튼 높이 37px, 위시리스트 별 ☆ 약 18px(실측 getBoundingClientRect).
   - 왜: 모바일 권장 최소 44px 미만 → 특히 별 아이콘 오탭 유발.
   - 개선: 버튼 min-height:44px, 별은 클릭 패딩으로 40px+ 히트영역 확보(아이콘 시각 크기는 유지 가능).

4. **캘린더 셀 정보·접근성 보강** — 우선순위: 보통
   - 어디서: 캘린더 뷰 날짜 셀(현재 카테고리 색 점 + title 툴팁까지 구현됨).
   - 왜: 색 점만으로는 무슨 게임인지 클릭/호버해야만 확인 가능(스캔 불가). 카테고리를 색으로만 전달 → 색각 이상 사용자 구분 어려움(a11y).
   - 개선: 공간 여유 있는 데스크톱 셀에는 게임명(말줄임) 1~2개 + "+N" 텍스트 노출. 점에 aria-label 부여, 색 외 형태/라벨 보조 단서 추가.

5. **태블릿 구간 반응형 브레이크포인트 부재** — 우선순위: 낮음
   - 어디서: 전역 CSS. 미디어쿼리가 max-width:480px 하나뿐.
   - 왜: 481~768px(태블릿)용 중간 레이아웃이 없어 그 구간에서 필터 행 과밀 또는 그리드 빈 컬럼 발생 가능.
   - 개선: max-width:768px 브레이크포인트 추가 — 필터 2행, 그리드 1~2열로 정리.

### 현재 양호 (트집 X)
- 본문/메타 텍스트 대비: 직전 개발자 사이클(#9aa0ac/#cdd2db 등 상향) 후 다크 배경에서 또렷함. OK.
- 오늘 셀 강조(파란 보더 + "오늘" 라벨), 카테고리 색상 팔레트 일관성, 검색+필터 한 줄 정렬: 양호.

---
